# CANVAS_UNIFIED_UI_DESIGN.md — superseded

**Folded into `docs/CANVAS_ARCHITECTURE.md` — the single source of truth for the canvas.**

The unified "one canvas, three surfaces, one interaction layer" design (doc/pdf fluid · ppt discrete
section-per-slide with overlays · xls grid + chart UI + ribbon; the shared `OverlayLayer` /
`ActOnSelection` / `AssistPanel`) now lives in **`docs/CANVAS_ARCHITECTURE.md`** — §2 (as-built surfaces),
§3 (the direction), §7 (phased path). Nothing here is authoritative; see the canonical doc.
