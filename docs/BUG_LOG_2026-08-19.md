# Bug log — full-project sweep (2026-08-19 →)

Every bug found during the full-project analysis. Each entry records the bug, **how it was
proven**, and the **class** it belongs to — because the point of the log is to turn one finding
into a sweep. A bug with no class is a bug half-used.

Status: `OPEN` · `FIXED` · `WONTFIX (reason)` · `NOT-A-BUG (why)`

---

## B1 — The heartbeat did not supervise the pipeline worker · FIXED
**Where** `frontend/scripts/sandbox-heartbeat.sh`
**Proven** The loop checks Postgres, the emulated-Claude endpoint and the frontend. Nothing checks
:8080. The worker died repeatedly through the overnight session while every heartbeat line read
`srv=ok emu=ok db=ok`.
**Why it matters** The worker is the component that RUNS the workflows. A monitor that reports
healthy while the automation engine is dead is worse than no monitor: it actively argues against
the symptom you are seeing.
**Fix** Added `start_worker` + a `wrk=` field. Starts on the OWNER connection per
docs/RLS_CUTOVER.md ("pipeline = owner") — as `govtech_app` every workflow dies writing
`process_instances`.
**Class → sweep for** *supervised-set gaps*: any monitor/health/status surface that reports on a
subset of the things it implies it covers. Check `/admin/workflows` Live monitor, the Command
Center System lane, and the failed-workflow alert row.

## B2 — "Never double-starts" guard depended on a binary that is not installed · FIXED
**Where** `frontend/scripts/sandbox-heartbeat.sh`
**Proven** `command -v ss` → missing in this container. The guard was
`ss -ltn | grep -q ':3000'`, so it ALWAYS evaluated false; on any cycle where `/login` was not yet
200 the script fell through to `start_server`. A Next standalone boot takes several seconds, so a
restart reliably spawned a second server, which then fought for :3000.
**Why it matters** The file header states "never double-starts if something already owns :3000".
The code promised something it could not do, and the failure mode was silent contention rather
than an error.
**Fix** `port_busy()` — `fuser` (present) first, `ss` retained as a fallback for hosts that have it.
**Class → sweep for** *unavailable-dependency guards*: any shell/tooling path that branches on a
command's success where the command may be absent. `command -v` before use, or pick a probe that
is guaranteed present. Also the general shape: **a comment asserting a guarantee the code does not
implement.**

---
## B3 — The admin Workflow Map showed 29 of 34 workflows and called it complete · FIXED
**Where** `frontend/app/admin/workflows/workflow-shapes.ts` (consumed by `workflow-map.tsx`)
**Proven**
```
catalog entries: 29     ← grep -oE "workflowName: '[A-Za-z0-9]+'"
pipeline classes: 34    ← grep -c 'class .*(Workflow):' pipeline/src/workflows/*.py
missing: OnIngestPhaseRequested{Extract,Matrix,Review,Molds}, OnPortalStageReviewRequested
```
`workflow-map.tsx:247` computes `const total = WORKFLOW_SHAPES.length` and renders
`{total} workflows · discovery + build spines + platform`. The count is derived from the catalog
itself, so it is **self-consistently wrong** — it can never notice it is missing anything.
**Why it matters** The four Ingest Studio phases are the ENTIRE gated ingest spine: the path an
admin uses to bring a solicitation in. They were invisible on the map, and a live instance of one
had no shape for the monitor to overlay its step status onto. The file header asserted "kept 1:1
with the Python registry (29 templates, verified file:line)" — the assertion was the stale part.
**Fix** Added all five shapes with DAGs read from the Python (including the loop-generated
`refute_0..2` colour-team steps at COLOUR_TEAM_SLOTS=3 and the shared `advance_phase` tail).
Header and the "29 templates" section comment now state 34 with the command to re-derive it.
**Class → sweep for** *self-referential counts*: any UI that reports "N things" where N is
`someLocalArray.length` rather than a count of the real population. The number cannot fail; it
just quietly describes the wrong set. Also: **a comment claiming 1:1 fidelity with another
system** — that is a promise nothing enforces.

## B4 — The Workflow Map displayed a trigger the workflow does not listen to · FIXED
**Where** `frontend/app/admin/workflows/workflow-shapes.ts` — `OnSolicitationReviewRequested`
**Proven** Catalog said `finder:solicitation.triaged:end`; the class declares
`finder:solicitation.review_requested:single`. Systematic diff of all 34 catalog triggers against
the Python found exactly this one mismatch.
**Why it matters** PATTERN_AUDIT HIGH-1 re-pointed that workflow at the tool's event because only
that one fires from the workspace (the triage route's `solicitation.triaged` is the legacy twin).
The fix updated the workflow but not its mirrors — so the admin console showed the dead trigger,
and `tests/test_pod4_wiring.py` asserted it too (fixed separately, commit c12c41d5).
**Class → sweep for** *un-mirrored corrections*: when a fix re-points a contract, find every
MIRROR of it — tests, catalogs, docs, UI labels. One source of truth changed; the copies did not.
Two copies of this one were stale in different languages.

## B5 — The live sandbox DB was 18 migrations behind the tree · FIXED (environment)
**Proven** `_migration_history` held 175 rows, head `176_seed_program_guide_drafts.sql`; disk had
193 files. `information_schema.columns` had **no** `volume_required_items.character_limit`.
**Why it matters** Not a code defect — but every character-limit feature silently did nothing on
this box, and five tables migs 177–190 create did not exist. Any E2E run would have been testing a
schema 18 versions old while the code assumed head. Independently surfaced by the docs audit.
**Fix** Ran `db/migrations/migrate.mjs` against the live DB: 17 applied, now 192 rows at head
`193_character_limits.sql`; 110 → 115 tables; all three character-limit columns present.
**Class → sweep for** *code-vs-schema drift on a long-lived box*: the migration runner is only run
on demand, so a sandbox silently rots behind the tree. The heartbeat could assert head parity.

---

# Quality bar — measured from the hand-built reference

`MIRAGE_OSW26BZ04_DP013_Patent_Holiday_Technical_Volume.pdf` is the standard the generated volumes
are judged against. Measured, not estimated:

| Property | Reference | My last run | Gap |
|---|---|---|---|
| Pages | **10 of 10** (envelope filled) | ~7 | under-filled |
| Text characters | **36,701** | 19,298 | ~half density |
| Images | **44** across 5 pages (4·11·12·3·14) | **0** | absent entirely |
| Font faces | **9** — Times NR reg/bold/italic/bold-italic · Arial reg/bold/italic · Symbol · Wingdings | 1 | no inline variation |
| Page size | Letter 612×792 pt | same | ✓ |

**The rule this sets:** fill the envelope deliberately. A 10-page cap means ~9.9 pages; a
3,500-character cap means just under 3,500. Under-filling is a failure mode, not a safe default —
an evaluator reads a half-empty volume as a half-made proposal.

**Surface that must be exercised** (Word · PPT · XLS): images, captions, inline bold/italic runs,
section headings, page headers/footers, tables, charts, bullet glyphs.

## B6 — Six RLS tables seq-scan on the exact column their policy filters · FIXED (mig 194)
**Proven**
```sql
-- RLS-enabled + has tenant_id + no index whose leading column is tenant_id
purchases · agent_task_queue · notification_read_state ·
proposal_amendment_flags · shadow_admin_grants · vault_members
-- purchases' unindexed FKs: tenant_id, opportunity_id, proposal_id  (all three)
```
Each carries a `tenant_isolation` policy reading
`tenant_id = current_setting('app.tenant_id')` in both USING and WITH CHECK. With no index the
policy is not a shortcut — it is extra work applied per row on a sequential scan.
**Why it matters** `purchases` grows monotonically with the business (a row per sale), is read on
every portal load to decide what the tenant owns, and had **none** of its three FKs indexed.
`agent_task_queue` is polled continuously by the fabric. Cheap to fix now precisely because the
tables are still small.
**Fix** mig 194 — the six policy-column indexes, purchases' other two FKs, and five
cascade/join-relevant FKs (`tenant_bucket_scores.bucket_id` is ON DELETE CASCADE and was scanning
the whole scores table per bucket delete). All `IF NOT EXISTS`. Gap query re-run after apply:
empty.
**Class → sweep for** *the index that should follow a policy*: whenever a table gains RLS on a
column, that column needs an index. Add it in the same migration that adds the policy.

## B7 — Three `sql<>` assertions declaring snake_case on a camelCase client · FIXED
**Where** `atoms/bulk/route.ts:81` (`atom_id`) · `scripts/drive-foundation-tvsf.mts:202`
(`artifact_id`) · `scripts/drive-navair-build.mts:374` (`artifact_id`, `volume_name`,
`volume_number`)
**Proven** `lib/db.ts` applies `postgres.toCamel` to both clients, and the transform ignores
quoting — an aliased `"volume_name"` still arrives as `volumeName`. All three files import that
transformed client. `drive-navair-build.mts:417` declares `artifactId` and reads `row.artifactId`
correctly, which makes :374 in the same file the outlier.
**Severity: LATENT, not live.** None of the mis-declared fields is read today (only `rows.length`,
`row.id`, `title`, `version`). Fixed anyway because the type actively lies: tsc trusts the
assertion, so the first person to read `row.artifact_id` gets `undefined` with no warning. This is
the class CLAUDE.md records as having shipped twice.
**Not-a-bug, checked:** ~40 files build their OWN untransformed `postgres()` client, where
snake_case declarations are CORRECT. A naive sweep would have "fixed" those into real bugs —
`scripts/drive-collaborator-boundary.mts:42` declares `password_hash` and reads it at :54, which
works precisely because that client has no transform.
**Class → sweep for** *assertion-vs-runtime mismatch*: a type assertion is not a check. Anywhere a
declared shape is asserted rather than derived, the compiler is repeating your claim back to you.

---

# Round 2 — the two audits (pipeline agents/workflows · frontend UI), each finding re-proven here

## B8 — Two routes skip `enterTenant`, so under live RLS the librarian is never queued · FIXED
**Where** `atoms/atomize-package/route.ts` · `atoms/upload/route.ts`
**Proven — twice, live, as `govtech_app`:**
```
-- document_cocoons bind (atomize-package:106)
without app.tenant_id → 0 rows      with it → 1 row     (the tenant's OWN cocoon)
-- agent_task_queue INSERT (requestAgentTask)
without app.tenant_id → 42501 "new row violates row-level security policy"
with it               → INSERT SUCCEEDED
```
`lib/db.ts:216-218` states it outright: *"a route that calls verifyProposalAccess must ALSO call
`enterTenant(tenantId)` in its own frame"* — context flows parent→child, never child→parent.
**Why it matters** Two distinct user-visible failures from one omission:
1. The DSIP sidecar attach returned **404 "attachToCocoonId is not one of your past proposals"**
   for a cocoon the tenant genuinely owns.
2. `requestAgentTask` catches its own error and `return null`s — so the librarian enqueue was a
   **silent no-op on every upload**. Compounding B9: the intended producer was dead, and the
   accidental one (the archetype fallback) was broken.
**Fix** `enterTenant(tenantId)` right after `verifyTenantAccess` in both, with the proof in the
comment. `atoms-upload-auto.test.ts` caught the missing mock export — its db mock now includes it.
**Sweep run (bounded, complete):** every `app/api/**/route.ts` calling `verifyTenantAccess` without
`enterTenant` (38 routes) × bare-`sql` reads of the 36 RLS-enabled tables, one level into imported
libs. **Exactly these two.** The other 36 use `withTenant`/`sqlBypass` explicit clients — correct.
**Class → sweep for** *a swallowed write*: `catch { return null }` around a write turns an authz
failure into "nothing happened". Any best-effort producer needs its refusal distinguishable from
its no-op — and any RLS-forced write needs the context pinned in the handler's OWN frame.

## B9 — The archetype-fallback dispatch is dead for 32/36 agents, and double-fires the 33rd · FIXED
**Where** `pipeline/src/workflows/processor.py:802` · every `handles_event` declaration
**Proven** `processor.py` passes the bare `event_dict["type"]` to `fabric.has_handler`. Live DB:
```sql
select distinct namespace, type from system_events;
library | package.atomized      finder | solicitation.update_scan_requested
proposal | compliance.checked   identity | user.logged_in            …
```
`type` carries **no namespace prefix** — it is its own column. Yet `curation_qa` declares
`"finder.solicitation.triaged"`, `scoring_strategist` `"finder.scoring.completed"`,
`compliance_reviewer` `"proposal.section.drafted"` … 32 archetypes declare a string that can never
match. Only `librarian` registers the bare forms (`package.atomized`, `document.locked`), and no
workflow claims those — so it is the sole archetype the fallback actually reaches.
**Why it matters** The upload routes emit `library:package.atomized:single` **and** enqueue an
explicit librarian task. The `single` phase passes the `phase != 'start'` guard → the librarian ran
**twice per upload**, the second time off an event payload of `{filesProcessed, totalAtoms, source}`
— no cocoonId — producing the literal prompt `Catalog the atoms of package (cocoon) .` Both
`document.locked` emitters are payload-poor the same way. That contradicts
docs/AGENT_WORKFORCE.md:386 ("one task per package… enqueue is idempotent") and spends a tenant's
agent budget on a request that names nothing.
**Fix** A payload-aware second gate, `handles_dispatch(event)` on the base archetype (defaults to
`handles_event(type)`, so every other archetype is untouched); `fabric.handle_event` calls it;
`librarian` overrides it to require a cocoonId. **Deliberately NOT fixed** by prefixing the dispatch
key: that would wake ~32 dormant archetypes in one commit, against CLAUDE.md's "woken one at a
time". `test_agent_event_dispatch.py` now pins the bare-type contract so nobody "fixes" it that way
by accident.
**Class → sweep for** *a match on the wrong key shape*: any string compared across a boundary where
one side composes (`namespace:type`) and the other does not. It fails silently — no error, just a
handler that never runs. Also: **a capability whose only proof it works is that it was declared.**

## B10 — `section_drafter` fences the RFP excerpt and leaves three siblings of the same provenance bare · FIXED
**Where** `pipeline/src/agents/archetypes/section_drafter.py:158,184-193`
**Proven** `evaluation_criteria` is read from `solicitation_compliance.evaluation_criteria`
(`workflows/actions/draft_v0.py:56-72`, passed at `:249`) — AI-extracted from the very
`curated_solicitations.full_text` that `rfp_excerpt` is fenced against ten lines above.
`section_title` is `proposal_sections.title`, tenant-editable free text.
**Why it matters** Extraction does not launder trust. An imperative lifted from a poisoned RFP into
a "criterion" reached the model **in instruction position**, directly under the comment explaining
why that is dangerous. Solicitations are the SHARED master — one poisoned RFP hits every tenant's
auto-draft.
**Fix** All three fenced with the canonical markers + forged-close neutralisation, via one local
`_safe()` the excerpt now shares. `tests/test_injection_fences.py` pins wrapping AND inescapability,
and separately pins that the content still REACHES the model (a fence must not silently drop text).

## B11 — The web fences were escapable by the most hostile input in the system · FIXED
**Where** `research_scout.py:45-47` · `market_analyst.py:47-49` — `fence_web()`
**Proven** Both did a bare `f"{FENCE_OPEN}\n{text[:limit]}\n{FENCE_CLOSE}"`. Executed against a
page containing the literal close marker: **2 close markers in the output**, with the attacker's
text after the first — i.e. outside the fence. After the fix: 1, at the end, payload contained.
**Why it matters** These two are the only archetypes that ingest the OPEN INTERNET. A fence that
wraps but does not neutralise is decorative exactly where it matters most.
**Fix** Neutralise the forged close before wrapping, mirroring `ContextAssembler._wrap` and the
`section_drafter` excerpt fence.
**Class (B10+B11) → sweep for** *a fence with one of its two jobs*: WRAPPED and INESCAPABLE are
separate properties and each fails alone. And *"the field next to the careful one"* — B10 is the
shape where the dangerous sibling sits ten lines from the paragraph describing the danger.

## B12 — Recording a WIN produced a To-do whose only button 404s · FIXED
**Where** `lib/tasks/completers.ts:139` → `/portal/{slug}/contracts/{id}`
**Proven** `find app -ipath "*contract*" -type d` → **empty**. The route is reachable, not
theoretical: `proposals/[p]/outcome/route.ts:351-357` creates a real `contracts` row and launches
the kickoff gate with `entityType:'contract'` on `outcome=awarded`; `taskHref` is consumed by
`task-queue.tsx:245`, `lib/admin/review-queue.ts:189`, `lib/partner/todos.ts:59`.
**Why it matters** The single best moment in the product — an award — ends at a 404.
**Fix** Built the page. Read-only (a contract is a record; nothing in V1 mutates one from the
portal), tenant-bound on `id AND tenant_id`, `enterTenant` pinned, and it links back to the winning
proposal via `contracts.proposal_id`.
**Also found, NOT fixed (logged, latent):** the sibling `section` branch resolves
`params.proposalId`, but the pipeline writes only `{task_type_raw, entity_ref}` into
`tasks.params` (`manager.py:916-920`) — live DB confirms the only key present anywhere is `kind`.
That deep link is dead the same way, and fixing it means threading `proposalId` through the
workflow manager. Not done here: engine change, no live consumer today.

## B13 — The admin rail offered `rfp_admin` a link that ejects them off the product · FIXED
**Where** `components/admin/admin-nav-data.ts:86` (sidebar) · `app/admin/command/page.tsx:201` (Command Center)
**Proven** `/admin/system` is master_admin-only **twice** — `lib/rbac.ts:135` and the page's own
guard. `app/admin/layout.tsx` rendered `ADMIN_NAV` whole with **no role filtering**, and
`middleware.ts:206` denies with `redirect('/')` — the public marketing homepage.
**Why it matters** Not a 403 screen, not a message: an `rfp_admin` clicking "System Health" is
thrown out of the admin console onto the marketing site with no explanation.
**Fix** `visibleAdminNav(role)` + the same filter on the Command Center actions — both **derived
from `requiredRoleForPath`**, the same table middleware enforces, rather than a second hand-kept
`minRole` field. New master_admin-only routes need no change here.
**Class → sweep for** *a nav that is not the authz*: any link list rendered independently of the
gate it leads to. The fix that lasts derives from the gate; the fix that rots restates it. (This is
B4's un-mirrored-corrections class again, in the other direction.)

## B14 — Buttons rendered to roles the API refuses · FIXED (4 surfaces)
| Control | Rendered to | Server floor | Result |
|---|---|---|---|
| **Purchase** (primary CTA on `/cards`) | delegated `tenant_user` (`canManageBuckets`) | `tenant_admin` | 403 "Insufficient permissions" |
| Library **Archive/Restore** | `tenant_user` | `tenant_admin` | 403 |
| **Save as template** | `tenant_user` | `tenant_admin` | 403 "Only an admin can save a template" |
| Trial banner **"Subscribe to keep your data."** | `tenant_user` | `/billing` redirects them back | click does nothing |
**Proven** Each row read from BOTH the component and the route/page it targets. `pipeline-cards.tsx`
is the sharpest: it **receives `role` and never reads it** — the prop was dead, and the only other
`role` in the file was the HTML `role="alert"`.
**Why it matters** `/cards` is where a designee lives, and its emerald primary CTA could only 403.
Worse, `library-review.tsx` counted only `r.ok`, so the refusal surfaced as
**"Nothing was archived."** — which reads as *there was nothing to do*, the opposite of *you may not*.
**Fix** Read the role. `canBuy` / `canArchive` / `canTemplify` threaded from the page (which already
computes it for its own redirect), plus a real message telling a base member who CAN act. The
archive loop now distinguishes 401/403 from an empty result.
**Class → sweep for** *the received-and-ignored prop*: a component taking `role` and never reading
it is a gate someone intended and did not finish. Also *the floor that differs from its page*: any
route whose minimum role is stricter than the page hosting its button.

## B15 — A failing extractor reported to the curator as "No topics found" · FIXED
**Where** `components/rfp-curation/curation-workspace.tsx:1180`
**Proven** No `resp.ok` check. `app/api/admin/extract-topics/route.ts:45` returns
`{ error: 'Topic extraction failed', code: 'DB_ERROR' }` on 500 — which **parses cleanly**, leaving
`json.data` undefined → `extracted = []` → `toast.info('No topics found…')`.
**Why it matters** The operator is told the solicitation HAS no topics. They then hand-enter what
the extractor would have given them, and the real failure is never reported.
**Fix** `resp.ok` first, surface `json.error`. Same treatment for `library-browser.tsx`, whose
missing check rendered a server failure as a confident, silent **empty library ("0 atoms")**.
**Class → sweep for** *the parseable error*: an error body that is valid JSON defeats a
`.then(r => r.json())` chain silently. This is the CLAUDE.md `res.ok` rule, and the tell is that the
failure state and the empty state render identically.

## B16 — Notification deep links landed on retired stubs that DROP their target id · FIXED
**Where** `lib/event-labels.ts:207-211`
**Proven** `/spotlights/[spotlightId]` redirects to `/cards` **without** the id.
**Fix** Point at `/cards?opp=<id>` and `/atoms` directly — and, because a query param nothing reads
is still a broken link, `PipelineCards` now reads `?opp=`, anchors each card, scrolls to it and
holds a highlight ring. Also `atomizer.tsx:233` → `/atoms?tab=review` (the Review tab it names,
not the Library tab the stub landed on).
**Class → sweep for** *the lossy redirect*: a retired stub that redirects to a LIST drops the
identity of the thing the link was about. Every deep link through a stub needs its id checked
end-to-end, and a param is only a link if something reads it.

## B17 — Two "reviewed" verdicts asserted without evidence the reviewer ran · FIXED (2026-08-19)
**Where** `pipeline/src/workflows/actions/ingest_actions.py:136-148` ·
`portal_stage_actions.py:71-88` → `frontend/lib/portal-workflow.ts:511-533`
**Proven** `record_ingest_review` writes
`status='reviewed'`, `review={"resolution": …, "recorded_by": "advisory_manager"}` where
`recorded_by` is a **hardcoded string literal** and `resolution` comes from `payload.resolution`,
never from an agent result. The step is `ACTION` with `depends_on=None` — correct per the
hard-step-behind-AI invariant — so when the AI steps safe-skip (no key, rate-limited, fabric
absent) the draft is still stamped reviewed. `record_stage_review` is the same shape and its
`verdict:'reviewed'` is what `portal-workflow.ts:511-517` uses to **auto-advance a portal stage**.
**Why it matters** A safe-skipped agent is indistinguishable from a clean pass. The comment at
`portal-workflow.ts:512-514` states the intent ("'the cohort ran' is not 'the cohort found
nothing'") — but "ran" is never actually checked.
**Fix** The THIRD signal, as scoped. `pipeline/src/workflows/actions/cohort_evidence.py` turns the
ENGINE's own record of what each AI_INVOKE step did into a verdict; `manager._execute_step` passes
it, and `processor._execute_action` injects it as `_ai_step_status` **only into actions that
declare that parameter** (a `**_ignored` catch-all does not count — injecting into one would make
the signal look wired and be inert). Both invariants survive: the record step still declares no
`depends_on` and still maps nothing from the cohort's results, because what it receives is
execution provenance, not agent output. It can learn that the reviewer ran; it still cannot learn
what the reviewer said. `not_reviewed` ⇒ the ingest draft keeps its status (a draft nobody read
stays staged), the portal gate emits `verdict='not_reviewed'`, `autoGateRefusal` refuses to
auto-advance on anything but an affirmative pass — a NULL verdict no longer passes — and the gate
panel turns amber and says "AI review did NOT run" with an "Advance anyway →" button. A human may
still close a gate the AI never read; they may not be told it was read.
**Found while fixing it — the deep half (see B30)** The first fix READ `step_status == 'completed'`
and was itself wrong, which the live drive caught and the unit tests could not: the engine records
a SAFE-SKIP as *completed*, so a cohort that never ran would have been certified 'reviewed' again,
now with evidence-shaped packaging. `manager._effective_step_status` reads the step's RESULT
(`{"skipped": True}`) rather than trusting the status string.
**Proof** `pipeline/scripts/drive_b17_evidence.py` — the real engine, real DB, one workflow, two
runs: fabric absent ⇒ `not_reviewed` + the summary says so; fabric present ⇒ `reviewed` + the
evidence names the step. Both runs COMPLETE (no dead-end). Unit: `tests/test_cohort_evidence.py`
(17), `tests/test_ingest_actions.py`, `frontend/__tests__/auto-gate-evidence.test.ts` (9).
**Class → sweep for** *provenance asserted by literal*: any `recorded_by` / `reviewed_by` /
`verified_by` written as a constant rather than derived from the actor that did the work. And its
partner, *the absence that reads as a pass*: any gate where "no findings" and "nothing ran" produce
the same value.

---

## B30 — The engine records a safe-skipped step as "completed" · LOGGED (deliberately not fixed)
**Where** `pipeline/src/workflows/manager.py` (the `if step_result is not None:` branch) ·
every safe-skip return in `pipeline/src/workflows/processor.py`
**Proven** Live, through the real engine (`scripts/drive_b17_evidence.py`, RUN 1). With no fabric
the log says `AI_INVOKE action 'tool.advisory.reconcile' skipped (fabric=False, mapped=True)` and
the persisted record says:
```json
{"record": "completed", "review_manager": "completed"}
```
Every safe-skip returns `{"result": None, "skipped": True, "reason": …}` — a truthy dict — and the
manager writes "completed" for any step whose executor returned anything at all.
**Why it matters** Two live consequences. (1) It is what nearly defeated the B17 fix: any check
reading `step_status` cannot tell a step that worked from one that declined to. (2) The
`/admin/workflows` instance overlay reads the same map, so an operator watching a run sees a green
"completed" AI step that never called an agent — the monitor showing a healthy cohort in a
deployment with no API key.
**Why not fixed here** `depends_on` treats `'skipped'` as a reason to skip dependents. Writing the
honest status into `step_status` would cascade: `reconcile` (depends_on `refute_0`) would start
skipping whenever the refuters safe-skip, and the same shape repeats across the full-draft and
Studio workflows. That may well be the RIGHT behaviour — reconciling nothing is pointless — but it
is a semantics change to the engine with real blast radius, and it belongs in its own change with
its own drive, not inside a bug fix that had to ship correct today. B17 is closed without it:
`manager._effective_step_status` derives the truth from the step RESULT and leaves the stored
record alone.
**Class → sweep for** *a sentinel that collapses into its success case*: any place where "did
nothing, successfully" and "did the work" are written to the same field with the same value.

---

# Round 3 — canvas quality (found by BUILDING the ruler and pointing it at real proposals)

## B18 — Drafting threw away every table and every emphasis marker · FIXED
**Where** `pipeline/src/document/markdown_to_canvas.py`
**Proven** `_strip_inline` (its own docstring: *"Drop markdown emphasis markers for v1 (no
inline_formats yet)"*) ran four `re.sub` calls that DELETE `**bold**`, `__bold__`, `*italic*` and
`` `code` ``. The scanner handled headings, bullets, numbers and paragraphs — nothing else. So a
drafter that wrote a milestone table and bolded its key claims produced a canvas containing
neither. Live confirmation on the tenant's own library: 1,010 atom nodes across 224 atoms, of
**exactly four types** — text_block 845 · bulleted_list 144 · numbered_list 19 · heading 2.
**Why it matters** This is the "one font face vs the reference's nine" gap at its source, and it
is upstream of everything: the editor, the compliance floor, and all four exporters can render
emphasis and tables perfectly — the content just never arrived carrying any.
**Fix** `parse_inline()` returns `(plain_text, inline_formats)` instead of discarding the markers,
computing offsets against the FINAL string in one left-to-right pass (successive `re.sub` calls
would leave every offset to the right of a substitution stale — and a run whose `start+length`
exceeds the text is not cosmetic, the docx writer indexes into the string with it). Added tables,
blockquotes, fenced code and horizontal rules. Verified before relying on it: every new node type
is handled by `canvas-renderer.tsx` AND all four exporters, and `inline_formats` is honoured in
html/docx/pptx — so this reaches the downloads, not just the model.
**Class → sweep for** *the lossy converter*: any transform between two formats where the target is
RICHER than what the transform emits. The tell is a comment excusing the gap ("for v1", "not yet")
that outlived the limitation — the canvas has modelled `inline_formats` and `table` the whole time.

## B19 — Every generated volume shipped with no running header and no page numbers · FIXED
**Where** `lib/ingest/molds.ts::buildMoldCanvas`
**Proven** Measured on a live 16-section proposal:
```
Technical Volume (narrative, 10 sections)   header NO · footer NO
Cover Sheet / CCR / Supporting Docs         header NO · footer NO
Cost Volume                                 header yes · footer yes   ← the only one
```
`buildMoldCanvas` spreads `CANVAS_PRESETS.letter_standard`, which sets `header: null, footer: null`.
`assembleArtifactCanvas` takes the FIRST section's canvas and only falls back to
`letter_sbir_phase1` (which does carry both) when no section has one — and a mold always has one.
So the fallback with the furniture was unreachable for every molded volume.
**Why it matters** Most agency BAAs require a running header and page numbers; the hand-built
reference volume for this solicitation carries both. The export routes were ALREADY passing
`company_name` / `project_title` / `topic_number`, and docx/pdf already turn `{n}`/`{N}` into live
page-number fields — the whole mechanism worked and nothing was using it.
**Fix** Molds now default to a header + page-numbered footer for any PAGINATED (non-spreadsheet)
item, using exactly those placeholders. `formatSpec` still wins, so a solicitation that mandates
its own header — or explicitly none — overrides it. A default, not a policy.
**Class → sweep for** *the unreachable fallback*: a default that only applies when an earlier
branch produces nothing, where that branch in practice ALWAYS produces something. The
sophisticated fallback is dead code and the bare path is what ships.

## B20 — My own warning fired where it could not apply · FIXED (self-inflicted, caught by running it)
**Where** `lib/proposal/document-furniture.ts::measureDocument`
**Proven** First live run printed *"No figures. A technical volume with no figure is a wall of
text"* against a **Cover Sheet**, a **Company Commercialization Report** and a **Cost Volume** —
and asked a spreadsheet for a running header, which has no page to put one on.
**Why it matters** A warning that fires where it cannot apply trains the reader to ignore the ones
that do. That is strictly worse than not warning at all.
**Fix** `measureDocument` takes the artifact type; figure/emphasis warnings are narrative-only,
page furniture is skipped for spreadsheets, and a caption gap still reports anywhere figures
exist. Defaults to the strictest reading when the type is unknown.
**Class → sweep for** *the unscoped assertion*: a check written against the case in front of you
and then applied to every case. Worth re-reading the other advisory surfaces for the same shape.

## B21 — "Full draft" silently skipped every registry-molded section · FIXED
**Where** `pipeline/src/workflows/actions/draft_v0.py::_has_prose`
**Proven** Mode C reported `sections: 2` on a **14-section** proposal. `draft_v0.py:209` excludes a
section where `content_source='template'` AND `_has_prose(content)`; `_has_prose` counts words in
non-heading nodes and returns true at **≥ 25**. Measured on this proposal's own archived molds:
```
507 words per section — 500 of them inside [brackets]        (20× the threshold)
residual words: form-table cells — "Proposal Number", "Topic Number", "Firm Name"
```
The docstring assumed a mold seeds "headings, a rules callout and EMPTY text blocks". But
`frontend/lib/provision-proposal.ts:275` falls back to the CODE REGISTRY when no admin-authored
mold is linked — **the default state of a freshly built-out master** — and those molds are a cover
sheet plus form tables plus long bracketed instructions.
**Why it matters** This is the headline feature doing nothing, silently, in the default case. It is
also the exact failure the comment at `draft_v0.py:183-190` says was already fixed ("every section
looked 'already drafted' and carried not one word of prose"); the registry-fallback path reopened
it, because those blocks are not empty — they are full of instructions.
**Fix** Two rules, both about telling STRUCTURE from CONTENT:
• a whole block that is one bracketed instruction is scaffolding (whole-block only — the drafter's
  own system prompt asks it to leave `[PLACEHOLDER: …]` markers *inside* real paragraphs, and those
  paragraphs are drafts that must never be clobbered);
• a table's cells are a form's LABELS. What makes a form filled is **values, not words** — so a
  table counts as content when any cell parses as a non-zero number, which protects a priced cost
  workbook by its actual distinguishing property rather than by accident of word count.
11 tests pin BOTH directions, because each failure mode is bad in a different way: a false positive
skips the section, a false negative clobbers a human's work.
**Class → sweep for** *the heuristic outliving its assumption*: a threshold tuned against one
producer's output, then applied to every producer. The tell is a docstring describing the input
("a mold seeds EMPTY text blocks") rather than checking it.

## B22 — AI drafting replaced the solicitation's page cap with the preset's · FIXED
**Where** `components/proposal/section-assist-bar.tsx:53` · `components/canvas/draft-all-sections.tsx:162`
**Proven** Live, every Technical Volume section of the T3CP build:
```sql
select page_allocation, content::jsonb->'canvas'->>'max_pages' from proposal_sections …
 10 | 15      ← ten rows, all identical
```
`provision-proposal.ts:290-293` stamps the item's REAL limit onto the canvas (10, for an NTE-10-page
Technical Volume). Both frontend landing paths then rebuild the document with
`createEmptyCanvas({ canvas: CANVAS_PRESETS.letter_sbir_phase1 })`, whose `max_pages` is hard-coded
**15** — so after an AI draft the section claimed a cap the solicitation never gave.
**Why it matters** This is a compliance-correctness bug, not cosmetics: the editor gauge AND the
export compliance floor both measure against 15. A 14-page volume passes every check in the product
and is refused by the agency. It also made my own quality measurement read `pages 4 / 15` on a
volume whose real allowance is 10.
**Fix** Both paths keep the provisioned cap (`{...preset, max_pages: pageLimit}`) — the same rule
the pipeline drafter already follows (`draft_v0.py:289-296`, "Prefer the section's OWN canvas
envelope when provisioning stamped one"). Three producers, one rule; two of them didn't have it.
**Class → sweep for** *the preset that overwrites provisioned state*: anywhere a default object is
spread over data that was deliberately computed upstream. Especially where a THIRD implementation
of the same rule already gets it right — that is the signal the rule was never made shared.

---

## B23 — (number skipped)
No entry was lost: the B24–B29 batch was appended with the count off by one. Recorded rather than
renumbered, so a reference to "B24" written down anywhere still points at the same defect.

---

## B24 — Retrieval never read the section's own words · FIXED

**Where** `frontend/lib/atoms.ts::selectForSection`, and the pipeline's mirror in
`pipeline/src/agents/archetypes/section_drafter.py::_search_library`.

**Proven** Four different technical sections of the Immobileyes T3CP volume — "Phase I Technical
Objectives", "Phase I Statement of Work", "Anticipated Performance Improvements…",
"Identification and Significance of the Problem" — each retrieved the **same six atoms**, all of
them fraud-waste-and-abuse boilerplate. Every one scored 6.5 with `ctxMatches=3`, `vectorSim=null`.

**Why it matters** The ranking axes were: scope-tag filter → context-tag count → (gated) vector
cosine → outcome score → usage count → `created_at`. Not one of them looks at the section's text.
On a real library every candidate carries identical tags and untouched score/usage defaults, so
the ordering collapsed to **`created_at DESC`** — the newest N rows, identically, for every
section. Every drafted section of the volume therefore opened with the same paragraph. No amount
of drafting quality downstream survives being handed the wrong paragraphs.

**Fix** A Postgres full-text rank axis (`ts_rank(…, 1|32)`, GIN index in mig 195), with the query
terms OR-joined so it measures COVERAGE rather than requiring every word of a seven-word section
title. Weighted so a strong lexical match ≈ one context tag — assist, never override. Ungated: no
API key, no extension, no backfill, which is the state most deployments are in.

**Class → sweep for** *A ranking function whose axes are all constant across the candidate set.*
Any ORDER BY whose leading terms tie for every row silently degenerates to its last tiebreaker.
Check the other scored selectors (bucket scoring, reuse index, atom pickers) the same way: run the
query for two DIFFERENT inputs and diff the result sets. Identical output is the tell.

---

## B25 — The reference fence kept out the folder and let out everything in it · FIXED

**Where** Both selectors, `a.grain <> 'reference'`.

**Proven** The DSIP *Fraud, Waste, and Abuse Tutorial* and the SBIR cover-sheet disclaimer —
uploaded as REFERENCE material — were the top-ranked drafting source for technical sections and
were being copied verbatim into the customer's Technical Volume.

**Why it matters** Atomizing a reference PDF produces `primitive` children that carry its text and
lose the label. The grain test excluded the uploaded container and nothing else. Reference material
exists for the system to READ; reusing it as the customer's own writing is exactly the
"no hand papering" failure, mechanized.

**Fix** Exclude anything DESCENDED from a reference document, by provenance (`source_anchor →
sourceAtomId → parent.grain`), not by keyword. Applied identically in both selectors.

**Class → sweep for** *A filter on a container that does not follow to its children.* Anywhere a
row has a parent/derived relationship (atoms→primitives, documents→sections, templates→instances),
a predicate on the parent's flag needs a matching predicate on the lineage.

---

## B26 — Every section searched the library for the literal word "Section" · FIXED

**Where** `frontend/scripts/test-harness/emulated-claude.mjs`, the pipeline drafter responder.

**Proven** The emulator log shows `search_library {"query":"Section"}` for all thirteen sections.

**Why it matters** The responder read the title with `/Draft the "X" section/`. The archetype's
prompt-injection hardening had already moved the (tenant-editable, untrusted) title inside a
`--- BEGIN USER CONTENT ---` fence, so the regex never matched and the literal default was used.
The output looked like a retrieval failure and was a title-parsing failure. The harness stands in
for the model; when it silently reads half the prompt it tests half the pipeline.

**Class → sweep for** *A parser coupled to a prompt's wording, with a silent default.* Every place
the harness or a consumer regex-extracts something a prompt emits, a failed match must be loud —
here, a named helper that documents both live shapes.

---

## B27 — The AI budget ran out and nobody was told · FIXED

**Where** `pipeline/src/workflows/actions/draft_v0.py`; `platform_agent_config.default_rate_limit_per_hour`.

**Proven** Worker log: seven consecutive `guardrail blocked section_drafter: Tenant … exceeded the
hourly call limit`, then `drafted 6, skipped 7`, and the workflow reported **completed**.

**Why it matters** Two problems, and the second is the bad one. (a) The default cap was 50/hour; one
Mode-C full draft of a 14-section proposal costs ~23 agent calls, so the second press in an hour is
refused partway. (b) The refusal was invisible: `draft_v0` logged "skipping" per section, kept
hammering a guardrail that would refuse every remaining call, and returned a clean-looking count.
The customer sees half a proposal drafted and no explanation anywhere.

**Fix** `draft_v0` stops on a guardrail refusal (every subsequent call fails identically) and
returns `blocked_sections` + the reason so the outcome and the notification can say so. Default cap
50 → 300/hour (mig 196) — still a runaway control; the monthly budget and platform cap are the
spend controls and are unchanged.

**Class → sweep for** *A per-item loop that swallows a global failure as N per-item skips.* Rate
limits, budget caps, expired credentials and closed connections are all global — the first failure
predicts the rest. Look for `for item in items: … except: skipped += 1; continue`.

---

## B28 — A compliant volume was reported as over budget · FIXED

**Where** `frontend/lib/types/canvas-document.ts::validateCanvasAgainstSpec`, `section_over_budget`.

**Proven** Technical Volume measuring exactly 10 of its 10 allowed pages exported with
`X-Compliance-Violations: 1` — "Phase I Statement of Work is estimated at 2 pages against its
1-page budget."

**Why it matters** A per-section `page_budget` is the OFFEROR's internal allocation of the agency's
cap (ten items sharing ten pages get one each). The agency caps the VOLUME. Ten one-page shares
leave zero slack for document furniture, so adding one figure produced a "violation" on a
submittable document.

**Fix** The check runs only when the assembled volume does NOT fit — which is when the number does
its real job: saying which section to cut.

**Class → sweep for** *A derived allocation enforced as a hard limit.* Any budget that was computed
by dividing a real cap is an estimate; enforce the real cap, and use the allocation to diagnose.

---

## B29 — "Page {page}" printed across the foot of every page · FIXED

**Where** `frontend/lib/proposal/volume-finish.ts::withPageFurniture`.

**Proven** Rendered PDF of the Supporting Documents volume, footer reading the literal
`Page {page}`.

**Why it matters** The exporters' own token is `{n}` / `{N}` (mapped onto Chromium's live
`pageNumber`/`totalPages` spans). Nothing type-checks a template string. Found only by rendering
the document and looking at it — no ruler, no test, and no measurement would ever have caught it.

**Class → sweep for** *A token written by one module and substituted by another, with no shared
constant.* Header/footer templates, email templates, filename patterns. And: **render the artifact
and look at it** — every defect in this last group (the truncated diagram labels, the unlabelled
cost slivers, the borrowed captions, this) was invisible to measurement.

---

## Verified-clean (negative results worth keeping)
- **Invariant 4a — a hard step gated behind an advisory agent: 0 violations** across all 34
  workflows (programmatic scan of `depends_on` AND `input_map` `step.<ai>.result` reads).
- **`validate()` genuinely rejects** an unmapped `AI_INVOKE`, a TODO without an assignee, and a
  `HITL_WAIT` without `wait_for` — verified by *executing* it against probe workflows, not by
  reading it. Gap: it does **not** check `timeout_minutes > 0` (a probe with 0 registered fine;
  runtime falls back to 1440, so latent).
- **No dead triggers** — all 25 trigger keys across 34 workflows have a producer. Two docs claim
  otherwise and are **stale**: AGENT_WORKFORCE.md:85-88 (`content_generator` "has no producer") and
  AUTOMATION_SPINE_MAP.md:14-16. Both producers are *dynamic* — `launch-template.ts` reads the
  trigger key from `process_templates.trigger_key`, so a literal grep finds nothing.
- **All 36 archetypes are registered and reachable** — 30 via an `AI_INVOKE` step, 6 via a queue
  producer or direct call. None orphaned.
- **No dead API targets, no HTTP method mismatches, no no-op handlers, no unregistered tool
  invocations** across 293 `fetch('/api/…')` call sites and 243 routes.
- **No native `alert()` anywhere** in `app/`, `components/`, `lib/`.

---

## B31 — The corpus regex was matching the letter "s", not whitespace · FIXED
**Where** `frontend/lib/library/corpus-verbatim.ts` (+ the backfill and drive scripts)
**Proven** Live, by the drive that exists to prove the feature works
(`scripts/drive-corpus-verbatim.mts`): it seeded one solicitation document, then asked whether that
document's own text appears in the corpus. It does not:
```
✗ the agency boilerplate IS recognised as the agency's words
```
The SQL read `regexp_replace(d.extracted_text, '\s+', ' ', 'g')` inside a **JavaScript template
literal**, where `\s` is not an escape — it is just `s`. Postgres received the pattern `'s+'` and
replaced every run of the letter S in the corpus with a space, so no normalized probe could ever
match anything. The fix is one character: `'\\s+'`.
**Why it matters** The failure is SILENT and reads as good news. The backfill had already been run
against the live library and reported *"0 atoms are the agency's words"* across 301 atoms — which
looks like a clean library and was in fact a detector that could not detect. Nothing would have
surfaced this in normal use: no error, no log line, just a feature that never fires.
**Why the unit tests could not catch it** They cover `corpusProbe`/`normalizeForCorpusMatch`, which
are pure and correct. The defect lives in the half that only exists once SQL runs. This is the same
lesson as B17's deep half, one day apart: a pure-function test proves the function, never the wire.
**Class → sweep for** *a backslash escape inside a JS template literal that is meant for another
language* — SQL regexes, `LIKE` escapes, JSON strings built by hand. And more generally: a checker
whose "nothing found" is indistinguishable from "never ran" needs a fixture that MAKES it find
something, or it is not proven.

---

# Round 4 — the end-to-end full-scope run (found by rebuilding from migration 001)

## B32 — A god-view admin still shipped with its credential committed to git · FIXED (mig 198)
**Where** `db/migrations/038_fix_admin_login.sql` · missed by `db/migrations/124_*.sql`
**Proven** Rebuilt the database from a dropped schema through all 197 migrations, then tried to log
in as every role (J1). `eric@rfppipeline.com` — a `master_admin`, which `lib/db.ts`
`verifyTenantAccess` gives unconditional access to every tenant — carried exactly what 038 set:
```sql
SET password_hash = '$2a$12$ssn42wVJWhpMuJl9MFP8KeFKaTgkruKTwEHSK/aHu52YDBb2NdrE6',
    temp_password = false   -- "so user is NOT forced to change on login"
```
Confirmed live: `temp_password = f`, hash cost `$2a$12$`, `is_active = t`.
**Why it matters** Migration 124 exists *specifically* to get committed credentials out of the
repo. It rotated the other master_admin (`eric.c.wagner@gmail.com`) to a random hash with
`temp_password = true` and hash-invalidated the `.test` accounts — and left this one alone. So the
one account with unconditional cross-tenant access is the one whose bcrypt hash anyone with repo
access can crack offline, and it is explicitly exempted from the forced change that would bound the
damage. Every clean build ships it.
**Why nothing caught it** It is invisible on a long-lived box: whoever set a working password at
runtime overwrote the committed hash, so the defect only exists on a machine built from the
migrations — which is to say, on a real deployment.
**Fix** Mig 198 applies 124's own treatment to the account it missed: a random hash nobody holds
plus `temp_password = true`, and a belt-and-braces sweep that invalidates the committed hash
wherever it reappears. The operator gets in by resetting out-of-band, which is the path 124 already
established (`scripts/sandbox-reset-passwords.mjs` is the sandbox stand-in for that, and refuses to
run against a non-local DSN).
**Class → sweep for** *a security migration applied to some of its targets*. When a fix enumerates
accounts, rows or routes by hand, the bug is whatever was not on the list. Re-read 124 against every
account that existed when it was written.

## B33 — The documented bootstrap can never run · LOGGED (structural, deferred)
**Where** `pipeline/src/seeds/master_admin.py` · `db/migrations/001_baseline.sql`
**Proven** The seed's own docstring promises the deploy story: *"Creates the very first master_admin
user so the platform has a working login immediately after deploy… generate a random 16-char token
and print a LOUD banner so the operator can grab it from Railway deploy logs."* It then opens with:
```python
row = await conn.fetchval("SELECT 1 FROM users WHERE role = 'master_admin' LIMIT 1")
if row is not None:
    log.info("master_admin already exists, skipping seed.")
    return
```
`001_baseline.sql` inserts a master_admin. Migrations always run before the worker boots, so the row
always exists, so the seed always skips. Observed verbatim in this run's worker log:
`master_admin already exists, skipping seed.`
**Why it matters** The path an operator is told to use to get their first login is dead code on any
real deployment. Combined with B32 that was survivable — there was a committed password to fall back
on — and B32's fix removes the fallback. After 198, a genuinely fresh deployment has NO documented
way in.
**Why not fixed here** Two candidate fixes and both are design calls, not patches: stop seeding an
admin in 001 so the bootstrap can own it, or let the seed reset an existing admin when
`INITIAL_MASTER_ADMIN_PASSWORD` is explicitly provided — which puts an environment variable in
charge of an admin credential and needs thinking about, not a quick commit mid-run.
**Class → sweep for** *a guard that makes its own feature unreachable*: an idempotency check whose
condition is guaranteed true by something that always runs first. Sibling of B31 — "it never fires"
and "it works" produce identical logs.

## B34 — FIXED. A new customer is provisioned with zero templates, silently
**Where** `frontend/lib/template-stable-sync.ts` (never runs) → `backfillTenantTemplates` in
`app/api/admin/applications/[id]/accept/route.ts:293`
**Proven** J1b, on a box rebuilt from migration 001: approving a real application provisioned the
tenant, the admin user, 6 buckets, 9 opportunity cards and 54 bucket scores — and **0 template
cards**, because `master_templates` holds **0 rows**. The 39-entry `TEMPLATE_CATALOG` lives in
TypeScript and only reaches the database when an admin runs the template-stable sync by hand.
**Why it matters** `backfillTenantTemplates` fans MASTER templates onto the new tenant's shelf. With
an empty master table it runs, finds nothing, succeeds, and the customer lands with no templates and
nothing anywhere saying so. Every proposal mold the product ships is invisible to them.
**Why not fixed here** WHEN the catalog should sync is a design call: a migration that seeds it, a
worker boot step like `seed_master_admin`, or an explicit admin action that provisioning refuses to
run without. Each has different failure modes on redeploy, and picking one mid-run would smuggle a
behaviour change into a test.
**Class → sweep for** *a backfill whose source is populated by a manual step*: any "copy the masters
in" that assumes someone already loaded the masters.

## B35 — FIXED. The starter library never arrives, and the fallback that guarantees it also fails
**Where** `frontend/lib/library/starter-offer.ts` → `lib/tasks/tasks.ts::createTask`
**Proven** J1b. The new tenant ended with **0 library atoms and 0 tasks of any kind**, while the
source is plainly present: 303 atoms carry `collection=system_starter`, and **18** match the copy's
exact criteria (`grain='foundation'` AND that tag). The frontend log names the cause:
```
[tasks] createTask insert failed: Error [PostgresError]:
  new row violates row-level security policy for table "tasks"
```
**Why it matters** The accept route is deliberately belt-and-braces: `copyStarterSetToTenant` copies
eagerly, and `offerStarterSet` raises a ToDo **only if the copy landed nothing** — the comment says
"so the tenant is never left with an empty library". BOTH halves produced nothing, so the guarantee
is broken and the customer's first impression of the product is an empty library with no prompt.
Both are wrapped in best-effort `try/catch` that logs and continues, so the accept reports success.
**This is a REGRESSION of a bug that was already fixed.** `offerStarterSet`'s own docstring
describes this precise failure — "the createTask INSERT fails the `tenant_id = app.tenant_id OR
tenant_id IS NULL` with_check, which the callers' best-effort try/catch swallowed → the new
customer's starter-set ToDo was silently never raised" — and the `runInTenant` wrapper is still
present in the source. So either the wrapper no longer establishes context on this path, or the
admin route's own connection context defeats it. That distinction is the next step and it needs the
call traced, not guessed.
**Class → sweep for** *a best-effort catch around a guarantee*: `try/catch` that swallows is correct
for an optional enrichment and wrong for the fallback that exists so the primary path can fail. Also
the RLS-context family: every provisioning helper called from an ADMIN route holds no tenant context
of its own and must establish one.

## B36 — The box served a build 198 commits old, and every test believed it · FIXED (tooling)
**Where** `scripts/sandbox-up.sh` — the bring-up had no notion of build freshness.
**Proven** `hitl-bucket-rls` failed on "PATCH clobbers the bucket criteria instead of merging, and
the synchronous re-rank never runs". Both symptoms were real *in the served binary*. A live probe
(`frontend/scripts/probe-bucket-rerank.mjs`) printed the stored criteria at three points:
```
after CREATE  criteria {"weights":{"keyword":1},"keywords":["additive","concrete"],"useTimeline":false}
              scores   100, 100, 50, 50, 50, 0, 0, 0, 0
after PATCH   criteria {"keywords":["zzznotarealkeyword"]}        ← weights + useTimeline GONE
              scores   100, 100, 50, 50, 50, 0, 0, 0, 0          ← unchanged: no sync re-rank
12s later     scores   0, 0, 0, 0, 0, 0, 0, 0, 0                 ← the async pipeline fixed it
```
The source, however, is correct: the PATCH handler shallow-merges the sanitized partial over the
stored criteria and calls `rankBucket` synchronously. Both landed in `0451e325` at **Aug 15 03:02**.
`.next/BUILD_ID` was stamped **Aug 15 01:20** — 1h42m earlier, and **198 commits** behind HEAD.
**Why it matters** This is worse than a missing test: it is a confident wrong answer. Every result
collected in the run up to this point was measured against a binary from 198 commits ago, so a
green meant nothing and a red pointed at code that no longer existed. Two of the day's "product
findings" evaporated on rebuild. Nothing in the loop could see it — the tests read the current tree,
the server served an old one, and the only thing connecting them was my assumption.
**Fix** `sandbox-up.sh` now compares `.next/BUILD_ID` against the newest `.ts/.tsx/.json/.css` under
`frontend/{app,lib,components}` and prints `build STALE — <file> is newer than .next/BUILD_ID` with
the rebuild command. It does not rebuild automatically: a 4-minute build inside an idempotent
bring-up would be a surprise, and the point is that the operator *knows*.
**Class → sweep for** *a derived artifact with no freshness check*: any place where something built
is served next to the source it was built from — `.next`, a generated client, a compiled schema, a
cached fixture. If the tooling cannot tell you the artifact is behind the source, every test that
runs through it is unfalsifiable.

## B37 — The standalone server cannot be found by name, so every "restart" was a no-op · FIXED (tooling)
**Where** `scripts/sandbox-up.sh` (and every ad-hoc restart line in this session).
**Proven** `pkill -f "standalone/server.js"` exits 0 having matched nothing; `pkill -f next-server`
matches the calling shell's own command line and kills the caller. Between them, three separate
"restarts" reported success while the original process kept serving. The cause: Node rewrites its
argv memory when it sets `process.title`, so seconds after boot `/proc/<pid>/cmdline` reads
`next-server (v15.5.14)` with no trace of the script path — a scan run immediately after `nohup`
finds the process and the same scan a minute later finds nothing, which reads exactly like "the
server died". Located it instead through the listening socket:
```
port 3000 inode=182580 → PID 18270
  cmdline: next-server (v15.5.14)
  cwd:     /home/user/govwin/frontend/.next/standalone
```
**Fix** Match on the working directory. `server.js` does `process.chdir(__dirname)`, so the
standalone server is exactly the process whose cwd is `.next/standalone`, and cwd is not rewritable
by the process the way argv is.
**Class → sweep for** *identifying a process by a string it controls*: a title, a name, an argv.
Anything self-reported can be rewritten. Prefer what the kernel owns — the cwd, the listening
socket, a pidfile you wrote yourself.

## B38 — The suite spent a full run on an app with no CSS · FIXED (tooling)
**Where** `scripts/sandbox-up.sh` — the standalone server was started without staging its assets.
**Proven** Measured the portal shell at a 390px viewport and printed every offender's ancestry:
```
aside.fixed inset-y-0 z-50 max-w-[85vw] …   x=8..382  pos=static ox=visible tf=none
nav.flex flex-col gap-1 text-sm             x=8..382  pos=static ox=visible tf=none
```
`pos=static` on a `fixed` element and a row-laid-out `flex-col` are impossible — unless no CSS
loaded. It hadn't: `/_next/static/css/cf149d9a1e580d24.css` → **404**, because `next build` does not
copy `public/` or `.next/static` into `.next/standalone`; you stage them yourself after every build,
and one `cp` in a restart line had been killed.
**Why it matters** With no Tailwind, `fixed`, `hidden`, `lg:` and `-translate-x-full` stop existing,
so the nav drawer renders inline at full width and `hitl-cc-mobile-audit` reports a 474px horizontal
overflow on **every tenant Command Center destination** — a convincing, specific, entirely fictional
layout regression. Once the stylesheet served, `scrollWidth` was 390 on the same pages: exactly the
viewport, zero overflow.
**Fix** `sandbox-up.sh` stages `public/` and `.next/static` unconditionally before starting, and the
verdict block now follows the stylesheet the login page actually links and fails the stack if it
does not return 200. No other probe in that script could see this: the server answers 200 on `/`,
renders every page, and runs every API route perfectly while completely undressed.
**Class → sweep for** *a health check that proves liveness instead of correctness*. "It returns 200"
is not "it works". For anything assembled from parts staged separately — static assets, a bundled
worker, a copied config — check the part, not the process.

## Retraction — the three "product findings" of 2026-08-20 were all environment
`hitl-bucket-rls` (criteria clobbered, no re-rank) was B36. The missing "Partner Console" heading was
`seed-foundation` resetting Paul off `partner_admin` — the console was never broken. The mobile
overflow was B38. Nothing in the product accounted for any of them. Recorded here because a bug log
that only accumulates is not a record, and "I reported three defects that did not exist" is the most
useful line in this file: every one was measured through an apparatus nobody had checked.

## B39 — Ten of 23 provisioned sections arrive with no content, and are still marked drafted · LOGGED
**Where** the provision → mold-delivery path, seen on a NEW solicitation built from
`docs/DoW 2026 SBIR BAA FULL_R1_04132026.pdf`.
**Proven** Drove the real spine — upload → shred → build-out → push → comp purchase → release →
provision — against a DoW 2026 SBIR annual BAA that lives in the repository. The product read the
document correctly: 788,002 chars shredded, 7 volumes and 22 required items derived, and a buyer
build provisioned with the authentic DoD SBIR Phase I shape (23 sections). Then:
```
content_source  template  13
content_source  (none)    10
```
and every one of the ten is a FORM or CERTIFICATION, not a narrative:
```
V1 Proposal Cover Sheet & Technical Abstract   V5 Foreign Nationals Disclosure (ITAR/EAR)
V3 Phase I Option Cost Proposal                V5 Letters of Support
V4 Company Commercialization Report (CCR)      V5 DD Form 2345 — Militarily Critical Tech Data
V6 Fraud, Waste, and Abuse Training Cert       V5 Technical Data Rights Assertions
V7 Disclosures of Foreign Affiliations         V5 Reps & Certifications
```
**Why it matters** Two things are true at once and only one of them can be right. All ten carry
`status = 'ai_drafted'` while `content_source IS NULL` — so the build reports them as drafted to a
buyer who opens them and finds nothing. Either the mold delivery should cover form/certification
items, or those items should be classified as completed-elsewhere the way the T3CP scenario marks
its DSIP volumes — in which case they should not be authorable sections at all. What they must not
be is authorable, empty, and labelled drafted.
**Not decided here.** Which of those two is correct is a product judgement about how the annual BAA
is meant to be worked, not something to infer from the schema. Logged with the scenario
(`641c837d-05ab-4bba-94cf-c43845530021`, proposal `99a4ac47-81de-406e-83c9-d893d6313b15`) so it can
be reproduced in one command.
**Class → sweep for** *a status field that outruns the thing it describes*: any place a row is
marked done/drafted/ready by the step that SCHEDULED the work rather than the step that finished it.

## B40 — FIXED. A large solicitation is silently truncated at 500,000 characters
**Where** `frontend/app/api/admin/rfp-upload/route.ts:550` — `textResult.text?.slice(0, 500000)`;
same cap in `lib/ingest/ingest-topic-files.ts:103,111`.
**Proven** Ingested four real solicitations from the repository through the admin upload form and
read the per-document extracted length straight out of `solicitation_documents`:
```
DoW 2026 SBIR BAA   3.0 MB → 500,000   ← exactly the cap
DoW 2026 SBIR CSO   1.6 MB → 288,002     intact
DoD 25.1 SBIR BAA   3.1 MB → 500,000   ← exactly the cap
DoD X25.5 CSO       1.5 MB → 198,474     intact
DoD 25.A STTR BAA   1.3 MB → 449,600     intact
```
Two of the five landed on the cap to the character. Nothing in `solicitation_documents`, the
readiness probe, or the curation UI records that the tail of the document was discarded.
**Why it matters** This is the ingest-provenance doctrine failing at its own foundation.
docs/INGEST_PROVENANCE.md is built on one rule — *a value the product did not read from the
solicitation must never look like one it did* — and the whole `pattern_match → ai → default` ladder,
the "Read from source" badge, the citation with rule + page + excerpt, and the "Set elsewhere"
deferral all rest on the assumption that the source text IS the source document. When the last
third of a 3 MB BAA is dropped, three things go wrong at once and all of them look like success:
a rule that lives past character 500,000 is reported as ABSENT rather than unread, so Assist emits a
DEFAULT (or a deferral) for a value the PDF actually states; a citation's page/offset is computed
against a truncated text; and the compliance matrix, volumes and required items are derived from a
partial document with no marker anywhere. The DoW 2026 scenario this session built an entire
23-section proposal on had its BAA truncated — the shape may well be right, but nothing proves it.
**Not fixed here** because the right answer is a product decision: raise the cap, chunk the
extraction, or keep the cap and record `truncated_at` on the document so provenance can say "this
rule was not read because we stopped reading". The third is the smallest change that restores the
doctrine, and it is the only one that stays honest if the cap is ever hit again.
**Class → sweep for** *a silent `.slice()` on ingested source material*: any truncation of input the
product later reasons about and attributes. Truncating is defensible; not recording it is not.

## B41 — Two of four solicitations derived nothing, and said nothing about it · LOGGED
**Proven** Same driver, same form, five documents:
```
sol       document                    chars     volumes  compliance  opportunity
641c837d  DoW 2026 SBIR BAA + CSO     788,002        7          1           1
6b00488a  DoD 25.A STTR BAA           449,600        0*         1           1
432452c4  DoD X25.5 CSO               198,474        0          0           0
fceafd42  DoD 25.1 SBIR BAA           500,000        0          0           0
```
X25.5 produced six provenance findings (page limit, font, margins, submission format all flagged as
system defaults, not read from source) and then landed nothing — arguably correct, since the point
of the provenance work is to refuse to invent. But 25.1 is different: its readiness probe reported
`{ready:false, chars:0, state:'shredding'}` at the moment the upload form had already returned and
navigated, while the database held 500,000 characters for that document. The form's "wait for the
shred" did not wait, so Assist ran against an empty extraction and derived nothing.
**Why it matters** With no opportunity row, the solicitation cannot be pushed to any tenant — the
entire discovery spine is blocked for that document, and the only signal an admin gets is a
curation page that looks ordinary. Two of four real solicitations went in and produced nothing
buyable, silently.
**Class → sweep for** *a caller that waits on the wrong signal*: the form waits for a navigation,
not for the shred it depends on. Anything that polls "am I done" must poll the state the next step
actually reads.

## B41 — CORRECTED, and split. One real bug, one HITL gate working as designed.
When B41 was written it said "two of four solicitations derived nothing, silently". Driving two more
documents and then reading the draft rows shows that was two different things wearing one label.

**B41a — the shred ceiling sat where it would split real inputs in half · FIXED**
`components/rfp-curation/upload-form.tsx` polled 20 × 2s = 40s for the async shred, on the stated
reasoning that "a 50-page PDF shreds well inside this". True — but the product's real input is an
ANNUAL BAA of 200+ pages. Measured across the solicitations in `docs/`:
```
1.3–1.6 MB CSO / STTR   →  6s     inside the ceiling
3.0–3.1 MB SBIR BAA     →  42–45s OUTSIDE it, both of them
```
Past the ceiling the poll gives up and Assist is skipped — correctly, since running it against an
empty extraction is what fabricates a matrix from a default skeleton. But the skip was SILENT: the
upload succeeded, the admin landed on an ordinary-looking workspace, and the solicitation had no
compliance and no opportunity, so it could never be pushed to a tenant. Ceiling raised to 90 tries
(~3 min), and a timeout now says so in words instead of leaving the admin to discover it. Verified:
the AF X24.5 CSO that previously died at 42s now completes and Assist runs.

**B41b — NOT A BUG. The blocker gate is doing its job.**
The two CSOs did not fail; they are PARKED. Reading their drafts:
```
DoW 2026 SBIR   landed   3 findings   0 blockers  → 7 volumes · compliance · opportunity
DoD 25.A STTR   landed   3 findings   0 blockers  → same
DoD X25.5 CSO   staged   9 findings   1 blocker   → nothing yet
AF  X24.5 CSO   staged   9 findings   1 blocker   → nothing yet
```
`lib/ingest/stage-skeleton.ts` refuses to auto-land a staged matrix carrying a blocker — "A person
must land this one" — so `materializeSkeleton` never runs, and volumes/compliance/opportunity are
never written. That is exactly the designed behaviour, and it is the difference between the two
program families rather than a defect: a SBIR/STTR annual BAA states its page limits and fonts, so
Assist reads them and lands clean; a CSO is an open call that states none, so Assist flags the
defaults as unread and parks the matrix for a human.

What was actually missing was on MY side: the drive stopped at the machine step and never performed
the human one, then reported the park as "produced nothing". The honest continuation is to drive the
rfp_admin review-and-land, which is a second workflow worth covering in its own right — the
auto-land path and the human-land path are different journeys and only one of them had ever been
driven.
**Class → sweep for** *a drive that reports a HITL park as a failure*: any assertion that treats
"waiting for a person" as "broken" will mislabel every gate the product deliberately installs.


## B39 — CORRECTED. They were not blank; they were DRAFTED, which is worse · CAPABILITY BUILT
B39 said ten of 23 provisioned sections "arrive with no content". That was wrong, and the correction
matters more than the original claim. Every one of the ten has 4 canvas nodes and ~4 KB of text:
```
DD Form 2345 — Militarily Critical Technical Data Agreement   4,477 bytes
Reps & Certifications                                          4,441 bytes
Fraud, Waste, and Abuse Training Certification                 4,181 bytes
Company Commercialization Report (CCR)                         4,064 bytes
```
`content_source` was simply never stamped, and the drive read that column as a proxy for "has
content". So the sections are not empty — the section drafter wrote them. Which is the actual
problem: nobody drafts a DD Form 2345. You obtain it, sign it and attach it. Reps & Certifications
are filed in SAM; the FWA certificate is issued after training; the CCR comes out of the agency's own
system. A buyer opening those finds four kilobytes of plausible AI prose where a signed federal form
belongs. Empty would have been safer.

**Why it happened.** Ingest only ever derives part of a solicitation's shape — on this DoD annual BAA
it delivered molds for all 12 Volume 2 sections and the Volume 3 base cost, and nothing for the rest.
An item with no mold still provisioned as an authorable section, and the drafter filled it.

**What was already there.** `volume_required_items.metadata.dsipOnly` and
`solicitation_volumes.metadata.dsipOnly` were read by `compliance-resolver` and honoured by
`provision-proposal` (`isAuthoredItem` / the volume guard) — wired end to end, with no way to SET
them outside a hand-written migration. The capability was 90% built and 0% reachable.

**Built.** Two PATCH routes (rfp_admin+, audited, solicitation-scoped) that set the disposition per
item and per volume, and `itemsUndecided` in `getBuildReadiness` — an item is DECIDED when it has a
mold or is marked completed-elsewhere, and anything else now holds the build-out bar rather than
passing silently. The volume-level twin exists because a volume can carry no items at all: Volume 7
here has zero, and provisioning's volume-with-no-items fallback stands up a placeholder section that
the item route could never have reached.

**Proven live** (`frontend/scripts/probe-build-or-mark.mjs`, 13/13): marking six items and one
item-less volume takes what a new build asks the buyer to write from 23 down to 16; DD Form 2345,
Reps & Certifications and the FWA certificate are no longer authored here; Phase I Statement of Work,
Technical Objectives and Commercialization Strategy still are, because the spec stated them. A
foreign solicitation's item is refused 404 and an unknown disposition 400.

**Still open:** the ten sections in the EXISTING immobileyes build were already drafted, so the marks
do not retroactively empty them; and the drafter should refuse form/certification item types outright
rather than relying on the item being marked first. Both are follow-ups, not part of this change.
**Class → sweep for** *a flag that is read and honoured but never settable*: fully-wired state with no
writer is indistinguishable from a missing feature, and it fails in the permissive direction.

---

# 2026-08-22 — the storage/provenance pass (B42–B45)

Four bugs, one shape. Every one of them sat behind a handler that caught its own failure and
carried on: a `try/except` that logged a warning, or a route that returned `201` with the error
folded into a field nobody reads. Each produced a run that **reported success having done less than
it claimed** — the state a test suite structurally cannot see, because there is nothing to fail.

The fix in each case is two-part: correct the behaviour, **and** make the failure legible in the
record it writes. Where only the first half is done, the next regression is silent again.

## B42 — The section-slug rule rejected half the shredder's own canonical vocabulary · FIXED
**Where** `pipeline/src/storage/paths.py` `_SECTION_SLUG_RE`, mirrored in
`frontend/lib/storage/paths.ts`
**Proven** The shredder's own prompt (`shredder/prompts/v1/section_extraction.txt`) enumerates ten
canonical section keys and instructs Claude to use EXACTLY those strings. The validator allowed
`[a-z0-9-]` only:
```
accepted  5/10   cover · commercialization · team · eligibility · appendix
REJECTED  5/10   technical_approach · cost_volume · evaluation_criteria
                 submission_format · compliance_requirements
```
So `rfp_pipeline_path(kind="shredded")` raised `StoragePathError` on half of every real
solicitation. `runner.py`'s per-section guard caught it, logged
`shredder: section %s S3 write failed`, and continued.
**Why it matters** These are the artifacts the comment in `runner.py` calls "what customer agents
will read from their isolated copies after portal purchase". `technical_approach` and `cost_volume`
— the two that matter most — were never written, for any solicitation, ever. And nothing
reconciled the discrepancy: `metadata.json` listed **ten** entries in `section_keys` and **five** in
`artifact_keys`, side by side in the same object.
**Fix** Widened to `[a-z0-9_-]` in BOTH mirrors (the test file declares them mirrors; the frontend
READS what the pipeline writes, so a one-sided fix leaves the frontend unable to address an artifact
the pipeline just stored). Failed writes now collect into `metadata.json` as
`section_artifacts_failed` — the same rule `section_extraction_skipped` already followed ten lines
below. Underscores are safe object-key characters; the widening is purely permissive.
**Proven fixed** 10/10 canonical keys now write and read back a real artifact through the local
storage driver, up from 5/10. The emulator's prose placeholder, `Cover` and `../escape` are all
still refused, so the guard did not loosen into anything-goes.
**Class → sweep for** *a validator that disagrees with the vocabulary it validates*. Wherever a
controlled list is declared in one file (a prompt, a CHECK constraint, an enum, a docs table) and
enforced by a pattern in another, the two drift and the drift is silent. The regression test now
PARSES the canonical list out of the prompt itself, so adding an unwritable key fails loudly in CI
instead of in a warning line during a live shred.

## B43 — A page count guessed from byte size, wearing the name of a measurement · FIXED
**Where** `pipeline/src/shredder/runner.py`, the `solicitation_documents` update
**Proven** The column was written as `len(pdf_bytes) // 40000 + 1  # rough page estimate`, and read
by `packaging_specialist.get_compliance` which hands it to the agent inside the document manifest,
beside values genuinely extracted from the source. Measured against the real solicitations in
`docs/`:
```
DoD 25.2 SBIR BAA      real 254pp   guessed  60    76% low
AF_X24.5_CSO           real  88pp   guessed  17    81% low
Air Force X25.6 v3     real  79pp   guessed  22    72% low
```
**Why it matters** This is the exact failure `docs/INGEST_PROVENANCE.md` exists to prevent: *a value
the product did not read from the solicitation must never look like one it did.* A page-limit
reviewer reading "60 pages" for a 254-page BAA is worse off than one reading nothing, because
nothing invites a check and a number does not.
**Fix** `extractor.pdf_page_count()` reads `doc.page_count` — pymupdf is already opened one function
above to extract the text, so the true count was free the whole time. Returns `None`, never a
fallback number, when the bytes are unopenable or the library is absent; the column is nullable and
the caller stores NULL. Unknown stays unknown, which is the other half of the same rule. Unlike
extraction itself this returns rather than raises: an absent page count must not fail a shred.
**Proven fixed** Real count off a real BAA (254); `None` — not `1` — for empty and non-PDF input,
where the old expression returned `1`, a document claiming to have a page.
**Class → sweep for** *an estimate stored in a field named for a measurement*. Grep for `# rough`,
`# approx`, `estimate`, `// 1000`, and any arithmetic on a byte length that lands in a column a
human or an agent will read as fact. If it cannot be measured, the column must be nullable and the
value must be NULL.

## B44 — Every topic upload failed on an ON CONFLICT that matched no index · FIXED
**Where** `frontend/lib/ingest/ingest-topic-files.ts`
**Proven** `there is no unique or exclusion constraint matching the ON CONFLICT specification` — the
clause named `ON CONFLICT (content_hash)`; the index it arbitrates is two columns:
```
CREATE UNIQUE INDEX idx_sol_docs_hash_per_solicitation
  ON solicitation_documents (solicitation_id, content_hash)
  WHERE content_hash IS NOT NULL
```
so Postgres matched nothing and threw on every call.
**Why it matters** The route still returned **201**, with the error tucked into a per-file `failed`
entry and an empty `created` list. An admin uploading topic files saw "no topics created" and would
reasonably conclude the PDF had not parsed — a data-layer bug wearing the costume of a bad document.
**Fix** Restate the index's own column list AND its WHERE predicate, exactly as the CLAUDE.md data
rule says. Per-solicitation is also the semantics we want: the same PDF under two solicitations is
two legitimate documents; only a re-upload to the SAME solicitation is a duplicate.
**Class → sweep for** *a 2xx that carries a failure in its body*. A route that cannot fail cannot be
monitored. Every per-item `failed[]` array needs either a non-2xx when the list is non-empty, or a
surfaced count the caller must read.

## B45 — The emulator filled a structural key with a prose sentence · FIXED (harness)
**Where** `frontend/scripts/test-harness/emulated-claude.mjs` `emulatedJsonFor`
**Proven** Every non-numeric schema field got
`EMULATED <field> — sandbox harness, not a real extraction`, so the shredder's section `key` arrived
as that sentence — which is not a slug, so B42's validator refused it and the write was skipped.
Net effect: **the sandbox exercised none of the per-section artifact path while appearing to.**
**Why it matters** A harness that silently skips the path it claims to cover is the same defect
class as the product bugs above, one level up. It is also why B42 survived: the only environment
that could have caught it was quietly not running it.
**Fix** The emulator now HARVESTS the controlled vocabulary from the prompt that declares it
(`Canonical section keys (use EXACTLY these strings…)`) and emits real slugs for `key`/`slug`
fields, plus one row per value — a single row cannot distinguish working iteration from broken.
Self-maintaining: change the canonical list and the harness follows with nothing here to edit.
**Honesty preserved** Every prose field still reads `EMULATED`. A key is structure, not content, so
emitting a real one does not risk a fixture being mistaken for a real extraction
(docs/INGEST_PROVENANCE.md).
**Class → sweep for** *a fixture generator that fills a typed field with untyped filler*. Any
placeholder that lands somewhere shaped — a slug, a path segment, an enum, an id, a date — is not a
placeholder, it is a wrong value, and the code that rejects it will do so quietly.

## Two bugs found in the fixes themselves, before they landed
Recorded because the point of this log is the class, and "the fix had a bug" is a class.

- **A string-prefix containment check is not containment.** `_local_path` guarded traversal with
  `str(target).startswith(str(root))`. With bucket `testbucket`, the key `../testbucket-evil/x`
  resolves OUTSIDE the root yet still starts with it — precisely the traversal the guard exists to
  stop. Now `Path.is_relative_to`. **Sweep for** any path/prefix/namespace check written with
  `startswith` where the thing being compared is hierarchical.
- **`importlib.reload` leaks module state that `monkeypatch` cannot undo.** A test fixture set env
  vars and reloaded the module to pick them up; monkeypatch reverted the env but not the rebound
  globals, and the leaked `BUCKET` broke an assertion in a LATER file. Now `monkeypatch.setattr` on
  the module attributes directly. **Sweep for** `importlib.reload` in any test that does not restore
  the module afterwards under the original environment.

## B46 — FIXED. `opportunities.solicitation_id` is inconsistently populated
**Found by** the end-to-end arc, on its third run — the first thing it found that a per-stage test
could not.

**Proven.** `solicitation.push` returned 200 with an `opportunityId`, and the bridge fanned out
correctly (bridge=1, **17 tenant cards**). But the opportunity it created has
`solicitation_id = NULL`. Only the FORWARD link is written:
```
curated_solicitations.opportunity_id → opportunities.id     set
opportunities.solicitation_id        → curated_solicitations.id   NULL
```
Across the whole table the back-link is set on 47 of 65 rows, and it does not split cleanly by kind
— 33/47 umbrellas and 14/18 topics have it. So this is not "umbrellas use one link, topics the
other"; it is genuinely inconsistent within both.

**Proven consequence (verification, not product).** `drive-baa-forward.mjs` resolved opportunities
solely through the back-link, so it printed
```
after {"opps":0,"bridge":0,"cards":0,"scores":0}
✗ nothing reached a tenant card
```
against a push that had just created seventeen. It has been reporting failure on success for as long
as pushes have left the back-link NULL — the sixth instance today of *a check that does not touch
what it claims to cover*. Fixed: it now resolves an opportunity through EITHER link.

**NOT proven — the reason this stays open.** The product reads the back-link in 15 places. The two
that matter most defend themselves:
```
opportunity-bridge.ts:88   ON cs.id = o.solicitation_id OR cs.opportunity_id = o.id
republish.ts:160           (same OR)
```
and the bridge's own comment says the two arms exist for umbrella vs topic. But two readers use the
back-link ALONE, with no fallback:
```
compliance-resolver.ts:74  SELECT solicitation_id FROM opportunities WHERE id = $topicId
opportunity-pin.ts:27      JOIN opportunities o ON o.solicitation_id = sd.solicitation_id
```
An inner join on a NULL never matches. Whether that produces a user-visible failure depends on
whether those paths are ever reached with a NULL-back-link opportunity, and **I have not established
that**. It is entirely possible both are only reached for topic opportunities that do carry it.

**What would settle it** — cheap, and worth doing before touching any product code: reach
`compliance-resolver` and `opportunity-pin` with an opportunity created by the push path, and see
whether the result is empty. If it is, the fix is either to write the back-link on push or to give
those two the same `OR` the bridge already has. If it is not, the column is vestigial for umbrellas
and should be documented as such rather than "fixed".

**Second site, found the next run.** `drive-buy-and-build.mjs:69` resolved the opportunity the same
way — `JOIN opportunities o ON o.solicitation_id = cs.id` — and failed with *"no opportunity for
solicitation …, or no tenant lighthouse"* against a solicitation that had just fanned 17 cards. Two
independent scripts written at different times both reached for the back-link, which says the
back-link is the INTUITIVE direction and the forward one is the surprise. That raises the odds that
a product reader has done the same thing somewhere not yet exercised, and it is why this stays OPEN
rather than being written off as a tooling quirk.

Both call sites now resolve through either link, the way `lib/opportunity-bridge.ts:88` already did.

**Class → sweep for** *a bidirectional link where only one direction is written*. Grep for pairs of
tables that reference each other and check which side the writer actually sets. The danger is not
the NULL; it is that half the readers compensate and half do not, so the defect is invisible until a
reader without the fallback is hit.

## B47 — the sandbox self-heal silently invalidated every admin drive · FIXED
**Where** `scripts/sandbox-reset-passwords.mjs` vs 48 drive/spec sites
**Found by** the end-to-end arc, run 6, failing a login that had worked in run 5 twenty minutes
earlier.

**Proven.**
```
reset script sets   eric@rfppipeline.com → SANDBOX_PASSWORD  ('SandboxDrive2026!')
48 drive/spec sites default to           → 'RFPAdmin2026!'

live check:  ✓ SandboxDrive2026!    ✗ RFPAdmin2026!
users.updated_at = 13:00 — the minute the self-heal ran
```
The self-heal calls the reset script on every repair. So the sandbox healing itself rotated the
admin password out from under every script that drives as an admin, and the next drive failed with
`?error=invalid` on a login that had been fine minutes before.

**Why it matters more than a wrong password.** The symptom is indistinguishable from a broken auth
flow. Run 6 spent its diagnosis budget on "is login broken?" when the answer was "something rotated
the credential between runs" — and nothing in the failure names the rotation. It also only bites
AFTER a heal, so it is invisible on a healthy box and appears exactly when someone is already
recovering from a restart, i.e. at the worst moment.

**Fix** `scripts/sandbox-env.sh` now exports the canonical set, and the reset script's
`SANDBOX_PASSWORD` is the single authority:
```sh
export SANDBOX_PASSWORD="${SANDBOX_PASSWORD:-SandboxDrive2026!}"
export RFP_ADMIN_PW="${RFP_ADMIN_PW:-$SANDBOX_PASSWORD}"
export LIGHTHOUSE_PW="${LIGHTHOUSE_PW:-LighthouseAdmin}"   # not in the reset target list
export TENANT_PW="${TENANT_PW:-DemoPass123!}"
```
All 48 sites already read `process.env.RFP_ADMIN_PW` before their literal, so one export repairs
every one of them without touching 48 files. The literals stay as a last-resort default for anyone
running a script without sourcing the env.

**Verified live, all three actors, from the env:** rfp_admin ✓ · lighthouse ✓ · foundation ✓.

**Class → sweep for** *two places that own the same secret/config, where one writes and the others
assume*. The tell is a default literal repeated across many files: every repetition is a chance to
disagree with the writer, and the disagreement only surfaces after the writer runs.

## B48 — NOT-A-BUG. `scout_findings.tenant_id`, and a claim I got wrong twice in one hour
**Reported by** schema-check `--unqualified`; **claimed by me** in commit as a real defect;
**retracted** on reading the code.

`drive-scout.mjs` §7 queries `count(*) FILTER (WHERE tenant_id IS NULL) FROM scout_findings`.
The column does not exist — confirmed against the live schema — and the query is wrapped in
`.catch(() => null)`. I wrote it up as textbook Mistake 0: a verification step that silently does
nothing.

**It is the opposite.** The handler I did not read:

```js
if (scoped) {
  check(scoped[0].tenanted === 0, 'findings are platform-scoped, filed under no tenant', …);
} else {
  note('scout_findings has no tenant_id column — platform scope is structural');
}
```

The probe deliberately tolerates BOTH schema shapes and reports the stronger one. If the column
exists, it asserts every finding is filed under no tenant. If it does not, platform scope is not a
property of the data that could drift — it is structural, guaranteed by the absence of the column.
That is a better guarantee than the check it replaces, and the code says so.

**What I actually got wrong, twice, inside an hour:**

1. The checker flagged a column that genuinely does not exist — technically true, materially
   irrelevant, because the query is *expected* to fail. A "column does not exist" finding cannot
   distinguish a mistake from a deliberate schema probe.
2. I then asserted in a commit message that the check "has never actually run", having read the
   `.catch` and stopped there. The failing branch WAS the point.

**Class → sweep for** *a finding that is true about the code and false about the system*. Before
calling a swallowed error a defect, read what the handler does with it. `.catch(() => null)`
followed by a real else-branch is a schema-shape probe, not a silent failure — the same three
characters mean opposite things depending on the ten lines after them.

The genuine lesson is narrower than the one I claimed: my checker needs to stay a LEAD GENERATOR.
Every one of its findings requires a human read before it becomes a bug, which is exactly why the
unqualified mode is opt-in and not a gate.

---

# Screenshot walk, 2026-08-22 — B49–B53

Five findings from ONE pass of `e2e/actor-walk-drive.spec.ts`, all found by **looking at the
rendered page**. Every one of them is invisible to a DB query: the rows are present and
well-formed, and the queries that would "verify" these surfaces return healthy. What is wrong is
what the operator SEES. This is the case for screenshots as an instrument rather than an artifact.

## B49 — FIXED. `Review application from ` — the applicant's name is never rendered
**Found by** reading `docs/manuals/img/admin/admin-dashboard.png`; **proven by** mutation test.

The admin ToDo list showed a work item titled `Review application from` with a trailing space and
no company. Cause: the seeded rule (mig 040) is
`Review application from {company_name}`, while BOTH emitters of `capture.application.submitted`
(`app/api/applications/route.ts`, `lib/partner/registration.ts`) send **`companyName`**.
`interpolate()` collapses an unknown key to `''`, so the name vanished silently.

3 live ToDos carried the empty title.

**The test that should have caught it made it invisible.** `__tests__/automation-triggers.test.ts`
asserted `interpolate('…{company_name}', { company_name: 'Acme' })` — it hand-fed the payload the
*template* wanted rather than the payload the *emitter* sends, so it passed for the whole life of
the bug.

**Fix**: `interpolate` now resolves a placeholder against its exact, camelCase, and snake_case
spellings (exact wins). Rule templates are admin-authored data at `/admin/automation` while
payloads are JS objects; neither side should have to know the other's convention. Regression test
uses the emitters' real payload shapes.

Mutation-proven: reverting the resolver fails the new test with
`expected 'Review application from ' to be 'Review application from Acme Labs'` — the exact string
in the screenshot.

**Class → sweep for** *a unit test whose fixture is derived from the code under test rather than
from its real caller*. It cannot fail the way production fails.

## B50 — FIXED. A contract is reachable only through one ToDo deep-link
`/portal/[tenantSlug]/contracts` is a bare Next 404 — the directory holds only
`[contractId]/page.tsx`. There is no index page and no nav link; the sole route in is
`lib/tasks/completers.ts:139`. 0 contracts exist today, so nothing is broken yet, but the entity
becomes unreachable the moment the kickoff ToDo is dismissed or the customer wants to look again.

The entity and its kickoff workflow were built (V1-10); the navigation was not.

**Fix.** An index page (`app/portal/[tenantSlug]/contracts/page.tsx`) listing the tenant's awards —
title, status, award date, value, period of performance — plus a **Contracts** entry in the portal
rail. Read-only, matching the detail page: nothing in V1 mutates a contract from the portal. Archived
contracts sort last and render dimmed rather than disappearing.

**Caught while building it, worth its own line:** the first version formatted
`award_amount_cents` straight through `toLocaleString({style:'currency'})` without dividing by 100,
rendering a $274,500 award as **$27,450,000**. It looked completely plausible on the page, and no
type checker can see it — cents and dollars are both `number`. The drive caught it because it
asserts on the *formatted string* against a known fixture value, not on the query.

**Proven** by `frontend/e2e/b50-b53-reachability-drive.spec.ts`: empty state renders (not a 404),
the rail links to it, a fixture contract lists at the correct formatted value, and the row leads to
the detail page it was always meant to.

## B51 — FIXED. One application raises TWO ToDos, and neither closes when it is decided
3 applications, all `status='accepted'` — and **6 open ToDos**:

| creator | task_type | title | entity link |
|---|---|---|---|
| `app/api/applications/route.ts` `createTask` | `application_triage` | `Review application: Acme Labs …` | yes |
| mig 040 automation rule | `admin_review` | `Review application from …` (B49) | no |

Two problems: (a) the automation rule duplicates the route's own explicit ToDo, and the duplicate
is the *worse* one — untyped for the domain, unlinked, and (until B49) unnamed; (b)
`app/api/admin/applications/[id]/accept/route.ts` sets `status='accepted'` and emits
`application.accepted` but never completes the open ToDos, so the admin queue never drains.

**Fix.** (a) **mig 204** deactivates the `Auto-todo on application` rule (both consumers honour
`is_active`) and cancels the entity-less copies it already left — matched on the rule's own
`Review application from %` title shape, so the route's `Review application: %` rows survive. The
route's ToDo is kept because it is the one that is typed, entity-linked and named. (b) a shared
`closeTasksForEntity` helper (`lib/tasks/tasks.ts`) closes every open ToDo on an entity through the
normal `completeTask` path — audit event, workflow resume and all — and **both** the accept and
reject routes call it, so answering the question closes it either way.

**The part that was not in the original report, and would have made the fix a no-op:** these rows
are platform-scope (`tenant_id IS NULL`), and mig 185 split `tasks` into per-command RLS policies
where UPDATE is `own only`. NULL never equals anything, so under the NOBYPASSRLS `govtech_app` role
a platform ToDo is **readable but not updatable**. Proven directly against the enforced sandbox:

```
select_no_ctx=1      -- SELECT finds the row
update_no_ctx=0      -- the paired UPDATE changes nothing
```

`completeTask` would then have seen 0 rows updated and returned `TASK_CLOSED` — reporting success
while changing nothing. The helper therefore runs the completion inside `runInBypass` (the scoped
form of the `enterBypass()` that `/api/admin/tasks` already uses) and gates on `rfp_admin+`, since
bypass removes the RLS layer.

**Proven end to end** by `frontend/e2e/b51-application-todos-drive.spec.ts` (2 cases, both green):
apply at the public form → exactly one ToDo, typed + entity-linked, no orphan duplicate → operator
decides it in their own queue screen → the row reads `completed` with `result.decision` and a real
`completed_by`, and nothing about that application is still open. Rejecting is covered as its own
case because accept and reject are separate files; a queue that drains on yes but not on no still
fills up, just only with the applications nobody wanted.

## B52 — FIXED. 21 identical triage ToDos, no entity link
`triage_new_opportunities`: **21 open rows, 1 distinct title, entity_id NULL on all of them**,
created in one 17-minute window. `pipeline/src/workflows/on_opportunities_detected.py` raises one
per detection with a **static** `task_title` — so N intakes produce N indistinguishable rows
pointing at a single shared backlog.

The source comment claims this is "bounded; the processor dedups by trigger_event_id" — that dedup
prevents one event creating two rows. It does not bound the queue.

The step maps `entity_ref="payload.source"`, but the payload's `source` is
`'intake:' + foundBy` (`lib/intake.ts:113`) — a string like `intake:admin` — while `tasks.entity_id`
is **uuid**. The declared entity link can never populate.

`tasks` already carries the right pattern for a singleton work item:
`uq_starter_offer_open UNIQUE (tenant_id) WHERE task_type='starter_set_offer' AND status IN ('open','in_progress')`.

**Fix (verified on both sides of the wire, 2026-08-22).** `on_opportunities_detected.py` now reads
`task_title="payload.triageTitle"` and `entity_ref="payload.solicitationId"` instead of a static
string and the unresolvable `payload.source`, and `lib/intake.ts` emits both — `solicitationId` (a
real uuid, so `_safe_uuid` keeps it and `taskHref()` can render an "Open →") and a per-opportunity
`triageTitle` (`Triage new opportunity: <title>`). Each intake therefore produces one DISTINCT,
openable row rather than another copy of a shared backlog. The reasoning is kept in a comment at the
step, so the two static values do not creep back.

Not re-provable by row count in this sandbox — it was reset, so `triage_new_opportunities` is
legitimately 0 rows. Confirmed by reading the producer and consumer against each other instead of
counting, which is the honest check for a bug whose signature was "N rows that should have been N
different rows".

## B53 — FIXED. The Scout Monitor's health panel is a dead instrument
`/admin/scouts` renders ACTIVE SOURCES 6 · **HEALTHY 0** · DEGRADED 0, with every source reading
`unknown` / `never` / `—`, while "Recent scout runs" shows 4 **completed** runs. An operator reads
that as a dead worker pool.

`source_health` is written **exactly once in the repository's history**: three seed rows in
`db/migrations/002_seed_system.sql`, status defaulted. It is read only by
`app/admin/scouts/page.tsx:85`. **No runtime writer exists** in the frontend or the pipeline — so
the HEALTHY and DEGRADED tiles are structurally always 0, forever.

The join compounds it. `LEFT JOIN source_health sh ON sh.source = sp.site_type`:

```
profile_site_type | health_source | status
afwerx            |               |          ← no health row
dsip              |               |          ← no health row (and dsip appears twice)
nsf               |               |
sam_gov           | sam_gov       | unknown  ← the only match, and it is 'unknown'
xtech             |               |
                  | grants_gov    | unknown  ← orphan, matches no profile
                  | sbir_gov      | unknown  ← orphan
```

Note also that the page reads `source_profiles` / `source_health` / `pipeline_jobs`, while the
newer intake queue (mig 175) uses `scout_sources` / `scout_runs` / `scout_findings`. Two parallel
scout data models exist; `scout_runs` is empty and the monitor does not read it.

**Class → sweep for** *a metric with no writer*. A tile bound to a table nothing populates renders
a confident zero, which reads as a measurement rather than an absence — the same failure the
ingest-provenance doctrine forbids for solicitation values.

**Fix.** `source_health` is out of the page entirely — not repointed, not backfilled, dropped from
the query, with the reasoning kept in the file header so nobody re-adds the join. Status is now
DERIVED from what a scout pass genuinely writes: `source_profiles.last_crawl_at` (written by
`lib/tools/source-scout.ts` §5 on every run) and the per-profile `source_diffs` record, with
`last_visited_at` shown separately as what it actually is — the HITL "an admin opened this" stamp,
which the old page mislabelled as scout activity.

The vocabulary is the substance of the fix. `manual` — active, auto-crawl off — is now its own
status, because **most of these sources are read by a person, and that is the configuration, not a
fault**. Calling that "unhealthy" (or leaving it as `unknown`) is what made the panel unreadable.
`Needs a look` counts only auto-crawl sources that have never run or are more than two days behind
— an actionable number that is zero only when it is genuinely zero.

Live now, and true: six sources, all `manual`, so `AUTO-CRAWLED 0 · NEEDS A LOOK 0`. Same digits as
the old panel; the difference is that these ones mean something, and the page says what.

**Proven** by `frontend/e2e/b50-b53-reachability-drive.spec.ts` — deliberately including the
NEGATIVE assertions that the `HEALTHY` and `DEGRADED / ERROR` tiles are gone and that no row reads
`unknown`. Replacing an always-zero tile with a different always-zero tile would otherwise pass.

## B54 — FIXED. "Include closed" hides ARCHIVED, not closed
**Found by** cropping `/portal/foundation/cards` at native resolution.

The filter is unchecked and the list still shows opportunities badged **Closed** — including
unpinned ones ("Pin to pursue", so not kept deliberately). `app/api/portal/[tenantSlug]/cards/
route.ts:95` read:

```js
${includeClosed ? tx`` : tx`AND c.lifecycle_status <> 'archived'`}
```

Three different notions of "closed" met on one screen:

| what the customer sees | where it comes from | did the filter touch it? |
|---|---|---|
| gray `Closed` chip | `closeDate` in the past, computed client-side | no |
| amber `Closed` badge | `lifecycle_status = 'closed'` | **no** |
| — | `lifecycle_status = 'archived'` | yes — the only thing it filtered |

`lifecycle_status` CHECKs to exactly `('open','closed','archived')`, so `<> 'archived'` passes every
admin-closed card straight through. And per the archive doctrine **cards are not an archive target**,
so the predicate excluded a state nothing produces — a filter that did nothing at all.

The line directly above it is correct — `includePassed ? … : AND c.pursuit_status <> 'passed'` —
which is what makes this a slip rather than a design.

**Fix**: unchecked ⇒ `lifecycle_status = 'open'` server-side, plus a client-side date predicate
using the same `daysUntil` the chip renders from (there is no date sweeper — `lifecycle_status` only
moves on an explicit admin transition, so a past close date on an `open` card is the common case).
The deep-linked card from a notification is exempt: telling someone to look at an opportunity and
then hiding it because it just closed is worse than showing it closed.

## B55 — DOC FIX, not a bug. Platform rows are un-writable but NOT invisible
**Hypothesis I formed, and disproved by clicking the button.**

The walk showed 34 open admin ToDos and exactly one ever completed. `tasks` RLS says:

```
tenant_isolation_select  USING (tenant_id = app.tenant_id OR tenant_id IS NULL)
tenant_isolation_update  USING (tenant_id = app.tenant_id)          ← no NULL arm
```

`completeTask` runs its `UPDATE tasks SET status='completed'` on the context-aware `sql`. I checked
as `govtech_app` directly — `UPDATE 0` — and concluded the admin's **Approve / Done** button was a
no-op, which fit the 34:1 ratio perfectly.

**It is not.** Clicking it (`e2e/admin-todo-complete-drive.spec.ts`) returns
`200 {"taskId":"3714a01f-…","resumed":true}`, the button count goes 36 → 35, and the completed
count goes 1 → 2 in the database. `/api/admin/tasks` calls `enterBypass()` after its admin gate, so
the `sql` Proxy routes to the owner pool for that request. Working as designed; the 34:1 ratio just
means nobody had been clicking them.

What IS wrong is a sentence in CLAUDE.md. It says a platform row is *"invisible AND un-writable
through the context-aware `sql` under `govtech_app`"*. Un-writable is right. **Invisible is not** —
the SELECT policy has an explicit `OR (tenant_id IS NULL)` arm, and a Foundation tenant context
reads all 35 platform tasks:

```
SET app.tenant_id='17780cad-…'; SELECT count(*) FROM tasks WHERE tenant_id IS NULL;  → 35
```

No leak reaches a user: `listOpenTasksForActor` filters non-admins to
`assignee_role IN ('tenant_admin','tenant_user','partner_user') AND tenant_id = $1`, so ToDos
assigned to `rfp_admin` never match. But the belt is doing that work alone, not the belt AND the
suspenders the doc claims. Corrected in CLAUDE.md.

**Class → sweep for** *a conclusion drawn from a policy definition instead of an execution*. I had
the row counts, the policy text, and a matching ratio — three pieces of evidence pointing one way,
and the wrong answer. The click cost 11 seconds.

## B56 — FIXED. The partner console counts 7 to-dos above a list of 8
**Found by** counting the rows in the Partner Console screenshot against its own badge.

The header reads **"7 open to-dos across your companies"** and Foundation's card reads
**"7 TO-DOS"**, directly above a "TO-DOS ACROSS YOUR STABLE" list of **eight** items. Two queries on
one screen, with different predicates:

| | file | predicate | result |
|---|---|---|---|
| the badge | `lib/partner/rollup.ts:41` | `assignee_role IN ('tenant_admin','tenant_user','partner_user')` | 7 |
| the list | `lib/partner/todos.ts:45` | …same… `OR tk.assignee_user_id IS NOT NULL` | 8 |

A ToDo named to a PERSON rather than a role — here "Set up your build workflow — review the dates,
owners & reminders, then Accept & Start", the TW-1 required setup gate — rendered in the feed and
went uncounted. Verified against the live tenant:

```
old_count | new_count
        7 |         8
```

**Fix**: the count takes the list's predicate, with a comment on each side naming the other, because
the invariant is that they agree.

**Class → sweep for** *a count and a list of the same thing computed by different queries*. The
screen is where they meet, so the screen is where the disagreement shows — and nowhere else.

---

### Sandbox-hygiene note (not a product bug)

Two of these surfaces render internal fixture names to a would-be customer: a card titled
`FLEX mid-window scenario [owned:flex]` in Foundation's opportunity pipeline, five cards named
`E2E ARC 1787403…`, and a partner ToDo reading `Final submission due: FLEX mid-window scenario
[owned:flex] [FLEX drive]`. That is the bridge working as designed — a push fans to EVERY tenant —
but it means **every E2E run permanently pollutes every tenant's customer-facing card list** in this
sandbox. Worth a cleanup pass before any demo, and worth naming fixtures so they read as fixtures.

## B57 — CORRECTED. my own verification command lied, in the exact way this log is about
While checking the B56 fix I ran:

```sh
npx tsc --noEmit 2>&1 | head -3; echo "tsc:$?"
```

It printed three TypeScript errors AND `tsc:0`. `$?` is the exit status of the **last** command in
the pipeline — `head` — which succeeds whether or not `tsc` did. Had I skimmed for the `0` instead of
reading the lines above it, I would have called a broken file clean.

The break was mine and it is a good one to know: I put **backticks** around an identifier in a SQL
comment —

```js
-- The `assignee_user_id IS NOT NULL` arm MUST match …
```

— inside a `sql\`` tagged template. The first backtick ends the literal, and the rest of the query
parses as JavaScript. Vitest still passed 1608/1608, because the tests never typecheck that file.

Two rules out of it:
1. **Never put a backtick in a comment inside a tagged template.** Same for `${`.
2. **Capture the exit status of the command you are testing, not the pipeline.**
   `OUT=$(npx tsc --noEmit 2>&1); echo "exit=$?"` — and print the error count, so silence is
   distinguishable from success.

This is Mistake 0 (*a handler that catches its own failure and carries on*) in the verification
harness rather than the product. A green tick I did not earn is worse than a red one I did.

---

## Verification — all four fixes photographed on the running build

Rebuilt (`next build`, exit 0), restaged `.next/static` + `public`, restarted the standalone server,
restarted the pipeline worker so it reloaded the edited workflow definition, then re-walked. Crops
kept in `docs/manuals/img/crops/proof/`.

| bug | before | after | evidence |
|---|---|---|---|
| **B49** | `Review application from` (no name) | resolver mutation-proven | reverting the fix fails the new test with `expected 'Review application from ' to be 'Review application from Acme Labs'` |
| **B52** | 21 rows, one title, `Approve / Done · Dismiss` only | `Triage new opportunity: B52 identity probe …` with **`Open →`** | live intake → `entity_type=solicitation`, `entity_id=a1813466-…` matching the id the intake returned |
| **B54** | `· 38 cards`, `Closed` badges throughout | `· 28 cards`, **no `Closed` badge anywhere** | page height 3827px → 2970px; every remaining card shows a live `Closes in Nd` chip |
| **B56** | `7 open to-dos` above a list of 8 | **`8 open to-dos`** | matches the 8 rows; SQL confirms `old_count=7, new_count=8` |

B50 (no contracts index) and B53 (dead scout-health instrument) are **open** — both are documented,
neither is fixed. B53 is now called out in the operator guide so nobody reads the tile as a
measurement; B50 is harmless while zero contracts exist.

**Backbone**: `tsc` 0 errors · `vitest` 1608/1608 · `next build` exit 0 · all three guides + index +
combined render with no missing images.

One caveat on B54, stated plainly: the fix is verified for the tenant whose data I walked
(Foundation, 38 → 28). I did not re-walk every tenant, and the client-side date predicate is
exercised by the screenshot rather than by a unit test — the pure function it calls (`daysUntil`)
predates this change.

---

# Midterm run, 2026-08-22 — MT-1 ingest: B58–B60

Clean-room start: the database was wiped and rebuilt from migration 001 (202 applied, 0 skipped),
so every count below is measured from a known baseline — 7 tenants · 16 users · 19 opportunities ·
**0 applications** · 4 tasks. The accumulated ToDo debris from earlier runs is gone.

Four solicitation fixtures now live in `frontend/e2e/fixtures/solicitations`
(`scripts/make-solicitation-fixtures.py`), one per agency shape, each stating its rules with
DIFFERENT wording and DIFFERENT values so a passing extraction proves the reader read *that*
document rather than fell back on a default that happened to be right.

## B58 — FIXED. The minimum type size is unreadable when stated subject-first
`min_font.no_smaller_than` requires the words in the order *"no font smaller than N point"*:

```
no      "Type size shall be no smaller than 11 point."     ← DoD, DOE, Ohio all print this
MATCH   "Use no font smaller than 11 point."               ← the inversion the rule was written to
```

Three of the four fixtures state it subject-first, and all three returned **nothing** for the
field — falling through to a default, which is exactly what the ingest-provenance doctrine exists
to prevent. Added `min_font.size_no_smaller_than`, anchored on the type/font subject so a bare
"no smaller than" about something else cannot be captured (there is a test for that).

## B59 — FIXED. Fractional margins could not be read, and would have been MISREAD if they were
The three margin rules alternated over `(one|two|1|2)` — integers 1 and 2 only:

```
MATCH   "Use 1 inch margins on all sides."
no      "Applications shall use 0.75 inch margins on all sides."   ← DOE
no      "Use 1.5 inch margins on all sides."
```

Half- and three-quarter-inch margins are ordinary. Worse, `inches()` parsed with **parseInt**, so
`"0.75"` → 0 (rejected) but `"1.5"` → **1** — reporting a margin the document does not state. The
integer-only alternation was the only thing keeping that latent misread unreachable; widening the
pattern without fixing the parse would have shipped it. Both changed together: `MARGIN_NUM` now
admits worded and decimal measurements, and `inches()` uses parseFloat.

## B60 — NOT A BUG, verified rather than assumed. The typeface rule is conservative on purpose
Three fixtures say *"shall be prepared in <name>."* with the word "font" only in the next sentence,
and the extractor declines to claim a typeface. That reads like a fourth gap. It is not:
`TYPEFACES` contains **Georgia** and **Cambria**, which are also place names, so
*"Proposals shall be prepared in Georgia"* is genuinely ambiguous. Requiring a font/typeface token
in the same sentence is the correct guard, and declining leaves the field to the AI layer or the
curator — which is the doctrine working. Recorded as an expected outcome in the probe rather than
"fixed".

**Verification.** `scripts/probe-pattern-extract.mts` runs the real extractor over the real shredded
text of all four fixtures and asserts a specific table: 20/20, exit 0. Nine unit tests added
(30 → 39 in that file); both fixes mutation-checked — reverting `parseFloat` fails the two margin
tests, removing the new rule fails the subject-first test. Backbone: tsc 0 · vitest 1617/1617.

### Environment note
`pymupdf4llm` is declared at `pipeline/requirements.txt:26` but was **not installed** in this
container, so `extract_text_from_pdf` raised for every PDF and the entire shred path was dead
here. Installed. Worth knowing that the container's Python environment drifts from
requirements.txt the same way the database drifts from the migration head.

## B61 — FIXED. A solicitation that prints its own page numbers loses every page citation
**Found by** the MT-1 live ingest: all four solicitations reported *"No page markers in the
extracted text — evidence cites excerpts, not page numbers"*, while a direct probe of the same
PDFs had resolved page 2 correctly.

There are **two PDF readers**, and that is the root of it:

| path | reader | output |
|---|---|---|
| `app/api/admin/rfp-upload/route.ts` (synchronous, on upload) | `pdf-parse` | plain text, **injects `-- N of M --` at every page break** |
| `pipeline/src/shredder/extractor.py` | `pymupdf4llm.to_markdown` | markdown, no separators |

The upload route is what fills `solicitation_documents.extracted_text`, deliberately — *"extracting
now beats hoping a worker can fetch them back out of storage."* So the stored text carries
pdf-parse's separator. And a real government solicitation ALSO prints a page footer. Both match
`PAGE_MARKER_RES`, so the marker stream arrives doubled:

```
@  603 '-- Page 1 of 3 --'      ← the document's own footer
@  622 '-- 1 of 3 --'           ← pdf-parse's injected separator
@ 1310 '-- Page 2 of 3 --'
@ 1329 '-- 2 of 3 --'
```

`buildPageIndex` reads 1,1,2,2,3,3, hits `page <= prev.page` with `page !== 1`, concludes "not a
paging scheme at all", and throws away **every** page number. Proven both directions on the live
stored text:

```
stored text (both marker sets)        → pageResolved: false, page 1
same text, document's footer removed  → pageResolved: true,  page 2
```

The value is still read correctly; what degrades is the CITATION — and citability is the entire
reason `pattern_match` outranks `ai` in the trust order. The documents this hits hardest are the
realistic ones, because government solicitations number their pages.

**Fix**: a mark repeating the previous page AND total is the same boundary seen twice — keep the
first, skip the echo. Four tests added, including two that hold the line the fix must not cross:
an erratic 7,3,9 sequence still refuses to resolve, and a genuine document boundary (numbering
restarting at 1) still segments. Mutation-checked: removing the dedupe fails both new assertions.

Two of those tests failed on first run because my fixtures were under `MIN_USABLE_TEXT_CHARS` —
the extractor declining a 110-character "document" is the guard working, not a bug. Padded.

**Class → sweep for** *two producers of the same artifact*. Neither reader is wrong; the defect
lives in the assumption that only one of them exists.

---

## B62 — FIXED (mig 203). a new tenant is born at 100% of the bucket cap, so it can never author its own scoring lens

**Found**: driving the midterm arc as a real customer. The tenant `northwind-additive` was
onboarded through the public application form, its library was built from its own uploads, and
then the very next thing a customer does — author the lens they want opportunities ranked by —
answered:

```
POST /api/portal/northwind-additive/buckets  →  409
{"error":"You've reached the limit of 6 spotlight buckets. Delete one to add another.",
 "code":"BUCKET_LIMIT"}
```

on a tenant that had authored **zero** buckets.

**Cause**: two correct features whose numbers were set independently and happen to be equal.

- `lib/spotlight/default-buckets.ts` (#104) seeds a starter set on every tenant-creation path
  (`applications/[id]/accept`, `create-tenant`, `create-partner-org`, `partner/own-org`) so fanned
  cards rank on arrival instead of ranking null. That set is **6**.
- Mig 181 moved `automation_framework.max_buckets_per_tenant` **12 → 6**.

Before 181 there were 6 free slots; after, zero. Proven on the live sandbox:

```
 slug               | active_buckets
 northwind-additive |              6      ← all 6 seeded, none authored
```

The design doc states both halves in one sentence, which is where the collision hid in plain
sight — RANKING_SPINE.md §15: *"Cap — global default raised **12 → 6**; keep all 6 seeded
defaults; enforce the cap"*. (It also calls a lowering a raise, which is the tell that the
direction was not being tracked.) The cap is enforced exactly as designed; the seeded set is
exactly as designed; the pair closes the door on the spine's own headline capability, which the
same document opens with: *"bucket authoring → cap → OPP-push rescore"*.

**Why the tests did not catch it**: the cap has a unit test, and it passes — it asserts that
create returns 409 `BUCKET_LIMIT` **at the ceiling**. It builds its own fixture to get there. No
test asked what a tenant's bucket count is the moment it is created, so nothing connected the
seeder's 6 to the cap's 6. This is only reachable by walking in as a new customer.

**Fix** (mig 203 + `lib/automation/policy.ts`): put the two numbers in a relationship instead of
leaving them as independent constants.

- `DEFAULT_MAX_BUCKETS = DEFAULT_BUCKETS.length + BUCKET_AUTHORING_HEADROOM` (6 + 4 = 10)
- `MIN_MAX_BUCKETS = DEFAULT_BUCKETS.length + 1`, and `getMaxBucketsPerTenant` floors any
  configured value at it — so no admin tuning at `/admin/automation-framework` can silently
  re-close authoring either.
- Mig 203 raises the stored cap 6 → 10 **only where it still sits at the mig-181 value**, leaving
  a deliberately-tuned cap alone.

Verified after the fix: cap reads 10, and a freshly-onboarded tenant authors its own bucket.

**Class → sweep for** *a limit and a default that were sized by different authors*. The bug is
not in either number; it is that nothing in the code made them aware of each other. Same shape as
B61 (two producers of one artifact) — a defect that lives in the gap between two correct pieces.

---

## B63 — FIXED (harness). the drive harness had no action timeout, so one missing selector ate a whole run

**Not a product defect — a test-harness one**, but it is the reason several midterm runs produced
nothing, so it belongs here with the rest.

`playwright.config.ts` set `timeout: 60_000` (per test) and `expect.timeout`, but never
`actionTimeout` or `navigationTimeout`. Both default to **0 in Playwright, which means wait
forever**. The long drives raise the per-test timeout to 30 minutes so the arc can finish, and the
combination is the trap: any `page.fill` / `page.click` / `page.goto` whose target never appears
blocks for the full 30 minutes and the run ends having reached ACT 4 of 8.

Observed exactly that on the arc: the last screenshot written was `06-opportunity-cards.png` at
16:49:32 and nothing moved for the rest of the run — a `signIn()` sitting on
`page.fill('input[name="password"]')`. There was no error to read, because nothing had failed
yet; it was still politely waiting.

**Fix**: bound both in the shared `use` block — `actionTimeout: 20_000`,
`navigationTimeout: 45_000` — so a missing element fails in seconds. On the drives that failure
is caught by the ledger's `step()` wrapper, recorded as `blocked` with its message, and the arc
carries on to the acts after it. `signIn` additionally reports the URL it landed on and the first
200 characters the page rendered, because "sign-in failed" is not a diagnosis and "landed on
/portal/x with no password field" is.

**Class → sweep for** *defaults that mean "infinite"*. A zero in a timeout field reads like
"unset, so something sensible applies" and means the opposite. The tell is a run that produces no
error at all — a hang is not a failure, and only a bound turns it into one.

## B64 — FIXED. The page ruler and the printed page disagreed, in the direction that loses bids
**Found by** building the instrument that was missing: `scripts/calibrate-page-ruler.mts` renders
synthetic documents through the real Chromium exporter and compares the printed page count to
`paginate()`.

`estimatePageCount` decides two things a customer's bid depends on — the gauge they watch while
writing, and the export gate that refuses to advance a volume over its page limit. Both call the
same engine, so they can never disagree *with each other*; that invariant was built deliberately and
it holds. **Nothing ever checked whether either agrees with the page.** The estimator's own comments
record three prior corrections of exactly this class ("a flat height for every image; a footer token
nothing substituted; captions borrowed from alt text"), each found by accident.

Three defects, all under-counting — the direction where the product clears a volume the printer does
not, and the customer is told they are fine:

| case | ruler said | Chromium printed |
|---|---|---|
| 40-row table, single-line cells | 2 | 3 |
| 40-row table, **wrapping** cells | 2 | **4** |
| one sentence of prose above that table | 2 | 3 |

1. **A table row was modelled as 1.35 lines of BODY text.** Measured by binary-searching the
   largest table that still fits one 648pt page, at three cell lengths:
   `1-line 19.64 < row ≤ 20.25pt · 2-line 30.86 < row ≤ 32.40 · 3-line 43.20 < row ≤ 46.29`.
   Solved together those admit `ROW_LINE ∈ (10.8, 12.76)`; 12.0 with a base of 8.0 satisfies all
   three. Reading the stylesheet instead gives `10 × 1.28 = 12.8` from the body's unitless
   line-height — **outside the measured bracket**, because table text resolves at `normal` leading.
   That is precisely why these constants are measured and not derived.
2. **Nothing modelled a cell that wraps.** The old code counted ROWS, so a milestone table whose
   Deliverable column holds a real sentence — the most common table in a proposal — printed a page
   longer and estimated exactly the same. This is the one that mattered.
3. **Columns were assumed equal width.** The table renders `width:100%` with auto layout, so a
   "Month" column holding `1`..`40` takes a sliver and the wide column takes most of the table.
   Splitting evenly made wide columns look narrow, so their text appeared to wrap twice as often as
   it does. Now weighted by each column's longest cell, which is the rule auto-layout uses.

A fourth, in `paginate()` itself rather than the height model: **`fitKeep` refused to relocate an
oversized block.** `table`/`figure` carry `break-inside: avoid` (canvas-html), and Chromium pushes
such an element to a fresh page *even when it cannot fit there*, then overflows. The guard
`h <= usableH` meant the ruler kept flowing it from wherever it landed. Proven directly:

```
table(40 rows) alone                   → 2 pages
ONE sentence of prose + the same table  → 3 pages     (the ruler said 2 for both)
```

**Real-world impact, measured on the four hand-authored proposals in the sandbox** — not synthetic
shapes. Old ruler vs. printed PDF:

```
TVSF-R45  Ohio TVSF Round 45      88 nodes · 6 tables   ruler  9   printed 10   ← a page short
N26D-CAM07 DoW STTR D2P2          45 nodes · 6 tables   ruler  3   printed  4   ← a page short
N261-EXP01 Navy SBIR Phase I      34 nodes · 0 tables   ruler  2   printed  2
TVSF Round 45 (earlier build)     46 nodes · 6 tables   ruler  4   printed  4
```

Two of four were being measured a page short, and both of those carry tables. After the fix all four
agree exactly, and all 15 synthetic cases agree.

**Class → sweep for** *a model of an artifact that nothing ever compares to the artifact*. Two
consumers reading one function agree with each other by construction; that is not evidence either is
right. The check that matters renders the thing and counts.

**Guarding it:** `__tests__/page-ruler-calibration.test.ts` locks the arithmetic (6 cases, no
Chromium needed) and `scripts/calibrate-page-ruler.mts` is the authority, exiting non-zero on any
drift. Re-measure with `scripts/measure-table-row-height.mts` rather than re-guessing the constants.

## B65 — FIXED. A list was measured as a paragraph, so the deck overflow check never fired
**Found by** extending B64's method to decks: `scripts/calibrate-slide-ruler.mts` exports real
`.pptx` files and compares them to `estimateSlideCount` / `overflowingSlides`. No `.pptx` had ever
been produced end to end, so these two had never been compared to a rendered artifact either.

Slide COUNTS were correct on all seven cases — v1 `page_break` splitting, v2 one-section-per-slide,
and the trailing-break edge case. The defect was in HEIGHT, and it turned out to be shared with the
page ruler.

`nodeStackHeightPt` had no case for `bulleted_list` / `numbered_list`, so both fell through to the
flow-text default: **every bullet concatenated into one string and reflowed at full column width**.
Twenty short bullets measured as three lines instead of twenty. Two consequences:

- **Documents**: 120 bullets read 3 pages and printed 4.
- **Decks**: a slide holding 30 bullets — needing ~648pt of a 452pt frame — reported **no
  overflow**. `overflowingSlides` is the only thing standing between a customer and a deck whose
  content is cut off the bottom, and it stayed silent until 60 bullets.

Same shape as B64's table-wrap defect: *the model flattened structure the renderer preserves.*
Fixed by measuring what the stylesheet actually lays out (`ul, ol { margin: 0 0 8pt 20pt }`,
`li { margin: 0 0 3pt }`): one wrapped line-box per item, `+3pt` between items, `+8pt` after the
list, and nested children indented another 20pt per level. The line height uses the stylesheet's
`max(lineSpacing, 1.28)` floor rather than `bodyLineH`, because for a list a fractional error
repeats **per item** instead of averaging out across a reflowed paragraph.

**Two harness mistakes worth recording, because both produced convincing false results:**

1. **`list` is not a NodeType.** The real ones are `bulleted_list` and `numbered_list`. A fixture
   built with `'list'` rendered as nothing and measured as nothing, so the first run showed
   "120 bullets → ruler 1, printed 1 ✓" — a perfect agreement about an empty document. It briefly
   looked like evidence that list content vanishes from exports entirely.
2. **A `.pptx` is a ZIP.** Regexing the raw buffer finds *filenames* (stored uncompressed in the
   local headers) but never the markup, so the first overflow probe reported "no shapes past the
   frame" for every slide, including ones that plainly overflow. Fixed by unzipping properly.

And one honest limit, now written into the script: the pptx exporter **clamps** each box to the
frame (`placeBox`: `Math.min(p?.h ?? defaultH, maxH)`), so overflowing content never appears as a
shape positioned past the slide edge — it is text spilling *inside* a box that fits, which the XML
geometry cannot see. Shape bottoms are still checked (one past the frame would be a worse bug), but
the case is decided on the arithmetic, which is what the script is auditing.

**Class → sweep for** *a model that flattens structure the renderer preserves*. B64 found it in
table cells, B65 in list items. Any node whose content is a LIST of things — rows, items, series,
steps — is a candidate; measuring its text length is not measuring its height.

**Guarding it:** five list cases in `scripts/calibrate-page-ruler.mts` (20/20 agree) plus the deck
harness (7/7), and six unit cases in `__tests__/page-ruler-calibration.test.ts` — including the sharp
one, that the same characters as a list must measure taller than as a paragraph.

## B66 — FIXED (two of three). Sweeping the rest of the node vocabulary for the B64/B65 class
Having named the class in B65 — *a model that flattens structure the renderer preserves* — the
cheap move was to sweep the remaining node types through the same harness rather than wait for the
next one to surface. Twelve node types added to `scripts/calibrate-page-ruler.mts` in one pass.
Two more found, one of them predicted from reading the stylesheet alone:

### `code_block` — FIXED. Newlines were reflowed away
Renders inside `<pre style="white-space:pre-wrap; padding:12pt; font-size:9pt">`, so every `\n` is
a hard line. Measured as flow text it became one reflowed paragraph: **60 lines of code read 1 page
and printed 2.** Now modelled on the `<pre>`'s own geometry, with a monospace advance of 0.6em
(Courier New) rather than the body's proportional 0.45 — using 0.45 would have over-estimated how
much code fits on a line. Exact on both cases (preserved newlines, and one very long wrapping line).

### `toc` — FIXED in isolation, with a NAMED residual. It was modelled as zero height
`nodeStackHeightPt` returned `0` for a contents list, while `buildTocHtml` emits one indented block
per heading in the document. A 40-entry contents list therefore cost **nothing** in the estimate and
two pages in the print.

The height depends on the whole document, which a per-node ruler cannot see, so `paginate()` now
collects the headings once and threads them in (only when the document actually contains a `toc`,
so the common path is untouched). The per-entry height is **measured, not read off the stylesheet**:
binary-searching the largest contents list that still fits one 648pt page gives **31 entries**,
bracketing the entry at `(19.14, 19.76]pt`; the model's 19.36 sits inside it.

**Residual, stated rather than tuned away.** On a document that is a contents list followed by
nothing but headings, the total can still be ±1 page. Isolated component-wise:

```
30×(h2 + prose)  — NO toc     ruler 4  printed 4  ✓
30×(h2 + prose)  — WITH toc   ruler 5  printed 4  Δ-1
40× h3           — NO toc     ruler 2  printed 2  ✓
40× h3           — WITH toc   ruler 3  printed 4  Δ+1
```

Both documents are exact without the toc, and the errors run in *opposite* directions — so this is
not the toc's height. It is `h1,h2,h3 { page-break-after: avoid }` in canvas-html: a heading at the
foot of a page moves to the next page **with its first paragraph**, leaving whitespace the ruler
spends. The ruler does not model that rule. It only bites when something pushes headings onto a page
boundary, which is what a contents list does. Left un-modelled deliberately: it needs the same
measure-then-fit treatment as the table row, and inventing a constant to make two synthetic
documents agree would be fitting noise. Both cases carry an explicit `tolerance: 1` in the harness
with this reason attached.

**Two hypotheses falsified along the way, worth recording because both were plausible:**
- *"The residual is the prose estimator rounding up once per paragraph."* Tested directly — 20/40/60
  short paragraphs versus the same characters as one paragraph — and prose is **exact in both
  forms**. Wrong.
- *"The pptx exporter positions overflowing shapes past the slide frame."* It clamps them
  (`placeBox`), so overflow is text spilling inside a box that fits and the XML geometry cannot see
  it. The deck harness now says so instead of implying a check it does not perform.

### The harness defeated itself, which is the same bug it exists to catch
Adding the `tolerance` field, the default landed at the comparison rather than at the push, so
`r.tolerance` was `undefined` — and **every comparison against `undefined` is false**. The script
printed "← off by −1" beside two rows and then declared "✓ agrees on all 28 cases", and would have
reported success for *any* delta from then on. Caught by noticing the output contradicted itself.

A calibration harness that cannot fail is worth less than no harness, because it is *believed*. The
default now lives at construction, and the run log in this session is the evidence it still fails
when it should: it exited non-zero four times on the defects fixed in B64/B65/B66 before going green.

**Class → sweep for** *a check whose failure path is never exercised*. The instrument deserves the
same suspicion as the thing it measures.

---

### B46 — the fix (2026-08-22)

**Counted the writers before changing any of them.** Ten places insert into `opportunities`; the
back-link was written by **seven**, and the three that never wrote it were the umbrella paths:

```
lib/intake.ts                                     *** NEVER ***   ← the head of the river
pipeline/src/workflows/actions/create_drafts_from_scout.py  *** NEVER ***
pipeline/src/ingest/base.py                       *** NEVER ***
lib/ingest/ingest-topic-files.ts                  writes it
lib/ingest/materialize.ts                         writes it
lib/tools/opportunity-add-topic.ts                writes it
lib/tools/opportunity-bulk-add-topics.ts          writes it
app/api/admin/rfp-upload/route.ts                 writes it
pipeline/src/ingest/sbir_gov.py                   writes it
pipeline/src/ingest/topic_expander.py             writes it
```

`lib/intake.ts` is the one that matters most: `stageIntake` is the head of the river, used by admin
intake **and** the #176 scout release, and it creates the opportunity and its curated solicitation in
one transaction — so the id was already in scope and simply never written back.

**My first count of this was wrong, in the way this log is about.** I grepped the INSERT column
lists and reported six writers missing it. `materialize.ts` sets the back-link in a **separate
UPDATE** after its insert, which a column-list grep cannot see. Re-running the check against *any*
statement that assigns the column — `INSERT` columns **or** `UPDATE … SET` — cut the list from six
to three. A narrow check reported more work than existed; the same narrowness in the other direction
is what let me report this entry closed when it was open.

**Which direction is canonical — established from the product, not chosen.** `compliance-resolver.ts`
already reads the back-link and carries an explicit umbrella fallback, with a comment recording why:
without it *"an umbrella purchase resolves to a NON-degraded empty result and the buyer is
provisioned a default skeleton while the fully-authored master sits unread."* Somebody had already
hit this and patched around it. So the forward link was the reliable one and the back-link a
topic-only convention — which makes the fix *populate it everywhere*, not teach more readers a
fallback.

**Verified safe before backfilling umbrellas**, because stamping a column that queries filter on
could silently widen a result set. Every topic-only query also filters on `topic_number`
(`extract-topics`, `ingest-topic-files`, `opportunity-add-topic`, `opportunity-bulk-add-topics`), so
an umbrella cannot start matching one; the two queries that want both arms already union them
explicitly (`solicitation-push`, `curation/republish`), where this makes the `OR id = <umbrella>` arm
redundant rather than wrong. **No query changes its result set.**

**The fix.** The three writers now stamp it — `intake.ts` inside the same transaction as both
inserts, so the pair is never half-written; `base.py` unconditionally from whichever triage row owns
the opportunity, so a re-run repairs a row an earlier ingest left NULL. **Migration 205** backfills
the rows they already wrote. Live proof, driving `stageIntake` itself:

```
forward  curated_solicitations.opportunity_id → 23552d66-…
back     opportunities.solicitation_id        → a422791f-…
✓ both directions written and they agree
```

After the backfill: **19 of 19 back-linked, 0 NULL, 0 rows holding a curated solicitation without a
link.** `compliance-resolver`'s fallback stays as defence in depth rather than as the load-bearing
path, and `drive-baa-forward.mjs` can resolve through either direction.

**One thing found on the way out.** Migration 205 ends in a `RAISE NOTICE` recording the table's
shape, and `db/migrations/migrate.mjs` had no `onnotice` handler — so postgres.js dumped the whole
notice object (`severity_local`, `file: 'pl_exec.c'`, `routine: 'exec_stmt_raise'`) to stderr, and a
migration that succeeded looked in the deploy log like it had thrown. Silencing notices, as the app
client does, would have been the other wrong answer: a migration raises one precisely so the deploy
log records what it did. The runner now prints `[pg NOTICE] <message>` on one line, which every
future migration inherits.

## B67 — FIXED. "The open list is empty" was a claim this log could not support
**Found by** being asked to account for the arc, and checking the claim before repeating it.

I reported the bug log clear. I had verified it with `grep '^## B[0-9]* — OPEN'`, which understands
exactly one of the **three** heading conventions this log has accumulated:

```
## B44 — Every topic upload failed on an ON CONFLICT … · FIXED        trailing status
## B51 — FIXED. One application raises TWO ToDos …                    leading status
## B46 — `opportunities.solicitation_id` … · PARTLY PROVEN, OPEN      trailing, compound
```

The grep matched the middle form only. B46 was open and read as absent.

**And the miss was bigger than one entry.** A status vocabulary of `OPEN` alone also cannot see
`LOGGED` — the entries recorded deliberately and not yet done. There are **five**, including B40
marked *high*: *a large solicitation is silently truncated at 500,000 characters.* Saying "the open
list is empty" erased those too.

`frontend/scripts/bug-log-status.mjs` now reads the status from **either end** of a heading, sorts
entries into closed / open / deferred, and **refuses to guess**: a heading it cannot classify is
reported as UNKNOWN and counted against the exit code, so a new convention surfaces as work rather
than as silence. It found three such headings immediately (B57, B62, B63 — two of them mine), which
now carry an explicit status. It also supersedes duplicate ids by document order, so B39 and B41 —
each logged once as found and once as CORRECTED — report their last word.

Its success message is deliberately not "all clear":

```
✓ nothing open. 5 deferred entries remain by choice — name them rather than calling the log clear.
```

**Class → the same one as B66**, *a check whose failure path is never exercised*, now found three
times: in the calibration harness, in a `tolerance` that defaulted to `undefined`, and here in a
one-line grep I trusted enough to report from. The pattern in all three is a check that can only
ever return "fine". **Ask what would have to be true for this check to fail, then make that true
once and watch it.**

---

### B40 — the fix (2026-08-22), and what was already done

**Half of this was already built, and the log was stale.** `lib/ingest/source-text-cap.ts` exists:
`capSourceText` caps and reports, `truncationNotice` writes the sentence an admin can act on, and
`extractionOf` reads the stamp back off a document's metadata. The upload route already persists
`metadata.extraction { chars, truncated, originalChars, capChars }`. Checking before rewriting saved
building it twice — the log said "not fixed here" and the code had moved on.

**What was actually missing was the reading end.** `extractionOf` and `truncationNotice` had **zero
callers**. The truncation was recorded faithfully and consulted by nothing — a fact filed where
nobody looks, which is B53's defect (*a metric with no reader*) rather than B40's original one.
And `lib/ingest/ingest-topic-files.ts` still called `capSourceText(...).text` at both sites,
discarding the stamp entirely, so topic files truncated with no record at all.

**Where it surfaces now: the provenance audit.** That is the one place a curator looks before
landing a matrix, and it already carries the vocabulary — `blocker` for provably unfounded,
`warning` for "a person must look". A partly-read document is a **warning**, deliberately:

```
N source document(s) were only partly read — worst: DoW-2026-SBIR-BAA.pdf at 500,000 of
1,013,000 characters (51% not examined). Any field showing a system default may be stated on a
page that was never read, so "not stated in this solicitation" is unverified for this matrix.
```

Not a blocker, because the values that WERE read still stand and the model already lands defaults
wearing a red "unverified" badge; what was missing is the reason they are more than usually suspect.
The blocker case is already covered — if nothing at all was read, `nothingRead` fires regardless of
why. The finding names the **worst** document and states the **share** lost, not just the count:
"we read 500,000 characters" sounds thorough, "51% was not examined" does not.

**Wired at both audit callers** — `stage-skeleton` (every Ingest Assist run) and `assess-ingest` (the
admin's readiness probe) — by selecting `metadata` alongside the document row and mapping it through
`extractionOf`. An absent stamp reads as **unknown**, never as "complete": inventing completeness for
rows ingested before the stamp existed would be the same defect pointing the other way.

**Guarded** by `__tests__/provenance-truncation.test.ts` (10 cases): it fires on a cut document,
names the document and the share, reports the worst of several, stays silent on a whole document,
stays silent on an absent stamp, and does not disturb the findings that were already there.

**Still a product decision, deliberately left to the owner:** whether to raise the 500,000-character
cap, chunk the extraction, or leave it. This change does not touch the number. It makes the
consequence of hitting it visible, which is the part that is required in every one of those worlds —
if the cap is raised, a longer document can still hit it, and the audit will still say so.

**One test-writing note worth keeping.** The first version of the regression matched findings on the
prose `/system default/i`, and the new truncation message legitimately contains that phrase, so it
counted as a second per-field finding and the test failed. Matching on the finding's SHAPE — a
per-field finding carries `field`, a whole-matrix one does not — is the stable predicate.

---

### B34 — the fix (2026-08-22)

**The whole chain was empty, and every link reported success.** Confirmed live before changing
anything:

```
master_templates=0   template_bridge=0   tenant_template_cards=0
```

`TEMPLATE_CATALOG` (39 entries) lives in TypeScript → `syncTemplateStableFromCatalog` →
`master_templates` → `template_bridge` → `backfillTenantTemplates` → `tenant_template_cards`. The
sync was reachable **only** from an admin clicking `/admin/template-stable/sync`, so on a database
nobody had clicked it on the first link never filled — and `backfillTenantTemplates` read zero bridge
heads, applied zero, returned `0`, and all four callers' best-effort `try/catch` treated that as
success.

**Not fixed by seeding the catalog in SQL.** That would copy 39 canvas bodies out of TypeScript into
a migration and let the two drift — the same duplicated-truth problem, one layer down.

**Fixed by treating an empty stable as an anomaly rather than an answer.** When
`backfillTenantTemplates` finds no bridge heads it checks whether `master_templates` is empty, and if
so seeds from the catalog before backfilling. The catalog stays the single source of truth; the sync
is idempotent (it change-detects against the stored body); `master_templates_template_key_key` makes
a concurrent double-run safe; and the check short-circuits forever once master is populated, so the
cost is one pass on the first provision that needs it.

**It repairs backwards, too.** `publishAndFanOutTemplate` inside the sync fans to tenants that
already exist, so the tenants provisioned while the stable was empty are fixed as well — proven live
on the sandbox:

```
[tpl-bridge] template stable is EMPTY — seeding from TEMPLATE_CATALOG before backfill (B34)
[tpl-bridge] stable seeded: 39/39 templates
backfillTenantTemplates → 39 applied

BEFORE   master=  0  bridge=  0  tenant_cards=0
AFTER    master= 39  bridge= 39  tenant_cards=273     (7 tenants × 39, every one of them)
```

**And the silence is closed.** A backfill that applies zero templates now says so, naming the tenant
and how many heads were available. "Found nothing and succeeded" is the shape this defect hid inside
for its whole life, and it is the shape worth refusing on principle — the same refusal as B53's
always-zero tile and B67's grep that could only ever pass.

Guarded by `__tests__/template-stable-selfheal.test.ts` (5 cases) on the contract the self-heal
seeds: the catalog is non-empty, every entry resolves to a real body (an entry without one seeds as
an `error` and silently shortens the shelf), keys are unique against the DB's unique index, formats
are renderable, and every entry carries the title and category a customer sees.

---

### B35 — verified fixed (2026-08-22), and the live reading that nearly misled me

Both halves are closed, and both were closed by earlier work this arc rather than by anything new
here — checked before writing code, the same discipline that saved rebuilding B40.

**The library arrives.** The "keep + copy" eager materialisation (`copyStarterSetToTenant`, task
KEEP+COPY) runs at tenant creation. Live, on tenants my own drive created today: **303 atoms each**.

**The fallback offer no longer dies on RLS.** `offerStarterSet` now wraps its work in
`runInTenant(tenantId, …)` — not `enterTenant` — with a comment naming the precise failure: it runs
from ADMIN provisioning routes that hold no tenant context of their own, so the idempotency SELECT
saw only `tenant_id IS NULL` rows and the INSERT failed the `tenant_id = app.tenant_id OR
tenant_id IS NULL` with-check, which the caller's best-effort `try/catch` swallowed. Proven by
calling it directly from an admin context holding no tenant context — the exact shape that failed:

```
result → {"offered":true,"alreadyOffered":false,"taskId":"bf720071-…"}
task in DB → open · "Add the proposal starter set to your library"
```

**The reading that nearly misled me.** Querying tenants showed `starter_offer_task=NONE` alongside
`atoms=303`, which looks like the offer still failing. It is the opposite: the offer is a FALLBACK,
raised only `if (starterCopied === 0)`. The library arrived, so no offer is owed — absence is the
correct state, and treating it as a symptom would have "fixed" a working design. The check that
settles it is calling the fallback path directly rather than inferring from what a healthy tenant
lacks.

## B68 — FIXED. An image with nowhere to load from was measured as a full-size figure
**Found by** running the mold sweep (`scripts/sweep-mold-quality.mts`, #152) for the first time since
the ruler was calibrated. B64–B66 calibrated against synthetic documents and four hand-authored
proposals, where the ruler is exact. It had never been pointed at the 39 **templates**, and there it
over-counted on 13 of them — by up to 4 pages.

**Not caused by the calibration work, but made marginally worse by it.** A/B against the pre-B64
file on the worst mold: `doe-sttr-phase1-technical` estimated **10** before and **11** after, against
**7** printed. The first A/B I ran said "identical, not mine" — that test was invalid, because
`canvas-document.ts` was already committed, so `git stash` had nothing to revert and both arms ran
the same code. Checking that the file actually changed is what caught it.

**Cause.** `canvas-html::renderImage` emits an `<img>` only when the key is a `data:` URI (the export
path inlines real storage keys first). With nothing to inline it emits a small dashed
`<div style="border:1px dashed;padding:24px">alt</div>` instead, in which the declared height acts as
a `max-height` **cap** rather than a height. The estimator read `content.width/height` regardless.
The molds carry images with an **empty** `storage_key` declared at up to 900×520:

```
image nodes: 5
  key=""  data:URI=false  declared 936x90
  key=""  data:URI=false  declared 900x520
  key=""  data:URI=false  declared 900x520
  key=""  data:URI=false  declared 900x460
```

Five figures at ~390pt of phantom height ≈ the three pages of pre-existing over-count.

**Fix, scoped to the provably-unresolvable case.** An image whose `storage_key` is empty is measured
as the placeholder box the renderer will actually draw. An image with a REAL key is untouched: it
resolves through `inlineImageDataUris` at export and does take its declared size, which is why real
authored proposals measured exactly right and must keep doing so.

**Result — and it is partial, which is why this entry is not FIXED:**

```
                    before   after
molds clean          38/39   39/39
needing a look           1       0     (the investment-pitch-deck violation cleared too)
estimator disagreed     13       9
```

**The first box was guessed, and came out at roughly double.** The fix above read the padding, the
border and the caption off the stylesheet by hand and modelled ~86pt where Chromium lays out 63.6pt.
Two mistakes, both invisible to a reading: the `<figure>`'s 12px margins were charged twice when they
COLLAPSE against the neighbouring paragraph's, and a caption line was charged to a box with no
caption. `scripts/measure-image-placeholder.mts` asks the renderer instead — lay the figures out,
report where each one starts — and the top-to-top delta IS the height to charge:

```
empty key, no declared size        inner   70.5px   advance  61.85pt   (in a run of figures)
empty key, declared 900x520        inner   70.5px   advance  61.85pt
empty key, declared 320x60         inner   60.0px   advance  54.00pt   (max-height caps it)
empty key + caption                figure  89.8px   advance  76.37pt
empty key, long alt (wraps)        inner  111.4px   advance  92.55pt
marginal cost interleaved with prose                        63.60pt
```

Model now: `36pt padding + 1.5pt border + lines × bodyLineH`, capped by the declared height, plus
14.5pt for a caption and an 11pt collapsed figure gap — 63.86pt against 63.76pt measured on the same
document. Five placeholder cases are now in the calibration harness, which had none.

**The remaining molds were a different bug — see B69 and B70.** Diagnosing them needed a sharper
instrument than "estimates 7, prints 5", so `scripts/diagnose-mold-ruler.mts` and the
`nodeHeightsPt` read-out were built: `--nodes` charges every node against the height Chromium gives
that same node in place, `--segments` does it per page-break segment, `--pages` replays the ruler's
own placement. Amplifying one node type ×240 — the method used before — LIES about anything with a
vertical margin, because a run of forty headings collapses margins a real document does not; it
reported headings +29% and images +84% where both were exact.

**Result after B69 and B70 as well:**

```
                    before   after B68   after B69   after B70
molds clean          38/39        39/39       39/39       39/39
estimator disagreed     13            9           4           4
```

**Class → the calibration set has to include the artifacts the product ships,** not only the ones a
harness invents and the ones a person happened to author. Templates are content the customer sees on
day one and they were the least-measured content in the system.

## B69 — FIXED. A running header was charged twice, taking 11% off every page
**Found by** chasing B68's remaining nine molds with a per-node instrument instead of a page count.
The per-node comparison came back almost perfect — `nsf-sbir-phase1-project-description` modelled its
whole 56-node body to within **2pt** of Chromium — and the ruler still said 8 pages against a printed
6. That rules out the heights and points at the FRAME, which nothing had ever measured.

**Cause.** `flowMetrics` computed the content box as

```ts
usableH = height − margins.top − margins.bottom − (header?.height ?? 0) − (footer?.height ?? 0)
```

but `exportToPdf` passes the canvas margins straight to `page.pdf({ margin })` and sets
`displayHeaderFooter`, and Chromium draws those templates **inside** the margin box. The content box
is `height − top − bottom`, full stop. The editor's page frame agrees — `canvas-renderer` positions
the header and footer absolutely in the margin bands. So a document with both lost 72pt of a 648pt
page, on **every page**: 11%.

Every agency mold the product ships carries a running header and a page-numbered footer.

**Why nothing caught it.** All 28 calibration cases used `CANVAS_PRESETS.letter_standard`, the one
preset that declares `header: null, footer: null`. The furniture path had no measurement at all. Two
more cases in that file were worse than useless: `frame · narrow margins` and `frame · 12pt body`
passed `{ margin_in: 0.5 }` and `{ body_pt: 12 }`, which are not fields of `CanvasRules` (`margins`
and `font_default` are), so the override landed as an ignored property and both cases silently re-ran
the plain two-page prose case. A frame variation that does not vary the frame is this harness's own
defect class, one level up.

**Measured, before the fix:**

```
prose · ~4 pages                                     ruler 3   printed 3
frame · running header + footer, 4 pages of prose    ruler 4   printed 3   ← same prose
```

**Fix.** `usableH = height − margins.top − margins.bottom`. Three furniture cases added to the
calibration harness (header+footer over prose, header+footer over a mixed volume, footer alone), the
two dud frame cases repaired to vary what they claim to vary, and a unit test
(`__tests__/page-ruler-frame.test.ts`) that pins furniture as free and margins as not — the second
half guarding against over-correcting.

**Result:** mold disagreements 9 → 4, every one of them still in the over-counting direction.

**Class → a default that switches off the thing under test.** The preset every case reached for had
the feature nulled out, so the harness grew to 28 cases without once exercising a path the product
uses everywhere. Same shape as B66's tolerance defaulting at the comparison, and as B67's grep.

## B70 — FIXED. A table column was starved below the width of the word inside it
**Found by** `diagnose-mold-ruler.mts --nodes` on the NSF Project Description mold: one table node
modelled at **148pt** against **105.9pt** printed, a 40% over-count on a single node, which was most
of the 78pt that pushed one page-break segment onto a second page.

**Cause.** B64 replaced equal-width columns with a share proportional to each column's longest cell —
right in direction, wrong at the narrow end. The milestone table's first column holds `Task 1`; its
proportional share came to **5.8 characters**, so the model wrapped a six-character cell onto a second
line, on every row.

CSS auto table layout does not work that way (CSS 2.1 §17.5.2): every column first gets its
**min-content** width — the longest unbreakable word — and only what is left over is shared out, in
proportion to how much more each column wants (`max-content − min-content`). When the max-contents
all fit, nothing wraps at all.

**Fix.** That algorithm, plus `Math.round` rather than `Math.floor` on the per-column character
allocation: flooring stacks a second conservatism on top of `CHAR_W`'s (0.45 against a measured ~0.42)
and stacks it once **per column**. The table above: 148pt → 124pt against 105.9pt printed.

**Result:** the milestone table 148pt → 124pt against 105.9pt printed. What was left of the
over-count then turned out to be two more defects — B71 and B72 — which the mold numbers alone
could never have separated.

## B71 — FIXED. Every line of prose was measured 11% short of the leading it renders at
**Found by** doing what should have been done before touching the ruler at all: measuring it
against REAL authored proposals. `scripts/verify-ruler-on-proposals.mts` runs the eight NILOC gold
volumes — full narratives, agency shape, page-numbered footers — through the product's own exporter.
Molds cannot answer this question; their content is brackets.

**The result was alarming and correct to be alarmed by:**

```
                        before this session   after B69   after B71
under-counted                    2 of 8         4 of 8       0 of 8
```

B69 did not create the under-count. It REMOVED THE MASK. Subtracting 72pt of furniture from every
page was hiding a defect that ran the other way, and correcting one exposed the other — which is
exactly why the safety property has to be measured on the artifacts that matter, not inferred from
"the direction of my change was conservative."

**Cause.** `canvas-html::canvasBaseCss` sets `line-height: Math.max(lineSpacing, 1.28)`, with its
own comment explaining why ("a solicitation that mandates single spaced means 1.0 of the LEADING a
typesetter would use"). `flowMetrics` computed `bodyLineH = fs * c.line_spacing` — the raw value.
On `letter_standard` (1.15) every line of prose was modelled at 13.8pt where the page draws 15.36pt.
11% short, on the most common node in a proposal.

Four node cases had already worked around it LOCALLY — lists, code_block, toc and the figure
placeholder each carry `Math.max(m.bodyLineH / fs, 1.28)`. The flow-text default, which is most of a
volume, had not. A local workaround repeated four times is the shape of a fact that belongs one
level up.

**Fix.** The floor moves into `flowMetrics`. The four local copies are now no-ops and stay as
documentation. All eight authored proposals land exact.

**Class → a constant duplicated at the call sites instead of computed once.** Nobody wrote the
model wrong; four people wrote the correction right, in four places, and the fifth caller never got
one.

## B72 — FIXED. The ruler charged margins the page collapses
**Found by** the same measurement: with B71 alone the prose was right and the page counts still
drifted, so the remaining error had to be BETWEEN nodes rather than inside them.

**Cause.** The ruler charged every node its full box plus margins, and did it inconsistently — a
heading carried its 20pt and 5pt, a figure carried both of its own, a paragraph carried nothing at
all (`p { margin: 0 0 7pt }` was simply absent from the model), a table carried neither. CSS
collapses adjacent vertical margins to the LARGER of the two, so:

```
                                real        old model
paragraph, then paragraph        7pt          0pt      (under)
paragraph, then <h2>            14pt         14pt      (right, by accident)
paragraph, then <figure>         9pt         18pt      (over)
```

**Fix.** `nodeMarginsPt(node)` gives every type its real `{top, bottom}` read off canvas-html, the
node heights become BOXES, and both stackers collapse: `max(previous.bottom, current.top)`. Two
rules that paged CSS applies and a first pass will miss — a margin at the TOP of a page is not spent
(charging it put `y` above zero before the first element, so `fitKeep` treated a table at the top of
a page as one to relocate, and a 2-page table landed on pages 2 and 3 of a 3-page document with
nothing on page 1), and a keep-together block's own outer margins collapse through it.

**And one more, found on the way:** a `chart` fell through to a flat `fs * 15.5` guess because chart
nodes carry no width or height — 201pt modelled against 256pt printed, a 21% UNDER-count. But
`renderChartSvg` fixes its own viewBox (480×300 for every plot type; 500 × (12 or 30 + 24/category +
26) for a gantt), so the height is computable. Measured with `scripts/probe-chart.mts` before being
written down.

**Where the ruler stands now**, across every instrument built for it:

```
authored proposals (8, NILOC)      8 exact, 0 under-counted
calibration cases (36)             36 agree, 4 within a stated ±1, 0 under-counted
deck cases (7)                     7 agree
templates (39)                     39 clean, 5 over-count by 1-2 pages, 0 under-counted
```

**The remaining five molds are one known cause, and it is not a constant to nudge.** `CHAR_W` is a
single number standing in for a font whose real average advance depends on the words. Measured on
40-line paragraphs so line quantisation cannot dominate (`scripts/measure-char-width.mts`, and the
first version of that script was wrong for exactly that reason — a 4-line sample reported the same
"4 lines" at 10pt and at 12pt, which cannot both be true):

```
lowercase technical prose   0.41      long compound words   0.44-0.47
numerals and identifiers    0.42      acronym-dense text    0.58
```

0.45 is the average of those, so it runs ~10% conservative on lowercase and ~20% optimistic on
capitals. Molds are bracket-heavy lowercase, which is the register where it is most conservative;
authored proposals mix all four, which is why they land exact. Closing the last five means
estimating the advance PER TEXT from its character mix — a real change with its own risks, done with
the harness watching, not a constant moved late in a session.

**Direction matters and is worth being explicit about:** over-counting cannot clear a bid the gate
should refuse. It is still wrong — it would tell someone their 6-page volume is 8 — but it is wrong
safely, and on molds, which are skeletons a customer fills.

**Class → correcting one error can expose another that was cancelling it.** Two of the three defects
here (B69 over, B71 under) were partly hiding each other, and the mold sweep — over-counts only —
could not see the under-count at all. Only measuring the real artifact could.

## B73 — FIXED. The ruler died on a canvas the exporter draws happily
**Found by** pointing the ruler at the artifacts that are actually IN THE DATABASE. B71's harness
(`verify-ruler-on-proposals.mts`) measures the eight NILOC volumes, which are markdown on disk
converted at run time. `scripts/verify-ruler-on-stored-artifacts.mts` measures the other thing —
real `proposal_artifacts` rows with their real sections, groups, furniture and stored images,
assembled by `assembleArtifactCanvas`, the SAME assembly the layout route and every export path
use. Three of twenty-two threw before they could be measured:

```
TVSF Round 45 · Narrative                 estimatePageCount THREW  ·  exportToPdf → 122,984 bytes
TVSF Round 45 · Willingness-to-License    estimatePageCount THREW  ·  exportToPdf →  11,432 bytes
TVSF Round 45 · ESP Support Letter        estimatePageCount THREW  ·  exportToPdf →  11,432 bytes
```

**Cause.** Those volumes carry a PARTIAL canvas spec — `{width, height, margins}` and no
`font_default`, no `line_spacing`, no `format` — and `flowMetrics` read `c.font_default.size`
directly: `TypeError: Cannot read properties of undefined (reading 'size')`. `paginate` guards a
MISSING canvas (`doc.canvas ?? CANVAS_PRESETS.letter_standard`); nothing guarded a partial one.

**The asymmetry is the defect.** `canvasBaseCss` defaults every field it reads
(`font_default ?? { family: 'Times New Roman', size: 12 }`), so the very same volume downloads as a
correct PDF — 122KB of it — while the page gauge (`canvas-renderer`), the layout route's "8 / 15
pages" panel, `computeSubmissionReadiness`, `document-furniture` and `volume-finish` all throw on
it. One half of the system tolerant of real data, the other half brittle, over the same rows.

**Fix.** `flowMetrics` fills a partial spec from `FRAME_DEFAULTS` — canvas-html's defaults,
deliberately, so the ruler measures the document the exporter will actually draw rather than
refusing to measure at all. Four unit cases in `__tests__/page-ruler-frame.test.ts`, including the
guard against over-correcting: a partial canvas that DOES declare wider margins must still change
the answer.

**Result:** 22 of 22 stored volumes now measure, and all 22 agree with the printed page exactly —
including the 9-page TVSF Narrative, the largest real document in the set.

**Re-checked on the CURRENT schema, because the first run was not.** That sandbox sat at migration
163 (see B74); after `migrate.mjs` brought it to 205 the corpus grew to **37** volumes — the DoW
2026 STTR and SBIR sets and the clean Ohio TVSF R45 build came with those migrations. On the full
37: **36 exact, 1 over by a page** (`TVSF-R45 · Proposal`, ruler 9 against 8 printed), **0
under-counts**. Diagnosed rather than assumed: per-node differencing puts the model within ~30pt of
Chromium on that document once the differencing method's own heading-margin artefact is removed, so
the extra page is atomic PLACEMENT, not height — the residual class B70 and B72 already name, and
the direction that cannot clear an over-length bid.

**Class → the fixture that never fails is the one built from a template.** Every canvas the
calibration harness, the unit tests and the mold sweep had ever seen was constructed by
`CANVAS_PRESETS`, which is complete by construction. Rows a customer's data actually contains are
not, and nothing in the test suite had ever handed the ruler one.

## B74 — FIXED. The tool the SOP tells you to run was reading 11% of the SQL
**Found by** following the SOP and watching it fail. CLAUDE.md says *"Check your file with
`node scripts/schema-check.mjs <file>`"* before running SQL. I did, on the script written for B73.
It answered:

```
schema-check: 1 file(s) · 0 reference(s) verified · 0 unresolved · 0 finding(s)

nothing contradicts the live schema.
```

The file contained `ORDER BY … pa.sort_index` — a column `proposal_artifacts` does not have. The
query threw on its first run, seconds later.

**Cause 1 — the tag can carry a type argument, and in this codebase it usually does.** The block
extractor matched `` sql` `` and `` sqlBypass` `` and nothing else. `sql<Array<{ id: string }>>\`…\``
— the form CLAUDE.md's own SOP writes — never matched, and neither did the `` tx` `` inside every
`sql.begin` transaction. Across `frontend/{lib,app,scripts}`: **767 of 2,174** SQL blocks were
visible, and **213 files containing SQL were skipped entirely** while being reported as clean.

**Cause 2 — silence was printed as a pass.** A file yielding zero blocks `continue`d, and the
summary still ended `nothing contradicts the live schema`. The header of that very file says *"It
reports what it checked, so a thin pass cannot be mistaken for a thorough one"* — the counts line
was there, the VERDICT line was not.

**And the fixed extractor immediately walked into the failure the file warns about.** Prose here is
full of backticks; a docblock reading "the legitimate `sqlBypass` uses" opens one, and `[^`]*` then
runs to the next backtick in the file, swallowing the comment and reporting English as a query.
Comments now come out BEFORE extraction, not inside each matched block (`//` is only a comment when
it is not `://`, so a URL survives).

**Fix, measured A/B on the same 683 files against the same schema:**

```
                    references verified   findings
old extractor (HEAD)          277            0
new extractor               2,560            0
```

9.2× the coverage and **zero new false positives** — the property that matters most for a tool whose
own header says a checker that cries wolf is worse than none. The verdict now reads
`nothing contradicts the live schema, across N verified reference(s)`, names every file it verified
nothing in, and refuses to call a zero-reference run a pass.

**Cause 3, found while fixing the others — the "live schema" was 42 migrations stale.** The first
full sweep produced 24 findings. Every one was phantom: the sandbox sat at migration 163 against a
repo at 205, so `curated_solicitations.build_complete` (mig 182), `opportunities.update_watch`
(mig 181) and the promo-code issuance set (mig 200) all read as missing. After
`node db/migrations/migrate.mjs` (41 applied), the same sweep returned **zero**. The tool now
compares `_migration_history` against `db/migrations/*.sql` and says so before it says anything
else — a wrong accusation is the one failure it cannot survive.

Both new failure paths were verified by mutation rather than assumed: restoring `pa.sort_index`
produces the finding, and deleting ledger rows makes the staleness banner fire and then stop.

**Class → a tool that reports coverage still has to be read.** The counts were printed all along:
`0 reference(s) verified` was right there above `nothing contradicts the live schema`. Nobody read
past the verdict, including me on the first run. Numbers that contradict the headline have to
change the headline.

### B73 addendum — reproduced as a live HTTP 500, then fixed on the same surface
The script-level throw understated it. Driven against the running app as
`kate.ulepic@foundation3dp.com` (tenant_admin, her own tenant's volume), a build made from the code
BEFORE the fix answers:

```
GET /api/portal/foundation/proposals/…/artifacts/…/layout → HTTP 500
   {"error":"Internal error","code":"INTERNAL_ERROR"}

server log:
   [artifacts/layout] unexpected error: TypeError: Cannot read properties of undefined (reading 'size')
```

So this was not "a harness could not measure it" — it was the page-budget panel returning a 500 to
a customer opening a volume they own, while the same volume downloaded as a correct PDF.

**After, same request, same user, a build carrying the fix:**

```
GET /api/portal/foundation/proposals/…/artifacts/…/layout → HTTP 200
  unit=pages  format=letter  totalPages=9  maxPages=null  over=false
  perSection: 15 section span(s); first {"title":"Abstract","startPage":1,"endPage":1,"pagesUsed":1}
```

`totalPages=9` is the number the stored-artifact harness measured for this volume (ruler 9, printed
9), so the harness, the exporter and the live HTTP surface now agree on the same document.

**Two false starts on the way, both worth writing down** — a `next-server` from an earlier session
still owned :3000, and `pkill -f "PORT=3001"` matched its own shell and died before killing
anything, so the first two drives were answered by the PRE-fix build and looked like the fix had
failed. Checking the serving process's start time against the build timestamp is what separated
"the fix does not work" from "you are talking to yesterday's server". Kill by PID.

## B75 — FIXED. Two compliance moats that would have passed having checked nothing
**Not a defect found in the product** — recorded because it is B74's second cause sitting in two
more places, and because one of the two guards backs a claim made to government customers.

`__tests__/event-contract.test.ts` and `__tests__/audit-coverage.test.ts` each scan the tree and
assert `violations === []`. Neither asserted that it had **found** anything. Move `app/api`, rename
the `emitEvent*` helpers, land a codemod that changes the call shape, or break `walk` — and both go
green having examined **zero** call sites, while the second one is asserting that no business write
in the product is unaudited.

Measured before touching either, because a floor set above the real number is its own bug:

```
event-contract    409 emitEvent* call sites in 195 files (274 with a literal namespace to validate)
                  409 is also the total in the tree — the regex misses none
audit-coverage    the walk reaches app/api and app/actions; floors set well under the live counts
```

So neither was under-reading the way `schema-check` was — the extraction is sound. What was missing
is the assertion that it ran at all. Each now carries a second case, *"actually READ / EXAMINED the
surface — silence is not a pass"*, with floors far under the current numbers so ordinary churn never
trips them and a collapse always does.

Both failure paths verified by mutation rather than assumed: pointing `SCAN_DIRS` at moved
directories, and pointing the route walk at a missing `app/api`, leave the original assertion
**passing** (nothing scanned, nothing violated) and fail the new one with the count in the message.

**Class → an emptiness check needs a non-emptiness check beside it.** `expect(violations).toEqual([])`
is only meaningful paired with evidence that the scan had something to violate. Three instances now
(B66's tolerance, B74's extractor, these two); worth looking for in any guard written as "collect
problems, assert none".

## B76 — FIXED. The glyph-advance constant was under-counting acronym-dense text by a fifth
**Opened to close the mold over-counts. It cannot, and finding that out is most of the value.**

B70 left one named residual: `CHAR_W = 0.45` is a single number standing in for a font whose
advance depends on the words. Swept against Chromium across the uppercase fraction, on 40-line
paragraphs at two font sizes, the relationship is near-perfectly linear and size-independent:

```
upper fraction   0.00   0.08   0.24   0.44   0.64   0.79   1.00
measured CHAR_W  0.414  0.427  0.455  0.488  0.526  0.552  0.588
```

So one constant at 0.45 was ~8% conservative on lowercase narrative and **~20% OPTIMISTIC on
acronym-dense text — an UNDER-count**, the direction that clears a volume the printer rejects. A
cover sheet or compliance matrix full of agency identifiers (SBIR · STTR · CDRL · DFARS · UEI ·
CAGE) is exactly that text, and nothing had ever measured it.

**Every wrap site now measures its own text** — flow prose, headings, list items, table columns,
toc labels and figure alt text each compute `glyphAdvance(theirOwnText)`. Monospace code is
untouched (it has its own constant).

**THE MARGIN IS THE FINDING.** A paragraph's realised advance sits above the 40-line average often
enough to matter. At the 3% headroom I first chose, the authored-proposal gate **failed** —
`cadence-technical` modelled 5 against 6 printed — and refused the change. That is the gate doing
exactly its job on the first real attempt to use it. 6% cleared all eight; 8% clears them with room
that is not tuned to those eight documents.

And at 8%, all-lowercase lands on **0.451 — the same value as the constant it replaces.** So:

```
                       before      after      effect
lowercase narrative     0.450      0.451      unchanged — the mold over-counts CANNOT close this way
acronym-dense text      0.450      0.597      +33% conservative — closes a real under-count
```

The headroom that keeps real prose safe is exactly what makes a bracket-heavy template read long.
The five mold over-counts are the price of that headroom, not a defect waiting to be fixed, and
B70's residual note should be read that way from here.

**Verified:** authored proposals 8/8 exact · stored artifacts 37, 36 exact + 1 over, 0 under ·
calibration 36 · decks 7 · molds 39 clean (5 over, unchanged) · tsc 0 · vitest 1670.

**Class → a measurement can refute the reason it was taken.** This was filed to remove an
over-count and instead removed an under-count nobody had looked for, while proving the over-count
is structural. Reporting only the part that matched the plan would have been the easy write-up and
the wrong one.

## B77 — PARTLY FIXED, one half LOGGED. Start/end brackets: the phase patterning is now
guarded; the missing durations are not
**Found by** checking the `.start`/`.end` patterning against the LIVE event table rather than
against the static guard. The guard checks that no file has more `emitEventStart`s than
`emitEventEnd`s. The table can answer the questions that actually matter.

**What is sound** — 1,591 rows, sandbox at head 205: 252 start · 252 end · 1,087 single; **0** ends
without a parent, **0** ends whose parent is not a start, **0 orphan starts** (nothing began and
never closed), **0** double-closed starts, **0** singles polluted with bracket fields. The
correlation spine is intact.

**Defect 1 — `duration_ms` is never set by the pipeline.** `pipeline/src/events.py::emit_event`
takes no `duration_ms` parameter at all. The column exists; the frontend helper fills it. So:

```
frontend brackets  tool:invoke 30 ends median 71ms · package.export_started 8 @ 350ms  — all populated
pipeline brackets  tool:agent.invoked 154 ends · draft.completed 9 · ingest.run.end 3  — 166 of 252, none
```

The most expensive operations in the system — agent invocations — record that they finished and
never how long they took. **Not fixed here:** the parameter is one line, but making it mean anything
requires threading elapsed time through ~60 end-emitters across 29 files. Half of it (adding the
parameter, no callers passing it) would look like a fix and change nothing.

**Defect 2 — four types encode the phase in the NAME**, which the `phase` column already carries:
`finder:ingest.run.start`/`.end` and `finder:rfp.shredding.start`/`.end`. A consumer grouping by
`type` sees two operations where there is one. `tool:invoke` (bare action + phase column) is the
sanctioned shape. These predate it; renaming orphans historical rows and breaks
`lib/tools/ingest-list-recent-runs.ts`, so they are now ALLOWLISTED WITH THEIR COST in
`scripts/event_catalog.py`, and a new one fails.

**Defect 3, and the reason the other two hid — the catalog could not see positional emits.**
`self._emit_event(conn, "finder", "ingest.run.start", …)` passes namespace and type positionally;
the scanner read keyword arguments only, filed both as `<dynamic>`, and reported the convention
clean in the one place it was broken. Teaching it positional form moved the inventory from **273 to
295 distinct types — 22 were invisible** — and surfaced the second phase-in-name pair, which I did
not know existed when I started writing the first one up.

**Class → the guard and the data answer different questions.** "No file has an unclosed start" is a
proxy. "No operation began and never finished, and here is how long each took" is the question, and
only the table can answer it. Both new checks now live in docs/EVENT_CONTRACT.md §6b with their
numbers, so the next reader gets the measurement rather than the proxy.

## B78 — FIXED. The proposal workspace was the red error card for four stored sections, and
answered HTTP 200 while doing it
**Found by** the guide screenshot pass — driving `/portal/foundation/proposals/[the 30-section TVSF
build]` as a real `tenant_admin` and *looking at the picture*. The page returned 200. The picture
said **"Something went wrong — This page failed to load."**

`CanvasRenderer` reads `canvas.font_default.family` with no default. Four sections of that proposal
— *Abstract*, *Market Opportunity*, *Willingness-to-License Letter*, *ESP Support Letter* — carry a
stored canvas of `{width, height, margins}` and **no `font_default`**. `TypeError: Cannot read
properties of undefined (reading 'family')`, thrown in a client component, so:

- nothing reached the server log (`grep -c error` on the whole run: 0), and
- Next served the error boundary with **status 200**, so the capture harness printed `✓ 200`.

**This is B73's sibling.** B73 was the same stored shape killing the RULER, and the fix defaulted
inside `flowMetrics` — which fixed exactly one caller. Every other reader stayed brittle, over data
that the EXPORTERS have always handled (`canvasBaseCss` defaults every field it reads, which is why
the same volume downloaded as a correct PDF the whole time).

**Fix:** one exported `normalizeCanvas(raw)` in `canvas-document.ts` is now the single answer to
"what does a missing canvas field mean". `flowMetrics` uses it; `withCanvasDefaults` is expressed in
terms of it (and its fast path no longer waves through a header that has a template and no font —
that shape crashes `canvas.header.font.family`); `CanvasRenderer` normalizes at its boundary; the
sheet editor and the fluid document view normalize the doc they mount. 10 cases in
`__tests__/partial-canvas-tolerance.test.ts`, including one that asserts the defaults still equal
`CANVAS_PRESETS.letter_standard` — if they ever drift, the ruler starts measuring a document the
exporter will not draw, which is the whole defect class.

**Class → a status code is not evidence that a page rendered.** Three harnesses in this repo gate on
`resp.status() < 400`. For a client-side throw, that check is structurally incapable of failing. The
capture harness now reads the rendered text for every error surface the product can show *and*
collects `pageerror`/console errors, which is how B79 was found ten minutes later.

## B79 — FIXED. `/admin/events` failed hydration on every load, because the clock ticked between
the server and the client
**Found by** the widened check from B78 — the very next run reported
`client threw — Minified React error #418` on `/admin/events`, a page that had been passing.

`relativeTime(iso)` read `Date.now()` **during render**. On a server-rendered page that makes every
timestamp cell a function of *when it rendered*: the server wrote `2s ago`, the client hydrated a
beat later and computed `4s ago`, the text did not match, and React #418 fired. #418 does not
degrade one cell — it fails hydration for the subtree, so the Event Stream dropped to its error
boundary. Status: 200, throughout.

**Fix:** `now` is `null` until mounted, so the first paint on BOTH sides is the deterministic UTC
timestamp; the relative form appears on the next tick and re-ticks every 30s so an open tab does not
go stale.

**Not claimed as swept.** A grep found ~15 other client components reading `Date.now()`/`new Date()`
where a render-time read is *possible*; most compute inside a memo or a handler and are fine, and I
have not proved which. Rather than eyeball them, `scripts/verify-surfaces.mjs` now drives **every**
`page.tsx` under `app/admin` and `app/portal/[tenantSlug]` as the right actor and fails on a
boundary or a client throw — enumerated from the tree, not from a list, and routes it cannot address
are **reported, not skipped**. The instrument answers the question; the grep only raised it.

## B80 — FIXED. The cockpit told a customer they had 6 active builds when they had 8
**Found by** `scripts/verify-ui-vs-db.mjs`, written for exactly this class and finding it on the
first run: `ui=6 db=8`.

The tenant dashboard reads its build list with `LIMIT 6` — correct, it shows six cards. The cockpit
summary sentence then counted **that array**: `${proposals.length} active build${…}`. Under six the
capped length and the true count agree, so the line was right for every tenant in the fixture and
wrong for every tenant who grows past the cap. It never errors, never logs, and reads as a fact.

**Fix:** the page issues a second, uncapped `COUNT(*)` (`activeBuildCount`) and passes it as
`counts.builds`; the sentence uses that. The list stays capped — showing six cards is a display
decision, and it is fine, *provided the page does not present the cap as a total.*

**Class → three green lenses can all miss a wrong number.** `verify-surfaces` proves a page renders;
a page renders a wrong number perfectly. `verify-api-contract` proves the envelope SHAPE; the shape
says nothing about the value. `verify-db-crud` proves a write LANDS; it never looks at what the
customer is shown afterwards. Nothing compared *rendered* against *stored* until now. The new lens
does, and its rule is that the expectation must be the page's **own query, copied from its source** —
earlier in this same audit I predicted a rendered count from a plausible-looking `SELECT count(*)`
and was wrong, because the page filtered differently. Guessing the predicate manufactures confident,
wrong findings.

Regression coverage is real, not notional: the check **creates scratch proposals to push the tenant
past the cap**, because under it the correct and incorrect implementations agree and the test proves
nothing. Verified failing before the fix and passing after, on the same build.

## B81 — FIXED (harness). The surface sweep bound its ids from a database the app was not serving
**Found by** chasing a "new broken surface" that kept changing identity between runs.

`scripts/verify-surfaces.mjs` still defaulted to `postgresql://claude@127.0.0.1:5433/govtech_intel`
long after the sandbox moved to `:5432` — while the two newer lenses defaulted to `:5432`. So every
dynamic route was addressed with an id bound from **a different database than the server was
reading**. Ids that happened to exist in both rendered 200 and the sweep reported "clean"; a
`:5433`-only id 404'd and looked like a page that had just broken. Static routes were unaffected,
which is precisely why it survived several runs and why my earlier "79/79 clean on the new cluster"
was only sound for the static half.

**Fix:** the default now matches its siblings, and both sweeps **echo the DSN they are binding
from** at startup — an audit that will not say what it measured cannot be trusted. Separately, every
`LIMIT 1` binding gained an explicit tiebreaker: the six seeded `source_profiles` share one
`created_at`, so `ORDER BY created_at DESC LIMIT 1` returned a different row run to run. A
verification tool whose results change while the code does not cannot be used to decide anything.

Re-run after both fixes: **79 driven · 79 clean · 0 broken**, ids bound from the served database.

## B82 · `Date.now()` during render, twice more — a class, not two incidents

Found by running `verify-surfaces` as a BACKWARD review after the scope programme, not by testing
the new work. Neither surface was touched this cycle.

**`/portal/[t]/processes`** and **`/admin/system-state`** each read `Date.now()` during render in a
`'use client'` component. The server writes "2s ago", the client hydrates a beat later and computes
"4s ago", the text does not match, and React throws **#418** — which does not degrade one cell, it
fails hydration for the subtree and takes the page to its error boundary. Both routes answered
**HTTP 200 the entire time**. This is the third and fourth occurrence of the class first recorded as
B79 on `/admin/events`.

A second defect surfaced while fixing the first: `relativeTime(iso: string)` was DECLARED string and
handed a `Date` at runtime — postgres.js returns a Date for a timestamptz and the server component
passed it straight through. `iso.slice(...)` threw, taking the page down again (React #419, this
time a genuinely failed server render). The mirror image of the `sql<typeof rows>` trap in
CLAUDE.md: a declared type trusted over the runtime value.

**The fix is now a component, not a helper**: `components/ui/time-ago.tsx` exports `<TimeAgo>`,
`<Elapsed>`, `useClientNow()` and the two pure formatters. `now` is null until mounted, so the first
paint — server and client alike — is a deterministic UTC timestamp and the relative form appears on
the next tick. A component rather than a helper because the call sites are scattered across
sub-components, and threading a `now` prop through every one is how the next site gets forgotten.

**Still carrying the pattern** (unflagged by the lens today, so unproven either way — each is a
`'use client'` component reading `Date.now()` during render):

```
app/admin/process/process-monitor-client.tsx:99,112
app/admin/processes/admin-processes-client.tsx:41
app/admin/workflows/workflow-monitor-client.tsx:37,53
app/portal/[tenantSlug]/activity/activity-stream-client.tsx:89
```

They are one `<TimeAgo>` substitution each. `app/**/page.tsx` reads are server-only and fine.

**The lesson is about the instrument, not the bug.** A harness gating on `resp.status() < 400` is
structurally incapable of seeing any of this, and two of these surfaces had shipped and survived
every prior sweep. The rule in CLAUDE.md — *run the four lenses on BACKWARD review too* — is what
found them.

## B83 · A collaborator could read the tenant library and write into the cost volume

**Mine, this cycle.** `POST …/sections/[s]/assemble` (Phase D) shipped with `verifyProposalAccess`
as its only check. That function answers *"can this actor reach this proposal"* — and an accepted
collaborator can, by design. It is not a library check and not a section check, and was never meant
to be one.

Measured live by `scripts/verify-collaborator-blast-radius.mjs` before the fix: a `partner_user`
subcontractor granted ONE narrative section POSTed at the **cost volume** and got **200**, with
**12 tenant library atom ids** in the response and a real `canvas_versions` row written.

Two gates close it, and **neither implies the other**:

1. **ROLE** — `hasRoleAtLeast(role, 'tenant_user')`. The tenant library is team-only, matching every
   other library route (`library/atoms`, `library/canvas`, `library/past-proposals` all gate there).
   `partner_user` is rank 20, `tenant_user` is 40.
2. **SECTION** — edit access to *this* section, via `resolveUserAccess().editableSections`. Role rank
   alone is not enough: a `tenant_user` who is only a per-proposal contributor holds edit rights on
   some sections and not others.

**Order is part of the contract.** Both run before `selectForSection`, which is the call that reads
the library. A check that runs after the read has already leaked; it only stops the write.
`__tests__/assemble-route-gates.test.ts` pins presence AND ordering.

### What the audit found was already closed

The blast-radius harness inventories the whole negative space, not just the one hole. Everything
below was already refused, by role rank rather than by anything added here:

- **the library** — atoms, canvas, past-proposals, system-templates: all 403
- **whole-proposal reads** — package json/docx/zip, assembled document, preview, readiness,
  compliance, strategy, supporting-docs: all 403 (they gate at `tenant_user`)
- **the cost volume itself** — versions/comments-by-id/activity 403; sections list, unscoped
  comments and findings return 200 but provably without the section's content
- **other writes** — lock, assign, atomize-node: 403
- **the rendered pages** — library, documents, templates, proposal workspace: no leaked content

### Three probes that proved nothing until fixed

Recorded because "the door is locked" and "I knocked on the wrong door" look identical in a log:

- `library/canvas` probed with GET → **405**. The route has no GET at all.
- `sections/[s]/save` probed with POST → **405** (it is PUT), then with `{document}` → **400**
  (the field is `content`). Only the third attempt reached the authorization check — which refused.
- `sections/[s]/assign` probed with POST → **405** (it is PATCH).

The harness now treats 405 and 400 as **not tried**, and says so in the output.

### The follow-on decision — one predicate, not two

The obvious "hardening" after B83 is to also wire `collaborator_stage_access.artifact_types`, so a
grant that omits `cost` blocks the cost volume by artifact type as well. **Decided against, 2026-08-23.**

`artifact_types` stays **deliberately unwired**: it is `SELECT`ed in `lib/proposal-access.ts:200` and
carried in three component prop types, and it is read by no filter, no gate and no rendered element.
The gate is `collaborators.assigned_sections`, resolved through
`resolveUserAccess().{editable,commentable,viewable}Sections` — section level, full stop.

The reason is B83 itself. That bug was a *weaker* check being mistaken for the real one. Wiring a
second predicate re-creates the same shape: `artifact_types` carries no section ids,
`assigned_sections` carries no artifact types, and the two can disagree — at which point every future
reader has to work out which one is authoritative. The cost volume is reachable exactly when its
sections are in the grant. One predicate, one answer, one place to audit.

**Rule for anyone extending this:** a new surface returning proposal content gates on the three
section arrays. If you reach for `artifactTypes`, what you want is already in them.

---

## B84 · The Proposal Studio silently drafted in the house voice, not the tenant's

**Found by Track A (#148), by rebuilding the AI-gated inventory instead of re-reading its conclusions.**

`OnReviewPhaseRequestedDraft.plan_draft` and `OnReviewPhaseRequestedRefine.restyle` both declare
`"voice": "payload.voice"` in their `input_map`. `requestReviewPhase` — the ONE canonical emitter for
their trigger, `proposal:review_phase.requested` — never wrote that key. The engine resolved it to
`None` on every Studio run, and `section_drafter` fell back to its house register.

Proven with the engine's own resolver against a real stored payload, before any fix:

```
OnReviewPhaseRequestedDraft.plan_draft  -> {"proposal_id": …, "tenant_id": …, "voice": null, …}
OnReviewPhaseRequestedRefine.restyle    -> {"proposal_id": …, "tenant_id": …, "voice": null, …}
```

### Why nobody noticed, and why nobody could

A failed or degraded `AI_INVOKE` is a **safe skip by design** — the workflow engine must never
dead-end. So a broken input contract and a working one are indistinguishable from the workflow's own
point of view: same status, same completion, same logs. `validate()` rejects an `AI_INVOKE` whose
ACTION is unmapped, at boot. Nothing anywhere checked whether its INPUTS resolve.

### What made it customer-visible rather than dead wiring

The asymmetry. `requestFullDraft` DOES carry `voice`. So one proposal, one persisted
`proposals.voice`, two front doors:

- **full-draft button** → drafts in the tenant's voice
- **Proposal Studio** → drafts in the house voice

and the Studio is the surface V1-4 collapsed the three AI-draft entry points INTO. The tenant sets
the register, the designated front door ignores it, and the output looks plausible either way.

A second fact fell out of the same trace: **`proposals.voice` was write-only.** `requestFullDraft`
persists it on every request and *nothing anywhere read it back* — `section_drafter` takes voice from
the invocation context, never from the row. The fix gives the column its first reader.

### The fix

One statement, no extra round-trip: `requestReviewPhase`'s existing state UPDATE gains
`RETURNING voice`, and the payload carries it. The UPDATE was already inside a non-critical
try/catch, so a failure degrades to `voice: null` — exactly the pre-fix behaviour, never a lost
phase run. The key is emitted even when null, because the lens below reads key PRESENCE.

### The instrument this produced — and its limits, stated

`pipeline/scripts/check_ai_invoke_contract.py` is the standing lens: for all **29** AI-carrying
workflows and **65** `AI_INVOKE` steps, it compares every `payload.*` key a step reads against the
keys real emitters really wrote, read from `system_events` rather than regexed out of TypeScript.

Three things it reports, and the third is the point:

- **MISS** — a key read by a fired workflow that no payload has ever carried. This found B84.
- **WEAK** — present but null in every observed payload. Presence is all this can check.
- **UNCOVERED** — **15 workflows** whose trigger has never been emitted on this box. These are
  **unmeasured, not passing.** The fix is to fire them, not to trust them.

And its own honest failure mode, printed with every miss: the evidence is historical, so a miss has
two causes that look identical — the emitter never writes the key, or it writes it *now* and every
stored payload predates that. Each miss therefore prints when its trigger was last fired. **A MISS is
a lead, not a verdict.** B84 was confirmed by reading `requestReviewPhase` and resolving a stored
payload through `resolve_inputs` — not by trusting the lens output.

Sensitivity shown rather than assumed: injecting one bogus `payload.no_such_key` read into a single
step made it report that step by name and exit 1; removing it returned exit 0 on the same build.

### Proof

- `frontend/scripts/verify-studio-voice.mts` — real emitter → stored event → real Python resolver.
  Red: `payload.voice = undefined`. Green: `["technical","research"]` resolving on **both** steps.
  Drives as a real tenant user and restores every row it touched.
- `frontend/__tests__/studio-voice-payload.test.ts` — 5 cases, runs in CI where the lens cannot
  (no database, no event history). 4 of 5 fail on the unfixed code.

---

## B85 · Two copy-forward paths disagree about provenance — and one docstring asserted the impossible

**Found by the E2E sweep**, chasing what looked like a failing drive and turned out to be a real
inconsistency plus a false comment.

### The measurement

`drive-copy-starter` asserted that every grain copied from the system_starter foundation carries
`derived_from` lineage back to its source. It reported **0 of 31**. That is not a regression — the
path *cannot* produce those edges:

`copyFoundationToTenant` passes `parentAtomIds: [srcId]`, and `createAtom` accepts a parent **only
when it belongs to the same tenant as the child**:

```ts
const ownedParents = new Set(... WHERE tenant_id = ${tenantId} AND id = ANY(requestedParents));
for (const p of requestedParents) { if (!ownedParents.has(p)) continue; /* edge dropped */ }
```

The guard is deliberate and correct — `atom_lineage` has **no RLS**, so a caller forging a foreign
parent could inflate a victim atom's `child_count`. The source of a copy-inward is in another tenant
by definition, so every requested edge is discarded.

### The inconsistency

Measured on this box:

```
atom_lineage: 303 rows, ALL of relation 'derived_from'
              ALL cross-tenant: rfp-pipeline (house) → entrepreneurs-center
              cross_tenant_edges = 303 of 303
```

So the **eager system_starter copy-forward (task #71) does write house→tenant lineage**, while
`copyFoundationToTenant` cannot. Two paths that copy the same shelf into a tenant, two different
provenance outcomes. Whichever is right, they should not disagree.

`atom_lineage` having no RLS is what makes this worth a decision rather than a shrug: `lib/atoms.ts`
computes `child_count` as `SELECT count(*) FROM atom_lineage WHERE parent_atom_id = a.id` with **no
tenant predicate**, so a house atom's child count reflects copies made by other tenants.

### What was fixed here, and what was deliberately not

**Fixed — the docstring was false.** `copyFoundationToTenant` claimed each grain is "linked back to
its source via derived_from lineage (createAtom parentAtomIds)". It is not and cannot be. The
argument is still passed and silently dropped. Corrected in place, with the reason.

**Fixed — the drive asserted the impossible.** Its lineage check was permanently red for behaviour
that is CORRECT. Inverted into the property actually worth guarding: **no cross-tenant lineage edge
is created by copy-inward**, counted across every relation rather than just `derived_from` (a
narrower check would let a differently-labelled edge through). It now fails if someone relaxes the
guard — which is a real guardrail, where before it was a permanent false alarm.

**NOT fixed, on purpose.** Whether the eager copy-forward *should* write cross-tenant edges, and
whether `atom_lineage` should be RLS-forced, is a design decision about the copy-inward guardrail —
open task **#118 (no cross-tenant shared objects)**. Changing the isolation posture unilaterally
while chasing a red test is exactly how a guardrail gets quietly weakened. Evidence recorded; the
call belongs with #118.

### The open question, stated plainly

Is a `derived_from` edge from a tenant's atom to the house shelf a *cross-tenant shared object*
(forbidden) or *provenance on a source shelf* (fine)? CLAUDE.md says the house tenant IS correct for
copy-forward CONTENT — "a source shelf tenants copy from, not platform state" — which argues the 303
edges are intended. If so, `createAtom`'s guard needs an explicit house-shelf exemption, and
`copyFoundationToTenant` should record lineage like the other path. If not, the 303 edges need
removing and the eager path needs the same guard.

---

## B86 · The sandbox was serving as a SUPERUSER, so no RLS claim made against it meant anything

**The most consequential finding of the E2E sweep, and it was about the rig, not the product.**

CLAUDE.md is explicit: *"The app runs as `govtech_app` and the sandbox emulates production exactly
(serve as `govtech_app`, RLS on)."* It was not. Every drive, lens and sweep in this session ran
against a server started with `DATABASE_URL=postgresql://govtech:…`, and:

```
rolname       rolsuper   rolbypassrls
govtech       t          f          ← SUPERUSER: bypasses RLS regardless of rolbypassrls
govtech_app   f          f          ← the production role
```

**A superuser bypasses row-level security entirely.** So the database layer was never engaged. Every
isolation result reported before this point was an APP-LAYER result — real, but half the story, and
described as if it were the whole one.

### Why it stayed hidden

`govtech_app` is created `NOLOGIN` by migration (094/177); prod sets its password via env, not
schema. On this box it therefore had no usable password, so the natural thing to hand a script was
the owner URL — which works, silently, for everything except the one property being tested.

Nothing failed. That is the point: RLS bypassed looks exactly like RLS satisfied, from every angle
except a cross-tenant read that should have returned nothing and didn't.

### Measured both ways, after `ALTER ROLE govtech_app LOGIN PASSWORD …`

Context set to immobileyes, counting **foundation's** rows:

| table | `govtech_app` | `govtech` (superuser) |
|---|---|---|
| tenant_spotlight_buckets | **0** | 5 |
| tenant_bucket_scores | **0** | 50 |
| tenant_opportunity_cards | **0** | 10 |
| proposals | **0** | 5 |
| library_atoms | **0** | 64 |
| *own* cards / proposals | **4 / 1** | 4 / 1 |

**RLS works.** 45 policies, 21 force-RLS tables, cross-tenant reads return nothing and own-tenant
reads still return rows — not a deny-all. The isolation posture is real; it simply had not been
exercised here.

### Re-verified under the production posture

Rig restarted as `govtech_app` (owner reserved for `sqlBypass`), then everything re-run:

- `verify-surfaces` **80/80 clean, 0 broken** · `verify-api-contract` clean ·
  `verify-db-crud` clean, fixture restored · `verify-ui-vs-db` clean
- all four RLS drives **pass** — and now they mean it

### The harness-side consequence, which is a real design point

Under RLS, a harness that computes its expectations through the SAME scoped connection sees nothing
— no tenant context, so RLS correctly denies it — and concludes the fixture is empty. `drive-rls-app`
did exactly that on the first run: *"no tenant has any rows"*, against a database holding thousands.

Fixed with `harnessDbUrl()` in `scripts/lib/drive-actor.mjs`: **the app under test stays scoped; only
the harness's own arithmetic uses the owner.** Working out what a tenant *should* see is precisely
the "legitimate cross-tenant read" the owner role exists for.

### What this changes for launch

Nothing about the product — it behaves correctly under both roles. Everything about the *evidence*:
until this run, no sandbox result distinguished "RLS is enforcing" from "RLS is not engaged".

**Two things to carry forward.** `govtech_app` needs a password set on any box meant to emulate prod
(`ALTER ROLE govtech_app LOGIN PASSWORD …`), and the sandbox bring-up should serve as that role by
default with `DATABASE_URL_OWNER` reserved for bypass — otherwise this reverts silently the next time
someone starts a server by hand.

---

## B87 · Cross-tenant references purged, and the rule made unbreakable

**Owner's ruling, verbatim:** *"Nothing reads or writes cross tenant. Ever. That's what the bridges
are for. Messaging back and forth and inward copy of data only (opps copied into tenants, templates
copied into tenants, etc.)"*

That settles the open question in B85 in one direction: a `derived_from` edge from one tenant's atom
to another tenant's atom is a **shared object**, not provenance, and it is forbidden.

### The sweep — from the catalog, not from a list

Two shapes, and the second is the one that hides:

- **direct** — a table with `tenant_id` whose FK points at another tenant-scoped row. **23 paths, all
  clean.**
- **joined** — a link table carrying **no** `tenant_id` of its own, joining two tenant-scoped rows.
  Invisible to any check that looks for a tenant column, because it has none. This is where the
  violation lived.

One violation class, and only one:

```
atom_lineage   303 of 303 edges tenant↔tenant   (rfp-pipeline → entrepreneurs-center)
atom_members     0 of 600
atom_tags        0 of 6015
```

### Residue, not a live hole

All 303 were written in a single minute on 2026-08-12 alongside their child atoms — the KEEP+COPY
starter copy-forward (task #71), *before* `createAtom` grew its same-tenant parent guard. The current
code cannot reproduce them: a live copy driven today produced **0 cross-tenant edges across 31
copied grains**. The app layer was already correct; what was missing was enforcement somewhere app
code cannot route around, plus the residue itself, which a fresh reader would reasonably take for
intended design.

### Migration 208

Deletes the 303, then installs a **trigger** on `atom_lineage`. A trigger rather than a CHECK (which
cannot see the two parent rows) or RLS (which scopes reads but still lets a bypassing path write —
and every one of the 303 came from exactly such a path).

Proven both ways: a cross-tenant INSERT is refused **even as superuser**, with a message naming both
tenants and pointing at inward copy; a same-tenant INSERT still succeeds.

### The standing sweep

`frontend/scripts/check-tenant-isolation-invariant.mjs` walks the catalog on every run, so a table
added by a future migration is covered without anyone remembering to add it.

**Two exclusions, both deliberate, both stated in the output** — an unexplained exclusion is how a
check quietly stops meaning anything:

1. **Platform scope is not a violation.** `tenant_id IS NULL` is the platform plane, the same
   distinction `tenant_isolation_select` draws with its `OR (tenant_id IS NULL)` arm. A tenant
   document pointing at a system template is the documented bridge. My first pass used
   `IS DISTINCT FROM` and reported that bridge as a violation — a phantom, corrected.
2. **A person is not tenant data.** A human legitimately spans tenants: multi-membership identity, an
   rfp_admin acting inside a tenant, an external collaborator on someone else's proposal (which is
   precisely what `proposal_collaborators.user_id` is). Counting actor references produced three
   guaranteed false findings per run. Membership and authority are enforced by `verifyTenantAccess`
   and `resolveUserAccess`, not by this sweep.

### Two bugs in my own instrument, caught before reporting

- It created a client with **no `toCamel`** and then read `r.childScoped` off a `child_scoped`
  column — always `undefined`, so all 23 direct paths silently took the join-table branch. The tell
  was in its own output: *"checked 0 direct FK path(s)"*. The camelCase trap CLAUDE.md documents,
  inverted.
- A SQL comment containing backticks **terminated the JS template literal** holding the query.

Current state: **✓ no cross-tenant references anywhere in the schema.**

---

## B88 · The sheet editor doubled the first character of every cell

**Found by:** driving the grid for the customer-guide captures. The screenshot came out reading
`DDirect labor`, `FFringe @ 31.4%`, `1184500`, `661%`.

**Why.** `handleCellKeyDown` (`components/canvas/sheet-editor.tsx`) opens the cell editor on the
first printable key and seeds it with that character — `startEdit(row, col, e.key)`. Every sibling
branch in that handler calls `e.preventDefault()`; **this one did not**. A keydown is a discrete
event, so React flushes the state update synchronously, the `<input autoFocus>` is mounted and
focused before the browser applies that same keystroke's default text insertion, and the character
lands a second time — now inside the input.

So typing `184500` into an empty cell produced `1184500`. Not a Playwright artefact: the ordering is
the same for a human keystroke.

**Fix.** `e.preventDefault()` in the printable-character branch. The seed is already the keystroke;
the default insertion was pure duplication.

**Proof, red then green on the same build.** The pre-fix capture is the red evidence
(`DDirect labor`). The capture step's assertion was tightened from `text=Direct labor` — which a
substring match satisfies even when the cell reads `DDirect labor` — to
`span:text-is("Direct labor")`, which is exact. Post-fix the grid renders `Direct labor / 184500 /
61%` and the assertion passes. The values also **persist**: the first pass typed but never pressed
Save, so `tenant_documents.canvas` still held `nodes: []` while the screen looked right; the capture
now clicks Save and the row carries the table.

---

## B89 · The compliance panel printed a raw float, and lied about "all"

**Found by:** looking at the deck capture. The sidebar read `Margins  0.5555555555555556" all`.

**Why.** `canvas-sidebar.tsx` rendered `{doc.canvas.margins.left / 72}" all`. Margins are stored in
**points**; a 40pt slide margin divided by 72 is an IEEE double with no rounding. And `all` was a
second, quieter defect: the expression read only `margins.left` and asserted the other three matched.
The very document that exposed it has margins 40/40/48/40 — so the old read-out was wrong about the
bottom margin as well as ugly.

**Fix.** `marginLabel()`: points → inches at 2dp with trailing zeros trimmed, and `" all` only when
the four sides are genuinely equal, otherwise all four. The deck now reads
`0.56 · 0.56 · 0.67 · 0.56"`.

**Proof.** `__tests__/canvas-margin-label.test.ts`, three cases, each failing against the previous
expression (verified by running the old expression directly: it returns `0.5555555555555556" all`
for the slide preset and a false `0.75" all` for `letter_onepager`'s 54/54/48/54).

---

## B90 · A guide figure was an error page, and both harnesses scored it green

**The worst one in this batch, because it reached a customer-facing document.**

The admin guide's "A platform document in the canvas" figure was a red **"Document not found"**
page. It sat in `docs/assets/guides/admin/12b-admin-document-canvas.png` and in the guide text until
someone opened the PNG.

**Two independent failures had to line up.**

1. **The binding was wrong.** `/admin/documents/[documentId]` is backed by the **object-storage
   document index** (`reference/documents/_index.json`), not by `tenant_documents` — the two
   document routes merely name the segment the same thing. The capture pass handed it a table id.
   `verify-surfaces.mjs` had *already recorded this route as not-by-row, with that exact reason*; I
   read it and bound a table id anyway.
2. **The error matcher was incomplete, in two private copies.** `verify-surfaces.mjs` and
   `capture-guides.mjs` each carried their own list of "texts that mean this page is broken", and
   neither matched a bare `… not found`. Next serves a client-side error surface with **status 200**
   (B78), so the rendered text is the only evidence there is — and the list of texts had drifted
   between two files that must never disagree.

**Fix.**
- `scripts/lib/error-surface.mjs` — **one** definition, imported by both harnesses. Adds
  `\bnot found\b`, with the word boundary deliberately excluding empty states ("No templates
  found" is a working page; "Document not found" is not).
- `act()` in the capture pass now runs the same gate, so an interaction that navigates into a broken
  page fails instead of being saved by a `prove` selector that happens to match.
- The route is now declared **not addressable** on this fixture, printed in its own section of the
  report, and **not illustrated** in the guide — with the whole story recorded in the guide text so
  the next person does not re-add it.

**What the widened matcher immediately caught (B91, B92).**

---

## B91 · `verify-surfaces` was driving a cross-tenant id and calling the refusal a pass

Once the matcher recognised `not found`, the lens went red on
`/portal/foundation/library/foundation/{id}`.

**The product was right.** The page's own predicate is
`WHERE id = $1 AND tenant_id = $2 AND grain = 'foundation'`. The lens bound that segment with
`SELECT id FROM library_atoms ORDER BY id ASC LIMIT 1` — **no tenant filter, no grain filter** — and
on this fixture that is an **immobileyes primitive**, driven while signed in as foundation's
tenant_admin. The page correctly answered *"Not found — this item doesn't exist, or you don't have
access to it."*

So the lens was manufacturing a cross-tenant probe, getting the right answer, and scoring it a
rendered page. Two verification rules broken at once: **copy the predicate from the source**, and
**bind ids from the tenant the harness signs in as**.

**Fix.** The binding now joins `tenants` on the slug the lens logs in with, and requires
`grain = 'foundation'`. Foundation holds no such atom on this fixture, so the route is reported as
**not driven, with a reason that says why a substitute is not acceptable** — uncovered, not passing.

---

## B92 · `/admin/pipeline` threw a hydration error once — a lead, not a verdict

The same lens run surfaced `Minified React error #418` (hydration mismatch) on `/admin/pipeline`.

**Not reproduced.** 18 subsequent loads — 3 with the default browser timezone, 15 with the browser
pinned to `America/New_York` to force any timezone-derived divergence — produced **zero** page
errors. Recorded here as seen-once rather than diagnosed, because a cause I cannot demonstrate is a
guess.

**What was fixed anyway, on its own merits:** the schedule column rendered
`new Date(s.nextRunAt).toLocaleString('en-US', {…})` with **no `timeZone`**, so it formatted in
whatever zone the renderer happened to be in and printed no zone label — an operator reading
"Aug 24, 06:00" could not tell whose 06:00 that was. Cron schedules are defined in UTC, so it now
pins `timeZone: 'UTC'` and shows the label. That also removes one candidate cause of the throw.

`relativeTime()`'s `Date.now()` was left alone: this is a server component rendered per request, so
"3h ago" computed at request time is correct, and changing it on suspicion would be a change made to
a symptom nobody has observed.

---

## B93 · Every notification deep-link to a TENANT task dropped the recipient on a dashboard

**The customer-facing half of `/go` was broken, and the admin-facing half was fine — which is
exactly why nobody noticed.**

Nudges and notifications link people straight into work: `/go?task=<id>`. The page resolves which
company that task belongs to, checks membership, and either lands them there or raises a switch
gate. The resolution query ran on the **tenant-context-aware** `sql`.

**`tasks` has row-level security, and at `/go` there is no tenant context — by construction.**
Working out which tenant to enter is the entire job of that query, so it cannot be inside a tenant.
The row came back empty, `targetSlug` stayed null, and the page fell through to
`redirect('/portal')` — the dispatcher, which drops the person on a dashboard.

So a reviewer clicking "review this section" in their email landed on a dashboard, with no error and
nothing in the log.

### Why it survived

**PLATFORM-scope tasks are visible without a context** — `tenant_isolation_select` carries an
explicit `OR (tenant_id IS NULL)` arm. Every admin deep link therefore worked perfectly. Only the
tenant tasks — section review, draft, acknowledge, every workflow to-do, i.e. essentially all the
customer-facing ones — silently missed.

It also survived my own first hypothesis. I checked whether a task was readable without a context,
picked "the most recent completed task" to test with, got `rows=1`, and concluded RLS was not the
problem. That task was platform-scope. **Sampling one row of a table whose visibility depends on
the row is not a test of visibility.**

### Measured, not argued

Same session, same pinned user, one difference:

| | result |
|---|---|
| `/go?tenant=<slug>` | renders the gate — *"You're working in Scenario home …"* |
| `/go?task=<id>` | redirects to `/portal/…/dashboard` |

Both take the identical `here` branch, so the difference had to be the lookup. Then, at SQL level as
`govtech_app`:

| task | visible without a tenant context |
|---|---|
| tenant-scoped | **no** (empty) |
| tenant-scoped, context set | yes |
| platform-scope (`tenant_id IS NULL`) | yes |

### The fix

The target lookup in `app/go/page.tsx` now uses `sqlBypass`. This read **is** the platform plane
doing dispatch, which is the documented bypass case, and it grants nothing: authorisation is still
`getActiveMemberships` immediately below, unchanged — someone who is not a member of the resolved
tenant goes to the dispatcher exactly as before. All that is restored is the ability to *read which
tenant the link refers to*.

**Red → green on the same build.** Before: `/go?task=…` → `/portal/…/dashboard`. After: *"That task
is already complete — nothing to do. Here is your current queue."* Regression cover is
`drive-identity-deeplink.mts` CASE 4, which now passes as part of 20/20.

**Found by** rebuilding that drive onto the scenario factory. The old version pinned two tenants and
two accounts that no longer existed, so it had been failing for a fixture reason and its CASE 4 had
not actually exercised this path in a long time.

---

## B94 · One rate-limit bucket for the whole suite — so late drives measured nothing and reported it as failures

**Class** harness · false findings at scale. **Severity** the worst kind: confidently wrong.

`middleware.ts` limits `/api/auth/*` to **20 requests per 15 minutes**, keyed
`${clientIp}:/api/auth`, and `getClientIp` reads `x-real-ip` / `x-forwarded-for` with a fallback of
the literal string `'unknown'`. Nothing sits in front of the app in the sandbox, so **every browser
in every drive shared one bucket named `unknown`** — the whole 27-drive suite had a combined budget
of twenty auth calls per quarter hour. A login costs several; each `GET /api/auth/session` costs one.

What that produced was not a red suite, which would have been honest. Drives that ran early passed.
Drives that ran late got `429 RATE_LIMITED` on every session read. And `session()` did this:

```ts
const j = await r.json().catch(() => ({}));
return (j?.user ?? {});          // 429 and "signed out" are now the SAME VALUE
```

so a refusal became an empty session, and **sixteen assertions across `pin` and `identity-deeplink`
reported `role=undefined tenant=undefined` as product failures** on a box where the product was
fine. I made it dramatically worse just before finding it, by replacing fixed sleeps with a 400ms
poll — fifty reads per call, enough to exhaust the box's budget in a single assertion.

**Proof, in one command.** With the shared bucket spent:

```
$ curl -o /dev/null -w '%{http_code}\n' localhost:3000/api/auth/session
429
$ curl -w '%{http_code}\n' -H 'x-real-ip: 10.77.0.99' localhost:3000/api/auth/session
null
200
```

**Fixed** — not by raising the ceiling or disabling the limiter, which is the contract the system
HAS and which a real deployment enforces per client behind a proxy. Three parts:

1. `scripts/lib/client-ip.mts` — every browser context declares its own RFC1918 `x-real-ip`,
   namespaced by pid. A browser context is one person's browser; the limiter still enforces
   20-per-15-minutes, now per simulated person, which is what it was written to do.
2. `session()` **throws `CannotRun` on 429**. A drive that cannot read the session has measured
   nothing — CANNOT-RUN, not FAIL. A refused read may never again look like an empty one.
3. `settledSession()` polls 8 times at 1.5s instead of 50 times at 400ms. Poll count is a budget.

**Red → green, same box, same minute:** with the shared bucket still returning 429 to a bare
request, `drive-pin` went 14/14. Only the per-context address differed.

**The lesson is rule 2 again** — the instrument before the finding. A harness that cannot
distinguish "the system said no" from "the system said nothing" will eventually report the second
and mean the first, and it will do it with a straight face.

---

## B95 · A URL that dispatches is not a URL that landed

**Class** harness · six invented findings in one drive.

`/portal`, `/go` and `/api/enter` decide where someone belongs and then redirect. In the App Router
the browser's URL becomes `/portal` **first** and the redirect resolves a beat later. So this
predicate, which I wrote to replace a fixed sleep:

```ts
await page.waitForURL((u) => !u.pathname.startsWith('/login'));
```

is satisfied while the person is still in mid-air. `identity-deeplink`'s login returned at
`/portal`, the next assertion read "no company selector", the company was therefore never picked,
and all five cases after it measured an unpinned session — six failures, none of them observed.

**Fixed** with `waitForLanding()` in `scripts/lib/cross-company.mts`: leg 1 waits off `/login`
(generous — there is a password round-trip); leg 2 waits *through* the dispatchers, but only 8s,
because someone with no workspace legitimately **comes to rest** on `/portal`. `/select-company`
counts as a destination; the bare dispatchers do not.

Two assertions in `drive-pin` had been passing while reading the same mid-flight `/portal`. They now
read `/portal/<slug>/dashboard` and `/admin/dashboard` — same verdict, but now earned.

---

## B96 · "I clicked it" is not "it took" — and a postcondition that is already true tests nothing

**Class** harness · two distinct mistakes in the same six lines.

Each company on `/select-company` is its own `<form action={selectCompanyAction}>` — a React server
action. `pick()` returned `true` for *"I found a button and clicked it"*, so a click that landed
before the form was wired left the session exactly where it was while the drive asserted "picked
the host company". Every case afterwards then measured the wrong session.

Fixed by verifying the postcondition: the pick is a pick only once the SESSION says so, with one
retry and a loud line when the retry was needed — because a selector that ignores the first click
would be a product problem, not a harness inconvenience. (Across subsequent runs no retry has
fired, so the first click does take; the wait absorbs the race.)

Then the second mistake, in my own fix: I wrote the postcondition as `tenantSlug === slug`. An
**unpinned session already carries the person's home slug**, so `pickHome` was satisfied before it
clicked anything — the check passed, the pick never happened, and CASE 4 measured an unpinned
session. Picking a company IS pinning it. The postcondition is
`tenantSlug === slug && membershipPinned === true`.

A postcondition that holds before the action is not a check. It is a comment that costs a round
trip.

---

## B97 · 403 tracked files were unreadable on disk while git called the tree clean

**Class** environment · silent data loss, with a recovery recipe.

`drive-atomization` died on `readFileSync` with `EIO: i/o error, read`. The obvious readings — a
moved fixture, a permissions problem — were both wrong: the directory listed fine, sizes and mtimes
were intact, and `git status` reported the tree **clean**. Every file in that directory was simply
unreadable.

A sweep over the whole checkout found the blast radius: **403 of 3232 tracked files**, a contiguous
region of the sandbox overlay covering `docs/archive/`, `docs/manuals/img/`, `docs/user-guides/img/`,
both sample-proposal sets, and the guide screenshots. Files elsewhere read fine, so the disk was not
broadly failing.

What makes this worth an entry is the shape of the lie. Git compares by **stat** (size + mtime),
not by content, so an unreadable-but-correctly-sized file is indistinguishable from an intact one:
`git status` says clean, `git checkout` therefore rewrites nothing, and the corruption survives
every ordinary repair. Meanwhile a harness that touches one of these files reports `EIO` from deep
inside Node, which reads like a code bug.

**Recovery** — delete first, so git has something to restore:

```
git ls-files | while read f; do [ -f "$f" ] && head -c1 "$f" >/dev/null 2>&1 || echo "$f"; done > /tmp/eio
git status --porcelain -- $(tr '\n' ' ' < /tmp/eio)     # MUST be empty: nothing uncommitted is at risk
xargs -a /tmp/eio -d '\n' rm -f
xargs -a /tmp/eio -d '\n' -n 100 git checkout HEAD --
```

403 restored, 0 still unreadable, tree clean. Nothing was lost because everything was committed and
pushed — which is the actual lesson for an ephemeral box.

---

## B98 · Three drives depended on state they did not create, and each failed in a way that looked like the product

**Class** fixture rot. Same root cause, three different disguises — this is what "resolve, don't
pin" is for.

**`drive-shadow-tenant-admin`** pinned the tenant `acme-navy-systems`. It was rebuilt away, so
`SELECT id FROM tenants WHERE slug=…` returned no rows, and destructuring that threw
`Cannot read properties of undefined (reading 'id')` on line 37 — the whole drive dead before its
first real assertion. Now builds one customer company and one platform admin who by construction
holds no membership there, so "a genuine shadow" is a measurement rather than a property of
whichever seed was loaded. **9/9.**

**`drive-spine-t1-section-todo`** pinned four uuids — a company, a proposal commented "TVSF-R45
draft (18 unlocked sections)", one section, two named people. `listSectionAssignees` still returned
five editors, which made it look alive; everything after it failed, because the actor no longer had
manage-team access on a proposal that had moved on. **Ten assertions reported the section-ToDo spine
as broken.** Now builds a company, a real editor added through the **team route** (the membership has
to be one the product made — `listSectionAssignees` reads `user_memberships`, and a hand-inserted row
would test the harness's idea of a member), a build, and an outsider. The guard case got stronger in
the process: it used to assign to the all-zeros uuid, which tests "no such user"; it now assigns to a
real, active person who is simply not a member here, which is what the guard is for. **15/15.**

**`drive-atomization`** picked its two companies off a candidate list of demo tenants and signed in
with demo passwords. The subject company survived that; the second one could not, and never will:
the `.test` seed accounts are **deactivated and hash-invalidated on purpose** (mig 124, no committed
production credentials). So the isolation section — the half of that drive that matters most — died
on a 30-second `waitForURL` timeout, *after* the upload checks had already printed green. Note the
candidate resolution did check the account existed and was active, which it was; what it could not
check was that the password works. **A fixture that depends on a security measure holding still is a
fixture that will break the day the measure works.** Both companies are now built for the run and
disposed after. **All six sections green**, isolation included.

---

## B99 · Two checks whose meaning depended on the connection, and neither one looked

**Class** harness · a verdict the check could not earn. The mirror image of B86.

**`drive-starter-offer` check 3** called `listOpenTasksForActor` — which reads through the
CONTEXT-AWARE `sql` — from a bare script. A real request sets `app.tenant_id` before that call; a
script does not. Under the scoped role RLS correctly returned nothing, and the check reported *"the
starter offer never reached the queue"* — a product finding invented by skipping the one step the
server always takes. It had been passing only because the suite happened to be running as the owner,
which is exactly the posture B86 says proves nothing. Now wrapped in `runInTenant`. **3/3 under the
production posture.**

**`drive-atomization`'s RLS probe** has the opposite failure available to it. Its meaning is "RLS
denied it", so through a bypassing connection it sees every row and reports a leak that is really a
posture. It used to read `DATABASE_URL` — which the suite now hands the OWNER for scenario drives,
so the check would have indicted the product for the runner's choice of connection. It now takes the
scoped URL **by name** (`DATABASE_URL_APP`), and if the role it lands on turns out to bypass RLS it
says so and measures nothing rather than rendering either verdict.

Both directions of the same rule: **a check whose meaning depends on the connection must look at the
connection.** Passing and failing are equally worthless when the instrument is misconfigured, and the
only honest third answer is "not measured".

**Also fixed in the same drive** — a watermark instead of a wall-clock window. "Created in the last
ten minutes" isolated the upload only because the company was months old; a company built for the run
has its starter library copied in seconds earlier, so the window swept ~300 copied atoms in with the
35 uploaded ones and the content check reported "208 empty" — a finding about the starter shelf,
attributed to the atomizer.

---

## B100 · Three drives poisoned themselves, then reported the wreckage as product failures

**Class** harness · a drive that can only run once.

Each staged a fixture under a fixed identifier that the schema declares unique, so the FIRST run
succeeded and every run after it collided — and each reported the collision as a product fault.

| drive | fixed identifier | unique key | what it printed on run 2 |
|---|---|---|---|
| `opp-scout` | a byte-identical intake notice | `opportunities.content_hash` | `✗ stageIntake succeeded — Failed to stage intake` |
| `provisioning-cockpit` | the portal label `'pv-proof'` | `(tenant_id, opportunity_id, label)` | a duplicate-key stack trace at line 56 |
| `provisioning-cockpit` (again) | the readiness precondition | — | see B101 |

All three now tag their fixture with a per-run id, and the cockpit additionally sweeps anything an
earlier crashed run abandoned. A leftover row is inert instead of fatal.

The pattern is worth naming because it is silent: a drive like this is green the day it is written
and red forever after, and the redness looks exactly like a regression in the thing it tests.

---

## B101 · Three instruments that reported a reading they had not taken

**Class** harness. The same defect as B94's `session()`, found three more times in one pass.

**A dead file, read as a live measurement.** `drive-opp-scout` counted lines in
`/tmp/govwin-sandbox/emulated-claude.log.jsonl` to prove the emulator served the agent's tool-loop.
That file exists on this box and was last written **eleven days ago**, by a rig that is gone. Both
reads succeeded, both returned the same number, and `+0 calls` was reported as a FAILURE — on a run
whose own evidence, printed four lines below, read `rounds: 2, tool_calls: 2, tokensUsed: 365`.
Absence was not the problem; *staleness* was, and a line count cannot tell a silent emulator from a
dead log. Fixed twice over: the authoritative measurement moved to the product's own
`agent.invoked` payload (which is what the drive was already printing as evidence), and the
transcript check now asserts only when the file's mtime shows something wrote it **during this
run** — otherwise NOT MEASURED, with the file's age named.

**A one-sided assertion against ambient state.** `provisioning-cockpit` asserted `ready === true`
under a label naming three of the readiness bar's five terms. Two terms — `itemsUndecided` /
`volumesUndecided` — were added after the master it points at was authored, so all 13 items were
undecided and the check failed correctly while explaining nothing. Worse, the first fix READ the
"not ready" side off whatever it found: a run crashed after marking the items decided and before
its restore, the master stayed permanently ready, and from then on the check reported a failure
**caused by its own earlier crash**. It now sets both states itself, asserts the bar in both
directions, prints all five inputs, and restores each item's exact prior metadata.

**A completeness check against a lazily-reconciled design.** `drive-bridge-buckets` asserted that
every card-holding tenant holds a card for every published opportunity. `immobileyes` was six
short — and all six were published three weeks before that company existed. The bridge is
forward-only; `reconcileTenant` is the self-healing consumer, and its own docstring names this case.
The gap is now the SETUP: the drive calls the admin backfill route as an rfp_admin and re-asks, so
the assertion is "a tenant behind the bridge catches up", which is both true and worth checking.
(Its first run was its own red→green, and every card-holding tenant is now whole.)

**The through-line, stated once:** an instrument that cannot report *"I did not measure this"* will
eventually report something else instead — a zero, a stale number, a state it caused itself — and
whatever it reports will be indistinguishable from a finding. Every one of these was a green or red
verdict that no measurement stood behind.

---

## B102 · Fixture rot, measured rather than felt — and made a standing check

**Class** meta. Not a defect: an answer to "how much of this problem is left?"

After B98–B101 the reasonable worry was that fixture rot is systemic — termites rather than a few
bad boards. So it was **counted**, by asking the live database about every uuid and email literal in
every harness script (`frontend/scripts/audit-pinned-fixtures.mjs`).

The raw number is alarming and misleading in equal measure: **66 of 239 scripts hold a dead
literal**. Three corrections separate the alarm from the finding, and each is load-bearing:

1. **Most scripts are not the safety net.** `immo-*`, `t3cp-*`, `seed-*` and the one-off probes built
   an artifact once, in a sprint that is over. A dead uuid in one of those is a dead script, not a
   blind spot — nothing runs it, so nothing reports it green.
2. **A dead literal behind an env var is unreachable.** `process.env.TEST_TENANT_ID ?? '<uuid>'`
   never reads the uuid when the runner exports the variable, resolved from the live database.
3. **A literal in a comment is prose.**

Scoped to what actually runs — the 27 branch drives, the four lenses, the guide capture — the count
was **3 of 31**, and validating the instrument before believing it (rule 2, again) reduced that to
**one real item**:

| flagged | what it actually was |
|---|---|
| `drive-collaborator-boundary` `TEMP_EMAIL` | an address the drive **creates** — absence is the correct state. A scanner false positive; it now recognises creation-intent naming. |
| `drive-rls-admin` `IMMO = 'dd831b77…'` | a dead constant **nothing read**. Removed. |
| `drive-starter-offer` actor email | REAL: a resolved `ADMIN` uuid sitting next to the literal `eric@immobileyes.com`, so every event the drive emitted was attributed to a person who does not exist — a small lie in exactly the field an audit trail is read from. Now resolved from the id, and CANNOT-RUN if the id is dead. |

**The standing part.** Counting once is worth little; the reason rot went unnoticed is that nothing
was watching for it. `run-branch-drives.sh` now runs the audit as a **preflight**, beside the
RLS-posture and cross-tenant-invariant checks, and prints

```
fixtures: nothing in the suite points at a row that no longer exists
```

or names the offenders. It does not abort — the drives still measure real things — but a non-zero
count means some green below may be green about nothing, and that is now said loudly and first.

**The 53 historical scripts are left as they are, deliberately.** Rewriting a script that ran once
in a finished sprint buys nothing and risks changing a record of what was done. They are reported
in their own section so nobody mistakes them for coverage, and the rule for reviving one is the
same as for any drive: build the scenario it needs.

---

## B103 · The self-disposing scenario erases the audit evidence another lens depends on

**Class** design tension, surfaced by fixing something else. Not yet resolved — deliberately.

`pipeline/scripts/check_ai_invoke_contract.py` reports which AI-carrying workflows have never had
their trigger fired on this box, and it answers that question by reading **observed payloads out of
`system_events`**. It currently lists `OnCollaboratorInvited` as UNCOVERED.

That is false. The route emits it —
`app/api/portal/[tenantSlug]/proposals/[proposalId]/collaborators/route.ts:302` — and
`buildCrossCompany` calls that route on **every** run of `pin`, `identity-deeplink` and
`spine-section-todo`. The trigger fires several times a suite.

The evidence does not survive. `system_events` carries a `tenant_id`, so the scenario factory's
catalog-driven teardown — which deliberately reads the catalog rather than a maintained list, so it
stays complete as the schema grows — deletes the audit rows along with the tenant. The drive proves
the emission and then removes the proof.

**Both halves of this are correct in isolation, which is what makes it interesting.**

- The factory's promise is *"the world is exactly as it was found"*, and its self-test asserts
  exactly that. Exempting a table would weaken the guarantee that makes nine drives trustworthy,
  and it would leak rows into a shared audit trail on every run, forever.
- Audit evidence is precisely the category of row you least want a test to delete, and a lens that
  reports "never fired" about something that fires constantly is producing the mirror image of this
  week's other defect: a false UNCOVERED rather than a false PASS. Both are unearned verdicts.

**Not resolved here on purpose.** The options are real and they trade off differently:

1. Fire the affected triggers outside a disposing scenario when doing the #198 coverage work —
   cheapest, changes nothing structural, leaves the lens permanently unable to see suite-fired
   triggers.
2. Read the lens *before* dispose, inside the drive — accurate, but couples an unrelated lens to
   the drive's lifecycle.
3. Let the factory preserve `system_events` behind an explicit opt-in — honest about audit rows
   being platform history rather than tenant data, at the cost of the unqualified "world identical"
   claim and a slow accumulation in a shared table.

Recorded so the next person meets the tension rather than the symptom, and so nobody "fixes" the
lens by exempting a table without knowing what that costs.

**Found by** re-measuring #198 rather than trusting the count written in it — 15 uncovered had
quietly become 5, and one of the remaining five did not survive reading the emitter.

---

## B104 · Opening a legacy guide in the Content Studio publishes its markdown source

**Class** silent content corruption on a public surface, reachable by the ordinary use of a feature.

The Content Studio is canvas-native, but almost every page it edits predates the canvas: guides,
blog posts and resources whose bodies are markdown strings. When a page has no stored canvas,
`app/admin/site/docs/[type]/[slug]/page.tsx` seeds one with `canvasFromDocBody(title, legacyBody)`.
`parseBodyToNodes` understood headings, bullets and numbered lists — and copied everything else
through **verbatim**, including the inline syntax.

So the canvas an editor opened contained the literal text `**SBIR**`, `*non-dilutive*`, and
`[eligibility checklist](/resources/sbir-sttr-eligibility-check)`. Save once and the server projects
that canvas to HTML into `blocks[0].body`, which is what the public marketing site renders. The
asterisks and the bracket-paren link go live.

Four active pages carry **76 emphasis markers and 4 markdown links** between them, and nothing about
triggering this requires a mistake: opening a page and saving it is the entire job the Studio exists
for. It had simply never been done to one of the older pages.

**What the canvas can and cannot hold** — the fix is shaped by the vocabulary, not by preference.
`TextBlockContent.inline_formats` accepts bold/italic/underline/superscript/subscript, so `**x**`
and `*x*` now round-trip and the web projection already renders them as `<strong>`/`<em>`. Two
things have no representation and are handled openly instead of silently:

- **Links.** There is no href anywhere in the node vocabulary, which is locked against every
  exporter. `[text](url)` flattens to `text (url)` — lossy, but the destination stays readable and
  no markup reaches the page.
- **List items.** `ListContent.items` is `{text, indent_level, children}` — no `inline_formats`
  field at all. Emphasis inside a bullet is stripped to plain text, as it is in headings. Losing
  the bold is a far smaller harm than printing the asterisks.

`_underscore_` and backticks are deliberately **not** parsed. Nothing in the corpus uses them as
markers, and every underscore that exists sits inside a word or a URL (`page_key`,
`file_name.txt`), where a marker reading would mangle real text. The test covering that is green
before and after the fix — it guards against over-parsing, so it has to be.

Offsets count **code points**, matching `inlineRunsWeb`, which indexes `[...text]`. Counting UTF-16
units would slide every format right of an emoji.

**Red first.** Six of the seven new tests fail against the parser as it was; the seventh is the
over-parsing guard described above.

**Found by** authoring new content through the same pipeline and reading the HTML it produced,
rather than reading the code that produces it.

---

## B105 · Publishing a page never closed the ToDo that asked for it

**Class** a human queue with no drain — the state accumulates until people stop reading it.

A `content_publish` ToDo means *a human still has to look at this and publish it*. Publishing is
that work. Nothing completed the ToDo.

The BAA program primer had been reviewed and published — `status='active'`, live at
`/resources/what-is-a-baa` — while its "Review & publish" ToDo sat `open`, its `entity_id` naming
the exact version the publish had archived. Every future publish would have left another one.

`publishDocument` and `publishPage` now close it. **Two things about this were easy to get wrong,
and the first version of the fix got both.**

**One — match by `page_key`, not by the ToDo's entity id.** A publish *rewrites rows*: the draft is
promoted, the prior active archived. The id a ToDo was created with is routinely stale by the time
anyone acts on it. `page_key` is what survives a version, which makes it the honest join.

**Two — it has to run in BYPASS, and this is the half that shipped broken.** These ToDos are
**platform scope** (`tenant_id IS NULL`) — curation is owned by no tenant — and mig 185 made `tasks`
UPDATE own-tenant-only. A tenant-equality policy never matches NULL, so the identical statement
through the context-aware `sql` under `govtech_app` updates **zero rows and reports no error**. It
is the same trap as B51, which the continuation doc had already written down.

**What makes this worth the entry is how it hid.** The first fix was proven red→green on a
disposable probe page — and that probe connected as the **owner**, which is `rolsuper` and bypasses
RLS entirely. Green there. Still broken in the product. CONTINUATION.md warns about exactly this
("an entire session's worth of *RLS proven* once came from exactly that"), and the warning is
usually read as being about isolation *claims*. It is not only that: **any behaviour that depends on
a policy is unmeasurable through a superuser connection, in both directions.** A privileged probe
cannot see this class of bug at all.

What caught it was the browser drive, running against the app as it actually serves — `govtech_app`,
RLS on. The rebuilt server produced the *identical* failure line as the unfixed one, which is the
only reason the false green did not stand.

Completion now goes through `completeTask` under `runInBypass` rather than a bare UPDATE, so the
close lands in the audit trail with the publishing admin as its actor. `requireAdmin` returns
`role` for that reason — the guard has already established it, and a caller acting downstream
should not have to guess.

**Mig 210** applies the same rule once over history (any open ToDo whose page_key has an active
version), and cancels a second category the fix cannot reach — ToDos whose `entity_id` names a page
row that no longer exists, whose "open it in the Studio" deep link resolves to nothing.

**Red first, through the browser.** `scripts/close-e2e-cms.mjs` now reopens the review ToDo as part
of its precondition (un-publishing a guide genuinely puts the review back in front of a human) and
asserts the publish drains it. Driving the real Publish button as a real signed-in admin:

```
build as it was          ✓ the guide is now LIVE — v5
                         ✗ publishing CLOSED the review ToDo — open=1 completed=0

after the page_key fix   ✓ the guide is now LIVE — v6
                         ✗ publishing CLOSED the review ToDo — open=1 completed=0   ← RLS

after the bypass fix     ✓ the guide is now LIVE — v7
                         ✓ publishing CLOSED the review ToDo — open=0 completed=1
```

Green as `govtech_app`, RLS on, driven through the real Publish button. The closed row carries a
`completed_by` — a real admin, not a placeholder — because completion runs through `completeTask`.

**Found by** counting the review queue while queueing more content into it, and noticing one row
whose page was already live. **Kept honest by** re-running the drive after the rebuild instead of
assuming the fix had landed.

---

## B106 · A seed re-run stranded three items in a human's review queue — page deleted before its ToDo

**Class** harness, ordering. Mine, and it produced exactly the artifact the two bugs above are about.

`seed-followon-guides.mts` replaces its drafts on every run. It deleted the previous draft **page**
first, then tried to delete the previous **ToDo** — and the delete it used was
`entity_id = ${draft.id}`, the row this run had just created, which no earlier ToDo can possibly
point at. Nothing matched, so every re-run left its predecessor's ToDo behind, pointing at a page
row the same run had already dropped. Three accumulated.

Switching the match to `page_key` was not enough, and the second run proved it: by the time the
lookup runs, the page row is gone, so the subquery returns nothing and the id the ToDo holds
resolves to nothing either. **The order is the fix** — delete the ToDo while the row it names still
exists, then delete the page. `seed-practice-guides.mts` had the same latent ordering and got the
same correction, and both scripts now sweep pre-existing orphans before they start.

Worth stating plainly because it is the general shape: **a row that references another by id must
be cleaned up before its referent, not after.** No page_key, slug or natural key can recover the
link once the referent is gone.

There is no product path that deletes a `content_pages` row — archive is the model
(docs/ARCHIVABLE_CONTRACT.md) — so an orphan can only come from a seed script, which is what makes
mig 210's cancel arm safe.

**Found by** watching the open-ToDo count go 7 → 10 → 13 across runs that were supposed to be
idempotent, and refusing to accept the number.

---

## B107 · The harness hit B105's own trap, and its brand-new precondition is what noticed

**Class** harness, RLS. The third instance of one lesson in one sitting, which is why it is written
down as its own entry rather than folded into B105.

The suite ran 30 pass · 1 fail, and the failure was `cms-publish` — the drive that had just gone
green by hand ten minutes earlier:

```
✓ precondition: BAA guide is NOT live (0 active)
✓ precondition: there IS a draft to publish — drafts=1
✗ precondition: the review ToDo is OPEN — open=0
…
✓ publishing CLOSED the review ToDo that asked for it — open=0 completed=1
```

Read those two lines together. The final assertion "passed" on a row that was **already completed
before the drive started** — it was reporting the leftover state of the previous run, not the
effect of this one. The precondition is the only thing standing between that and a confident
false green.

**Why the reopen did nothing.** `run-branch-drives.sh` exports `DATABASE_URL` as the SCOPED
`govtech_app` role, correctly — the isolation drives need it. `close-e2e-cms.mjs` read that
variable for its own bookkeeping, and its reopen is an UPDATE on a `content_publish` ToDo, which is
**platform scope** (`tenant_id IS NULL`). Mig 185 made `tasks` UPDATE tenant-equality, NULL
satisfies no equality, and a policy that matches nothing raises nothing. Zero rows, no error, and
a precondition that silently did not happen.

The `content_pages` half of the same reset worked, which is what made it look like the script was
running fine.

**Fixed** by reading `DATABASE_URL_OWNER` / `GUIDE_DB` — the variables the runner *already* exports
for exactly this, because a harness computing what a tenant should see cannot ask through the
scoped connection. Verified by re-running under the suite's own environment (`DATABASE_URL` =
`govtech_app`), where it now reports `open=1` and then closes it.

**The lesson, stated once for all three instances.** B105's product bug, B105's false green on an
owner-connection probe, and this: the same fact from three sides. **Whether a write lands is a
property of the connection, not of the SQL.** Any check whose meaning depends on a row being
written must state which role it runs as and why — and for a platform-scope row (`tenant_id IS
NULL`) under `govtech_app`, the answer is always that it will not land. This is B99's rule
("checks whose meaning depended on the connection, and neither one looked") recurring, and it will
recur again; the durable defence is that seed and reset code touching platform rows uses the owner
by NAME rather than inheriting whatever `DATABASE_URL` happens to be.

**Found by** the suite, running a drive that had passed by hand — which is the entire argument for
running the suite after a green single-drive result rather than instead of one.

---

## B108 · The rig reported itself healthy after a fatal failure, and the RLS preflight cried wolf

**Class** harness. Two instruments in the bring-up path, both giving a confident wrong answer, found
within ten minutes of each other on a fresh container.

**B108a — `rehydrate-sandbox.sh` finishes cleanly on a database with zero tables.**

A reclaimed VM comes up with the repo re-cloned and nothing else. The script's job is to restore the
box, and it printed:

```
[rehydrate] running migrations…
Node.js v22.22.2                       ← the tail of a stack trace, swallowed
[rehydrate] resetting admin passwords…
Node.js v22.22.2                       ← and again
[rehydrate] foundation atoms: ?
[rehydrate] logins — tenant: … / admin: …
[rehydrate] state restored (pg+db+build+stage)
```

`0 tables`. Migrations died on `Cannot find package 'postgres'` — there is no root `node_modules`,
and `db/migrations/migrate.mjs` resolves from the repo root. The password reset died the same way.
The script is `set -uo pipefail` **without `-e`**, so no step aborts the run, and the final line
claims the state was restored. Three more things it does not do that a fresh box needs: install
`pgvector` (migration 001 fails without it), repair an interrupted `dpkg`, and give `govtech_app` a
LOGIN password.

The shape to keep in mind: **a restore script that cannot fail is worse than none, because its last
line is believed.**

**B108b — `check-rls-posture.mjs` reported `RLS POSTURE WRONG` against a perfectly isolated database.**

This is the preflight `run-branch-drives.sh` uses to mark every isolation drive CANT-RUN, so a false
alarm here stops the suite measuring anything. The bug is an off-by-one on *which tenant*:

```js
FROM tenants t ORDER BY cards DESC LIMIT 2   // A = foundation (9 cards), B = immobileyes (0)
if (pair.length < 2 || pair[0].cards === 0)  // guards A's count …
const [own] = …WHERE tenant_id = ${B.id}     // … but asserts on B's rows
```

On a freshly migrated box exactly one tenant owns cards. A cleared the guard, B had none, and
"B sees 0 of its own" was read as a deny-all. Verified wrong by hand before touching it:

```
context = foundation  → 9 own cards        (sees its own)
context = immobileyes → 0 foundation cards (cannot see the other's)
```

Both halves of isolation holding, while the checker said WRONG.

**Fixed** by requiring rows from BOTH tenants and — the part that matters — reporting **CANNOT-RUN,
not WRONG**, when the fixture cannot support the assertion, with a verdict that says so instead of
sending the reader after a policy bug that does not exist. Proven red→green by seeding one card
(`context 'immobileyes': 0 foreign, 1 own — isolation, not deny-all`) and then removing it.

It failed SAFE — a false alarm rather than a false pass — which is the right direction and still
wrong: **a preflight that cries wolf on a healthy box is one people learn to skip.**

**Residual, stated so it is not rediscovered:** on a rehydrated box the behaviour assertion is
genuinely UNMEASURED, because only one tenant owns cards. The posture is correct (verified by hand);
the instrument cannot say so. The durable fix belongs in the SEED — production fans every activated
opportunity to every tenant, so one-tenant-only is the unrealistic state.

**Found by** rebuilding the rig for the canvas sweep and refusing to start measuring on a box whose
preflight disagreed with a hand probe.

---

## B109 · A spacer had five readers and four different heights, none of them the author's

**Class** one node model, several readers, disagreeing silently — the B64/B65/B73 family, and the
first instance where the ruler and the renderer disagreed about a *node's own property* rather than
about typography.

`spacer` is the canvas primitive whose entire purpose is a height. Five things read one, and they
did not agree:

| reader | height it used | where |
|---|---|---|
| page **ruler** | `content.height` ?? body line | `canvas-document.ts:946` |
| **HTML → PDF** | `style.space_after` ?? **12pt** | `canvas-html.ts:471` |
| **docx** | hardcoded `after: 200` twips (10pt) | `docx-exporter.ts:491` |
| **pptx** | hardcoded `0.3` in (21.6pt) | `pptx-exporter.ts:570` |
| **editor** | hardcoded `h-8` (≈24pt) | `canvas-renderer.tsx:684` |
| xlsx | filtered by name — correct, a grid has no whitespace to place | `xlsx-exporter.ts:120` |

So an author who set a spacer's height saw four different numbers in four artifacts and the one they
typed in none of them. Proven straight off the emitted HTML, with no pagination argument needed:

```
content.height = 600     →  html emits height:12pt      ← the author's value, discarded
style.space_after = 600  →  html emits height:600pt
```

The canonical fixture (`probe-node-vocabulary.mts`) sets `{ height: 24 }` — the field the renderer
ignored — so every existing test agreed with a renderer that was not reading it.

Nothing is *lost*: it is whitespace. What made it worth fixing is the gauge. The ruler measured
`content.height` and the page did not render it, so the number the customer is shown as their page
count was computed from a height no writer would produce — the exact divergence B64 and B65 were
about, in a node whose only job is that number.

**Fixed** with one accessor, `spacerHeightPt(node, fallback)`, used by the ruler, canvas-html, docx,
pptx and the editor. `content.height` is canonical (it is spacer-specific); `style.space_after`
stays honoured as a fallback because stored documents may carry it.

**Red first** — 4 of 6 new assertions fail against the old code; the 2 that pass both ways are the
backward-compatibility guards, which have to:

```
× renders the height the author set in content.height
× prefers content.height when both are present
× the RULER and the rendered page agree — a tall spacer pushes both to a second page
× docx scales the author height into twips rather than hardcoding 200
  Tests  4 failed | 9 passed (13)
```

Green after, with the gauge and the artifact finally agreeing:

```
content.height = 900   ruler 2 pp · PDF 2 pp · html emits height:900pt
```

**Found by** driving all 22 primitives through the real create → save → export routes rather than
calling the exporters directly, then asking why one structural node's differential was inconsistent.

**A note on the instrument, which was wrong five times before it was right.** The differential that
found this took six iterations, each giving a confident wrong answer: measuring structural nodes
ALONE (a `toc` with no headings correctly renders nothing); a byte comparison across a zip (the same
input does not always compress to the same length — one cell flipped between runs); a 900pt spacer
that moves content without moving the compressed byte count; and page count, which sees a
`page_break` but cannot see a `toc`. **No single metric answers for all four structural types in all
four formats.** The drive's table now REPORTS what it measured and names its instrument, and the
decisive per-node assertions live in `scripts/probe-structural-nodes.mts`, where each type is
measured by the effect it actually has. A table that says "this instrument saw nothing" is worth
keeping; one that says "the writer emitted nothing" on the same evidence is not.

---

## B110 · The tool that gates "the log is clean" was blind to a third of the log

**Class** the same one B67 was, recurring in the same file — and found by using it, not by reading it.

B67 fixed this script's *status* reader for the log's three heading conventions. It left the **entry
matcher** accepting exactly one separator:

```js
const m = /^##\s+(B\d+)\s+—\s+(.*)$/.exec(line.trim());   // em-dash, and only em-dash
```

Every `## B108 · title` heading was skipped. **114 headings in the file, 80 counted, 34 invisible** —
and the count did not move when two entries were appended, which is what gave it away.

The consequence is the one this script exists to prevent: an OPEN entry among those 34 would have
been reported as `✓ nothing open` by the only tool whose job is to stop that claim being made.

**Two more things surfaced once it could see.**

**The status is not always in the heading.** Newer entries put the title in the heading and the
disposition in the body (`**Fixed** by …`). Reading only headings left 28 unclassifiable — honest,
but useless for the one question the tool answers. It now reads the entry body, bounded by the next
entry, for the same vocabulary.

**A stated choice is deferred, not open.** With the body reader on, B103 came back OPEN — an entry
whose whole point is that a trade-off was recorded *on purpose*. "Not yet resolved — deliberately"
contains "not yet resolved", and open-first ordering read the gap and missed the choice. The words
that mark a decision now beat the words that mark a gap.

```
before   80 entries — 77 closed · 0 open · 3 deferred · 0 unclassifiable    ← 34 entries unseen
after   108 entries — 83 closed · 0 open · 4 deferred · 21 unclassifiable
```

**The 21 remaining unclassifiable entries are reported, not hidden** — that is the difference that
matters. An entry the tool cannot read shows up as something to fix; an entry it cannot *see* shows
up as nothing at all.

**Found by** appending two entries and noticing the total did not change.

---

## B111 · The editor page was scaling itself twice

**Class** two computations of one quantity — the session's recurring shape, this time inside a
single style object.

The page container set `width: canvas.width * scale`, `minHeight`, `padding` and every font size
pre-multiplied by `scale` — and then carried `transform: scale(scale)` on top. The page rendered at
**scale²**.

It hid well. At full width `scale` clamps to 1 and the bug does not exist. When it does bite, every
dimension inside shrinks together, so the page looks *correct* and is merely too small — which no
screenshot review can catch, because there is nothing wrong with the picture.

Measured rather than argued. `offsetWidth` is layout; `getBoundingClientRect().width` is
post-transform; equal means one scaling:

```
                     BEFORE                          AFTER
viewport 1600   layout 612 · visual 612  ok      layout 612 · visual 612  ok
viewport 1100   layout 460 · visual 346  ✗0.752  layout 460 · visual 460  ok
viewport  800   layout 612 · visual 612  ok      layout 612 · visual 612  ok
viewport  600   layout 536 · visual 469  ✗0.875  layout 536 · visual 536  ok
```

**Fixed** by removing the transform — the redundant end, since every dimension was already scaled.
Centring is unaffected: the outer container is `flex … items-center`, which was doing the real
centring; `transformOrigin: 'top center'` only chose which way the surplus shrink pulled.

`scripts/probe-page-scale.mts` is kept. It is the only instrument that can see this class.

**Found by** building the measurement grid, which cannot be written without answering exactly what
coordinate space the page is in — and the answer was two.

---

## B112 · Page boundaries drawn from arithmetic, when the paginator relocates blocks

**Class** a model that is not the model the product uses. Mine, shipped in 129910d3 and corrected
within the hour after the user asked whether sections overlapping pages were contemplated.

The first boundary overlay drew a line at `marginTop + k × usableHeight`. That is only where a page
breaks if content flows continuously — and it does not. `fitKeep` **relocates** a block that will not
fit (a table, a figure, a `keep_together` group) wholesale to the next page, which the paginator's
own comment had already measured:

```
table(40 rows) alone                   → 2 pages
ONE sentence of prose + the same table  → 3 pages
```

So with prose above a tall table, printed page 2 **begins at the table**, while the geometric line
falls in the middle of it. That is worse than drawing nothing: it asserts a break where none happens
and hides the one that does. Every document containing an atomic block was affected — most real
proposals.

**Fixed** by taking boundaries from `paginate(doc).perNode`, whose `startPage` already models
`fitKeep`, and resolving each to a position by measuring the first node of that page in the DOM. The
geometric line survives only as the pre-measurement fallback on first paint.

**A claim of mine that was wrong.** I had told the user boundaries needed no measurement — true only
for continuous flow with no atomic nodes. They now join the group boxes on the post-paint pass; the
grid and rulers genuinely are pure geometry and stay so.

**The relocation gap is now drawn**, and that is the part that matters to an author. A pushed block
leaves the previous page part-blank in print, and a continuous editor shows that nowhere — which is
exactly why an image appears to "pop" to the next page when a sentence is added above it. The
hatched band is that whitespace, visible while it is still editable, with the boundary labelled
`pushed` so the cause is legible.

Driven end to end on the case itself (`scripts/drive-ruler-overlays.mts`), which asserts the fixture
really relocates BEFORE it looks at the UI — so a pass cannot come from a document that never
triggered the behaviour:

```
the paginator says: 3 pages · the table starts on page 2
✓ a page boundary is drawn — 3 dashed rule(s)
✓ the relocation gap is shaded — 2 hatched band(s)
✓ a boundary is labelled "pushed" — the cause is legible
```

**Found by** the user asking whether content overlapping a page boundary was fully contemplated. It
was not.

---

## B113 · Seven proposal-spine tables had no row-level security at all · FIXED
**Where** `canvas_versions`, `proposal_artifacts`, `proposal_compliance_matrix`,
`proposal_collaborators`, `proposal_comments`, `proposal_stage_history`,
`collaborator_stage_access` · fixed by `db/migrations/212_rls_proposal_spine.sql`

Every one had `relrowsecurity = false` and zero policies, so every row was readable by any
connection on the NOBYPASSRLS `govtech_app` pool whatever `app.tenant_id` held. This violates the
project's absolute rule — nothing reads or writes cross-tenant, ever; bridges copy inward.

**Proven** as `govtech_app` with the tenant context set to a UUID that owns nothing:

```
table                          owner sees   foreign tenant sees
proposal_artifacts                     17                    17
proposal_compliance_matrix             64                    64
collaborator_stage_access               8                     8
proposal_collaborators                  4                     4
canvas_versions / proposal_comments / proposal_stage_history — empty, identical posture
```

100% of rows in every table that had any. The three zeroes are **not** evidence of protection:
those tables were simply empty, and CANNOT-RUN is not PASS. `canvas_versions` is the consequential
one — the stored content of every version of every section, plus the `ai_instruction` that made it.

**Class — a table whose tenancy is FK lineage falls out of every tenant_id-shaped audit.** None of
the seven has a `tenant_id` column; they reach `proposals.tenant_id` through a foreign key. Every
audit in this repo looked for the column, so the whole class was invisible to the instrument meant
to cover it. Fixed with one `FOR ALL tenant_isolation` policy each, EXISTS up the FK chain, matching
`proposal_sections` verbatim. Verified both directions on all seven — each empty table seeded first
so it was actually measured — zero foreign rows visible, full count still visible in the owning
context (isolation, not deny-all).

**Found by** a corpus count that looked wrong while re-running the ruler harnesses. The first
diagnosis was itself wrong: a psql session as `govtech_app` with no tenant context reported
`proposals = 0`, which was correct RLS behaviour, not a missing corpus.

---

## B114 · The RLS posture checker proved ONE table and reported a posture · FIXED
**Where** `frontend/scripts/check-rls-posture.mjs`

It tested isolation on `tenant_opportunity_cards` alone and printed
`✓ RLS posture correct — isolation results from this box mean what they say`. It passed green with
all of B113 wide open, and every drive in the suite gates on it.

**Fixed** by asking the general question in two independent forms:

* **STRUCTURE** — every tenant-owned table must carry a policy. The table set is a transitive FK
  closure over anything reaching a `tenant_id`-bearing table, with `users` excluded as a hop
  (nearly everything FKs to `users`; admitting that edge drags every platform-scope table into a
  tenant set it does not belong to). Needs **no fixture data**, so it holds on a freshly migrated
  box — exactly when a missing policy is easiest to add and hardest to see.
* **BEHAVIOUR** — every row visible in exactly one tenant context, or in all of them. Expected sum
  is `owned + shared × N`, **not** `total`: the system template catalog and platform-scope tasks are
  supposed to appear in every context, and scoring those as leaks is how a check becomes noise.

**Structure exists because behaviour structurally cannot see the whole class.** A table where every
row leaks to every tenant is indistinguishable, by row counts alone, from a shared catalog meant to
be visible to everyone — `canvas_versions` with no policy and `volume_required_items` as designed
produce identical numbers. Counting cannot separate them; the catalog can. My first behavioural rule
would have passed the very bug it was written for.

Red-first: dropping `canvas_versions`' policy trips the structural rule; restoring clears it. The
widened check immediately found four more of B113's shape — `process_instance_transitions` (300 rows,
all visible in all 5 contexts), `agent_task_results`, and the empty `stage_completion_snapshots` /
`stage_gate_requirements` that behaviour could not see. Closed by mig 213, whose child policies
**inherit** the parent's predicate via a bare EXISTS rather than restating it (measured: 444 across
5 contexts against 300 rows = 264 owned seen once + 36 platform-scope seen five times; unfiltered
would have been 1500).

Exemptions are an allowlist, which is a thing to be suspicious of. Two rules keep it honest: every
entry states WHY, and entries are REPORTED on every run rather than skipped.

**Class — an instrument whose scope is narrower than its claim.** See B110, B115, B116.

---

## B115 · The entire AI agent workforce was inert · FIXED
**Where** `pipeline/src/agents/fabric.py`, `pipeline/src/workflows/actions/cms_content.py` ·
fixed by `pipeline/src/sdk_compat.py`

`anthropic` 1.0.0 removed `temperature` from `messages.create()` — not renamed, not folded into
`output_config` (which carries only `effort` and `format`). Passing it raises `TypeError`
**client-side, before any HTTP request**, so a call site that still sent it did not degrade. It
failed, every time, in full.

**Proven** across a 30-day window of `system_events`: every archetype that ran recorded `start`
followed immediately by `error`, 100% failure, zero tokens — `opportunity_scout`,
`content_generator`, `formatter`, `cost_estimator`, `librarian`, `curation_qa`,
`continuity_manager`, `advisory_manager`, `amendment_monitor`, `library_seed_suggester`,
`partner_coordinator`, `redaction_guard`. Separately the CMS draft generator, which falls back to a
brief on any exception, reported success while generating nothing.

**Why nothing surfaced it.** The fabric records the failure and safe-skips rather than dead-ending a
workflow — a deliberate invariant, an agent must never strand a build. From outside, every flow
completed. Only a check asserting the agent PRODUCED something can see it, and the one that does
(`drive-opp-scout`) was itself dying on a stale fixture UUID. **Two failures in series, the outer
masking the inner** — the drive had been written off as known-bad, so its real finding sat behind a
fixture error.

Fixed by one capability probe that introspects **this interpreter's** SDK rather than comparing a
version string. Both call sites spread `sampling_kwargs(...)`: they fail independently, and fixing
the fabric alone left the CMS generator broken — a capability with two definitions will eventually
disagree with itself.

After: `drive-opp-scout` ALL PASS — `rounds=2 · tool_calls=2 · tokens=365 · $0.0008`.

**Known consequence, not hidden:** archetypes no longer get their requested sampling (several set
0.2–0.3 for deterministic analytical output). Logged once at import. Whether those want
`output_config.effort` instead is an open decision.

---

## B116 · Two pipeline workers ran against one database, holding different code · FIXED
**Where** `scripts/sandbox-up.sh`

`drive-opp-scout` passed standalone and failed in the suite, same box, same minute. Not flake.

The start guard was `pgrep -f "python3 src/main.py"`. That is one of **two spellings** that start
the same program — `python3 -m main` from `pipeline/src` is the other, and it does not match. A
worker already up under the second spelling was invisible, a second started beside it, and both
polled `agent_task_queue`.

Worse than a wasted process: a long-lived worker keeps whatever code it imported at start, so the
two were **different checkout moments**. The one up since 15:14 kept writing B115's error into
`system_events` while the 16:08 restart succeeded. On what looked like one process I measured
`completed` at 16:08:55 and errors at 16:10:28 — which sent me hunting for stale bytecode and a
second fabric that never existed. A drive reading "the latest invocation" saw whichever worker
answered last, so the same drive passed or failed at random.

**Fixed** by matching the worker by what it IS rather than how it was spelled, via one `worker_pids`
function used by both the start check and the confirmation — two patterns for one question is how
this began — and warning when more than one is found. Red-first with a real second worker under the
other spelling: old guard saw 1 and missed it, new matcher saw 2.

**Class — an instrument whose scope is narrower than its claim** (B110, B114, B115), here in its
cheapest form: a process pattern. The same session also had `pgrep -f` matching the invoking shell
and killing its own waiter.

---

## B117 · The slide ruler had never measured a deck out of the database · FIXED
**Where** `frontend/scripts/verify-deck-ruler-live.mts` (new)

`verify-ruler-on-stored-artifacts` promises the corpus. Measured:

```
stored artifacts by canvas format → letter: 64      (slides: none)
molds by format                   → letter: 34 · slide_16_9: 5
```

Every stored proposal section is a letter page, so the slide half of the canvas rested entirely on 7
synthetic in-memory decks and 5 molds whose whole content is brackets. A deck is not a narrow letter
page: it does not reflow, `estimateSlideCount` is one slide per section rather than a height
division, and the failure that matters is not "runs long" but **content falls off the frame and is
simply gone**. A green run over 17 letter volumes says nothing about it.

**Fixed** by a harness that authors decks through the real portal routes as a signed-in
tenant_admin, reads the canvas BACK out of the database, and measures the stored object against the
rendered `.pptx` (unzipped — the XML is DEFLATE-compressed). Carries a deliberately over-stuffed
deck; red-first, neutering `overflowingSlides` reports `NOT FLAGGED — content would be cut off
silently` and exits non-zero. Restored: 21/21, ruler exact on both decks, the over-stuffed slide
flagged at 811pt of content on a 540pt frame.

**Two defects in the harness itself, caught before trusting it** — the instrument before the
finding. It asserted `200`/`data.id` against a create route that answers `201`/`data.documentId` and
posted `canvas` to a save route whose field is `content`. And it read the stored row on the
context-aware `sql` pool while setting no `app.tenant_id`, so RLS correctly returned nothing — then
**fell back to the in-memory document and carried on**. Every assertion after still passed, while
measuring the object built three lines earlier instead of the one the database holds. The harness's
whole claim was false in the way hardest to notice: green. A failed read is now fatal to the case.

**Limitation stated, not papered over:** pptxgenjs clamps shape boxes, so the rendered `.pptx` shows
no shape past the frame even on the overflowing deck. The ruler is the only instrument that can
catch this, which means that assertion rests on the model rather than the artifact.

---

## B118 · The cross-tenant isolation proofs were unfalsifiable — one owner, nothing to refuse · FIXED
**Where** the seed fixture · fixed by `frontend/scripts/seed-isolation-fixture.mts`

Four instruments reported this on the same day, each correctly, each as a NOTE rather than a
finding:

```
check-rls-posture     17 tenant-owned tables empty → "unmeasured, not passing"
verify-db-crud        all 4 Foundation proposals LOCKED — no in-flight build, so the primary
                      customer action cannot be demonstrated
verify-surfaces       /portal/[slug]/contracts/[contractId] not driven — `contracts` is empty
mig 212's own proof   canvas_versions / proposal_comments / proposal_stage_history had to be
                      seeded BY HAND to measure whether they leaked
```

**A note repeated by four instruments is a finding.** Every proposal on the box belonged to ONE
tenant and every section of every one was locked. So the eleven tables migs 212/213 had just put
policies on had exactly one owner, and *"no foreign rows visible"* could not be distinguished from
*"there was no foreign tenant to see them"*.

This is the RLS analogue of B64's rule about rulers: **a check that cannot fail is not a check.**
The cross-tenant assertion was passing for the wrong reason, and would have passed identically on a
database with no policies at all — which is precisely the state B113 found and mig 212 fixed. The
proof and the bug coexisted for as long as both existed.

**Fixed** by seeding a second tenant's build — left UNLOCKED at `stage=draft` so section editing is
demonstrable — with artifacts, compliance matrix, version history, a review comment, a stage
transition, and a contract. It also restores version history to three INCUMBENT sections, which
makes `canvas_versions` two-sided and corrects a fixture modelling a state the product cannot
produce: 64 sections locked with zero version history, when a section is locked *because* it was
edited and then locked, and the lock path itself writes a version row.

```
                            before  after
tenants owning proposals         1      2
unlocked sections                0      3
posture tables measured         36     40    (empty 17 → 13)
stored volumes in corpus        17     18    (still 0 under-counts, all exports clean)
```

Five of eight spine tables are now TWO-SIDED — both tenants own rows, sum equals total, no leak —
including `canvas_versions`, which holds the actual proposal text. Three remain one-sided: that
proves the leak direction but cannot rule out a deny-all, and is recorded as such rather than
claimed.

**Three defects in the seed itself, caught before it was trusted.** `artifact_type='document'` is
not in the CHECK vocabulary (`narrative|cost|form|matrix|other`) — CLAUDE.md says confirm a literal
against the CHECK before writing it, and I did not; every literal is now verified and listed
inline. It resolved the target tenant BEFORE calling `undo()`, so a row left by a failed run still
counted against its own tenant and the seed silently moved to a different one — a re-run landing
somewhere else is not idempotent, it just looks like it. And the undo rolls the section version
back as it removes the row, so the numbering invariant holds in BOTH directions.

**Class — a fixture thin enough to make a check unfalsifiable.** Distinct from B102 (fixture rot,
where pinned identifiers stop existing): here every identifier was valid and every assertion ran.
There was simply nothing in the database that could have made them fail. Related: B114, where the
instrument's scope was too narrow; here the DATA's scope was.

---

## B119 · A harness littered the corpus that other instruments measure · FIXED
**Where** `frontend/scripts/verify-deck-ruler-live.mts` (the harness added for B117)

It deleted the `tenant_documents` rows it created and printed `cleanup: 2 of 2 probe deck(s)
removed`. Wrong twice, and neither showed in its own output — the evidence was a later run of a
DIFFERENT lens:

```
verify-db-crud:  ✓ restored atom "Overflow probe — deliberately over-stuff" → un-archived
```

Another instrument finding this one's litter in the tenant library.

**1 · Saving a document MINTS LIBRARY ATOMS.** Fourteen per run, measured — not the two or four a
reading of the code suggests. None are inserted by the harness, so a delete-what-I-created teardown
never touched them. Five runs left twenty-plus atoms in Foundation's library, which is exactly the
corpus the atom lenses and `verify-db-crud` read. **A harness that pollutes the data other
instruments measure is worse than one that cleans up nothing, because the mess looks like data.**

**2 · A throw skipped teardown entirely.** It sat at the end of `main()`, so the run that died on a
schema error left its document behind permanently, and later runs piled on top of it. Now in a
`finally`.

**Fixed** by reconciling atoms on an ID DELTA — the set present before the run subtracted from the
set present after — never by title. A title match is a predicate about content, and these titles
("Counter-UAS Edge Autonomy") are plausible enough that a real row could share one: the box holds a
legitimate `p7 · Introduction and Summary: Counter-UAS…` from an upload seed, which a title sweep
would have destroyed. Deleting by identity cannot reach a row the run did not cause.

Proven by counting the whole table either side, not by trusting the teardown's own report:

```
before: atoms=1688 documents=113
        cleanup: 2/2 deck(s), 14 atom(s) minted by saving them
after : atoms=1688 documents=113        ✅ box is EXACTLY as found
```

**THE SWEEP FOUND A SECOND ONE, which is why this is a class and not an incident.** Snapshotting
all 113 table counts either side of a full branch-suite run showed `drive-canvas-authoring` doing
the same thing at nine times the scale — it authors five documents through the real save route, and
each save mints an atom per primitive in the vocabulary matrix:

```
library_atoms      1688 → 1737   +49
atom_tags         10546 → 10840  +294
tenant_documents    113 →  118   +5
```

Every run, forever, into the tenant library. The fix is now shared code
(`scripts/lib/harness-residue.mts`) rather than copied a third time, and both drives are proven to
leave the box byte-identical:

```
before           1737/118/10840   (atoms/documents/atom_tags)
  deck-ruler     cleanup: 2 tenant_documents, 14 library_atoms
after            1737/118/10840
  canvas-author  cleanup: 5 tenant_documents, 49 library_atoms
after            1737/118/10840
```

The helper deliberately does NOT touch append-only history — `system_events`, `agent_task_log`,
`process_instances`, `episodic_memories`, `page_views`. Those SHOULD grow when a drive runs; they
are the record that it ran, several lenses assert against them, and erasing them would be the B103
mistake of a harness deleting evidence another instrument depends on.

**MEASURED AFTER THE FIX, on the same instrument.** The atom and document families drop out of the
delta entirely — 27 tables changed before, 22 after, and the five that disappeared are exactly the
ones the helper reclaims:

```
library_atoms  +49 → 0      atom_tags   +294 → 0      atom_members    +44 → 0
atom_embeddings +49 → 0     tenant_documents +5 → 0
```

**WHAT REMAINS, and why it is not chased here.** The 22 that still move are almost entirely
append-only history that SHOULD grow — system_events +1277, process_instance_transitions +138,
process_instances +46, episodic_memories +24, agent_task_log +21, tasks +8. Underneath that sit
roughly ten business-entity rows per run across ten tables: 3 `partner_user` accounts (all in the
marked `@ext.test` namespace with random suffixes, from `partner-invite` and `uncovered-triggers`),
an opportunity, a curated solicitation, a content page, a collaborator, an amendment.

That is the same class at a tenth the scale, and it is left DELIBERATELY. Some of those drives
create a scenario a later drive reads, so reclaiming a row here can break a passing drive there —
and unlike the atoms, which no drive wanted, each of these needs its own check of what depends on
it. Bounded, identifiable, and named rather than quietly tolerated.

**A LIMIT, STATED.** This measures ACCUMULATION, not blast radius: a drive that creates and deletes
the same row nets to zero here while still having perturbed data another instrument was reading
mid-run. Zero delta is necessary, not sufficient. And the residue left by runs BEFORE this fix is
still on the box — bounded and known (roughly 50 atoms and 5 documents per prior suite run), but not
safely identifiable after the fact without the id snapshot, and a title sweep is exactly the
predicate this entry exists to warn against.

**Class — a teardown that removes what it INSERTED rather than what it CAUSED.** Related to B100
(drives poisoning themselves and reporting the wreckage as product failures) and B103 (a scenario
erasing evidence another lens depends on); the common shape is a harness whose blast radius is
larger than its own bookkeeping. The general form of the fix is a before/after delta over the
tables a run can reach, which needs no knowledge of what the product does downstream of a write —
and that ignorance is the point, since the fourteen atoms were a side effect nobody had written
down.

---

## B120 · A document authored through the API was unopenable — three unguarded reads of an optional field · FIXED
**Where** `canvas-renderer.tsx:524`, `canvas-editor.tsx:1009`, `sheet-editor.tsx:730`

Create a document, save content, reopen it: **"Something went wrong — this page failed to load."**
The content was perfectly intact. The same canvas exported to `.docx` and `.pdf` without complaint
the entire time. Only the workspace was gone.

```
TypeError: Cannot read properties of undefined (reading 'replace')
```

`status` is OPTIONAL on `CanvasDocument.metadata`. A canvas authored as `{ metadata: { title } }`
has none, and three places read it unguarded and called `.replace()` during render. React unmounted
the tree and the route error boundary took over. `canvas-editor:1009` is the one a document and a
deck actually hit; `sheet-editor:730` is the same bug on the spreadsheet surface.

Every sibling read in those files was already guarded — `(node.provenance?.source ?? 'manual')`, and
`canvas-sidebar` does `(doc.metadata?.status ?? 'draft')` **for this exact field**. These three were
the ones that were not, sitting behind a status line nobody looks at.

**Isolated by bisect, not by reading**, because reading had already been wrong twice this session:

```
metadata WITH status         ✅ renders
metadata WITHOUT status      💥 CRASH  ← one plain text_block. That is the whole bug.
```

**WHY NOTHING CAUGHT IT — the part worth keeping.** `verify-surfaces` drives EXISTING documents, and
every document in the fixture was written by an older path that sets `status`. Nothing in the suite
ever authored one the way the API does, so the crash could not appear. A byte-level harness had no
reason to look: the exports were fine throughout, which is exactly what made it invisible — **the
data was never wrong, only the screen was.**

It took building from nothing and LOOKING. The screen grab is the whole reason this was found:

```
document-02-authored.png →  "Something went wrong · This page failed to load"
```

**It also explains three dead ends** that had been chased as separate problems:
`drive-control-reachability` reporting zero controls on all three surfaces, and the demo finding no
`[data-node-id]` and no Grid toggle. None were reachability problems. The page was crashing.

**Class — a harness corpus that cannot contain the bug.** Distinct from B118 (a fixture too thin to
falsify a check): here the fixture was rich, and every row in it had been written by a path that
happened to set the field. The corpus was not small, it was *unrepresentative* — and a sweep over it
proves only that the paths already exercised still work. The fix for that class is not more
assertions; it is authoring from nothing, the way a customer does.

After, from the same demo on a rebuilt sandbox:

```
document  27 nodes rendered · 15 controls visible · grid captured
deck      10 nodes · ribbon reachable · grid captured
exports   docx 44KB · pdf 100KB · pptx 136KB · pdf 65KB — all 200
```

Registered as `canvas-demo` in the branch suite so the from-scratch path is exercised every run.

---

## B121 · A deck the customer opens is missing rows and bullets the author wrote · FIXED
**Where** `lib/export/pptx-exporter.ts` — `table`, `bulleted_list`, `text_block`, `text_box`, `callout`, `blockquote`

Export a five-slide capability brief with a three-row risk register. Open the `.pptx`. **The third
risk is not there**, and the second is cut off mid-word:

```
Thermal gradient during cure exceeds the window in cold-weather
basing                                                            ← clipped
┌────────────────────────────────────────────────────────────┐
│ Ask: range access at two field windows, and the theatre…   │   ← drawn over the gap
```

The deck writer flows body nodes by asking each one how tall it is and advancing by that much.
Several of those answers never read the text:

| node | it said | which describes |
|---|---|---|
| `table` | `rows * 0.36` | a table where every row is one line |
| `bulleted_list` | `items * 0.42` | a list where every bullet is one line |
| `text_block` | `length / 95` | one font size at one width |
| `text_box` · `callout` | `length / 60` | ditto |
| `blockquote` | `length / 65` | ditto |

A wrapping cell is taller than that, so the declared frame was short — measured at **0.90in short**
for the table and **0.71in** for the list. PowerPoint does not spill a table onto the next slide; it
CLIPS at the frame. Two different symptoms from one cause:

* the **table** grew past its frame and was cut — the third row never rendered at all;
* the **list** stayed inside its frame and the opaque callout beneath it was painted on top — the
  third bullet is in the file, drawn, and invisible.

**Why nothing caught it.** Every row's text is emitted faithfully into the slide XML. The bytes are
complete, the node-vocabulary probe finds all 22 types, the export gate reports no violations, the
ruler counts the slides correctly. All of that is true and the delivered file is still missing
content, because the loss happens at RENDER. It was found by converting the deck with LibreOffice
and looking at the page.

This is the direction B64 forbids. On a page an under-count clears a volume that is over its agency
limit; on a slide it deletes content from what the agency receives.

**Fix.** `wrappedLines` is now exported from `canvas-document.ts` — the page ruler's own
character-advance model, with its calibrated 8% safety margin — and the deck writer uses it for all
six node types. A deck and a document can no longer disagree about how tall the same paragraph is
(B112: one ruler, one answer). The table additionally declares per-row heights via pptxgenjs `rowH`,
so the renderer never has to grow a row past a total it was not told about.

**A CORRECTION THIS REPO WAS CARRYING.** `render-artifact-pages.mts` stated that "LibreOffice will
not open the .pptx this product writes." It does. The container had `libreoffice-core` with no
filter packages, so `soffice` failed on *everything* — including a plain text file, which is the
control that should have been run before drawing any conclusion about our output. One `apt-get
install libreoffice-impress` later, our decks convert cleanly. The note blamed the product for a
gap in the tooling, and it had been believed for long enough to keep an entire class of defect
invisible.

**Four instruments, three of them wrong.** Kept because each was convincing:

1. **Declared geometry** (`<a:off>`/`<a:ext>` from the XML) — always clean. The writer leaves a
   tidy gap under the frame it believes in. The frame is the lie.
2. **Ink position** on the rendered page — caught the table (it grew past its frame), passed the
   list (it did not). A tick on a slide that had lost a third of its content.
3. **Text presence** in the rendered page's text layer — found every authored phrase *including the
   invisible one*. Occluded text is still painted into the PDF. A check that cannot tell "on the
   page" from "under an opaque box" cannot answer the question.
4. **Node height, measured in isolation** — works. Export the node ALONE on a slide with the whole
   body band free, render it with an engine that shares no code with ours, and compare the ink span
   against what the writer declared.

And the fourth was wrong once too: its first version computed `inkBottom - BODY_TOP`, which charges
the vertical-balance offset to the node as if it were content height, and reported every node type
under-declared — including pages that were visibly perfect. Measuring the ink SPAN fixed it. A
boxed node (its own fill or border) is reported INDETERMINATE, never green: there the ink measures
the box, so declared and realised agree by construction.

**Proof.** `scripts/probe-deck-overlap.mts`, red on the shipped writer (table −0.90in, list
−0.71in) and green after. `__tests__/unit/pptx-wrapped-height.test.ts` asserts the property the old
code could not satisfy at any tolerance — that height is a FUNCTION OF THE TEXT — since
`rows * 0.36` returns the same number for a one-word cell and a paragraph. Two of its six cases are
marked as NOT having caught the original bug; they guard declared geometry, which was never
violated.

**Also fixed alongside** (both found the same way, by looking at a rendered deck):

* **Slides were top-anchored.** Body content began at `BODY_TOP` regardless of volume, so a
  two-line slide sat on five inches of white. Now optically centred where genuine slack exists —
  0.38 of it, above true centre — and a dense slide is not moved, because nudging one down would
  push content off the frame that currently fits. `__tests__/unit/pptx-vertical-balance.test.ts`.
* **The title slide's subtitle was pinned to `y=3.0`.** The hero path returns before the body flow
  and never got the rule. Same fix, so the deck is internally consistent.

The vertical-balance test was itself wrong first, in this codebase's recurring shape: `isTitleSlide`
fires on a level-1 heading with `text_block` siblings, which described every deck it built, so it
measured the hero path — a hardcoded subtitle at y=3.0 and an accent rule at y=7.22 — and reported
three greens about code it never ran. `assertContentSlide` now refuses to measure a hero slide.

---

## B122 · The collaborator invite flow was dead — the page was public, the API it lives on was not · FIXED
**Where** `middleware.ts` — `PUBLIC_EXACT_PATHS`

Invite an external collaborator. They receive `/invite/<token>` and open it, having no account —
which is the entire premise of an invite link. The page renders, and then:

```
GET  /api/invite?token=…   →  401 {"error":"unauthenticated"}
POST /api/invite           →  401 {"error":"unauthenticated"}
```

The first 401 lands in a `catch {}` the page treats as non-critical, so the invitee sees no inviter,
no company and no proposal title — a blank courtesy screen rather than an error. The second is the
one that matters: it is the request that sets their password, and it never reached the handler.

The route knew what it was. Its own header, unchanged since it was written:

> `Public, token-gated collaborator-invite flow (the token IS the credential, no session yet)`
> `No auth required — the token itself is the credential.`

`/invite` (the PAGE) was in `PUBLIC_PATHS`. `/api/invite` matches neither `pathname === '/invite'`
nor `startsWith('/invite/')`, so every request to it needed a session — from the one caller who by
definition does not have one.

**This is a repeat, and the first instance is documented four lines above the bug.** The
`CRON_EXACT_PATHS` block in this same file explains at length that two cron routes were written to
authenticate by bearer token and were made *unreachable* by the session gate in front of them —
"the check runs first, so a correctly-authenticated cron poke got `{"error":"unauthenticated"}`
before the handler was ever entered". That was found, fixed for those two paths, and **never swept
for other instances of the same shape.** `/api/invite` is the other instance: a handler that
authenticates by something other than a session, sitting behind a gate that only understands
sessions.

**Why no test caught it.** `__tests__/middleware.test.ts` asserts `/invite/*` is public — the PAGE.
`__tests__/invite.test.ts` calls the route handler directly, which never runs middleware. Both
passed. The defect existed only in their COMPOSITION, and nothing composed them.

**Fix.** `/api/invite` joins `PUBLIC_EXACT_PATHS` — exact, never a prefix, so `/api/invite/anything`
stays gated. Because it is now an unauthenticated endpoint that accepts a token and **sets a
password**, it also joins `RATE_LIMITED_PATHS` at 5 per 15 minutes, matching `reset-password`: the
limit is what stops an open path from being a token-guessing oracle. A real invitee needs two
requests. Regression tests assert both halves together, and both directions of the exactness.

---

## B123 · A malformed invite token read as a server fault · FIXED
**Where** `app/api/invite/route.ts` — GET and POST

Found on the FIRST anonymous request after B122 opened the path:

```
GET /api/invite?token=abc  →  500 {"error":"Internal error","code":"DB_ERROR"}
```

The token IS a `proposal_collaborators.id`, and that column is `uuid`. `WHERE pc.id = 'abc'` makes
Postgres raise `invalid input syntax for type uuid` before the query runs; the route's catch turns
that into `500 DB_ERROR`. A typo in a URL — the single most likely thing to go wrong with a link
someone pastes out of an email — was reported as the server breaking.

**Fix.** Validate the token is a UUID before either handler queries, and answer the SAME 404 as an
unknown token. Identical answers for "malformed" and "not found" are deliberate now that the
endpoint is unauthenticated and rate-limited: distinguishing them would confirm which tokens are
well-formed. The POST guard also runs before `bcrypt.hash`, so a bad token costs nothing.

**The pattern worth naming: fixing one layer exposes the next.** This defect was unreachable while
middleware refused the route, and it appeared within a minute of opening it. That is not a reason
to be cautious about the first fix — it is a reason to re-run the probes immediately after one,
because the layer underneath has never been exercised by a real caller.

---

## B124 · The layer that answers first for every API call did not carry `code` · FIXED
**Where** `middleware.ts` — the 401, and both 403s

CLAUDE.md: *"EVERY error response MUST include both `error` and `code` fields."* Every one of the
250 route handlers obeys it — **2,525 error responses, all conforming**, measured by parsing each
`NextResponse.json` call in the tree. The middleware in front of all of them did not:

```
{"error":"unauthenticated"}          401  — any API call without a session
{"error":"password change required"} 403  — any API call on a temp password
{"error":"forbidden"}                403  — any API call above the caller's role
```

A client switching on `code` fell through to its default on **the most common failure in the
product**, and nowhere else. The tell that this was an oversight rather than a decision: the two
rate-limit branches in the same file already carried `code: 'RATE_LIMITED'`.

**Why nothing caught it, and it is a scope story, not an oversight story.** `verify-api-contract`
drives every route through a real signed-in session — deliberately, and its header argues the case
well: grading an authed route anonymously answers a different question. The consequence, never
written down, is that **no lens ever saw a middleware 401.** The unit assertions checked `json.error`
and never `json.code`. Uncovered, not passing.

**Fix.** `UNAUTHENTICATED` / `PASSWORD_CHANGE_REQUIRED` / `FORBIDDEN`, matching the handlers' own
vocabulary so a caller needs one branch and not two. `error` strings are unchanged, so the existing
assertions still hold. Proven red-first: the three new tests fail on the unfixed file with
`expected undefined to be 'UNAUTHENTICATED'`.

Fixed alongside, same class: `app/api/command/seen/route.ts` answered `code: 'unauthenticated'` /
`'bad_request'` / `'server_error'` in lowercase where the whole tree uses SCREAMING_SNAKE. Not a
missing code — but a caller matching `code === 'UNAUTHENTICATED'` misses it just as completely.

---

## B125 · Two lenses reporting green over a scope smaller than their claim · FIXED
**Where** `frontend/scripts/verify-api-contract.mjs`

Its closing line is *"every reachable GET honours the response contract."* It walked two directories.

```
104 called + 12 unbound  =  116
GET routes on disk       =  130
```

Fourteen routes were never enumerated, never called, and never listed as uncalled — including the
whole `/api/partner/*` console, `/api/enter`, `/api/events`, `/api/health` and `/api/invite`. The
arithmetic was visible in its own output and nothing compared the two numbers.

Two causes, both the documented class:

1. **Two hardcoded roots.** `app/api/portal` and `app/api/admin`, one actor lane each. Everything
   else was outside the walk by construction.
2. **One export form.** `/export\s+(async\s+)?function\s+GET/` does not match
   `export const GET = withHandler({…})`, so `/api/admin/system` — the master_admin-only one — was
   skipped in silence. **B74 exactly**: a matcher that cannot see a construct reports the files
   containing it as clean.

**Fix.** Walk the whole tree, match both forms, four actor lanes (anonymous · tenant · partner ·
admin) — and an **accounting assertion**: every enumerated route must end up graded, exempt, unbound
or actor-less, or the lens exits 2 as a HARNESS DEFECT before printing any verdict. A coverage claim
that does not reconcile against the tree is a claim about the harness.

**The widened lens then produced five reds, and four were the harness.** Kept, because the ratio is
the lesson — *a new harness's first output describes the harness*:

| red | what it actually was |
|---|---|
| `/api/invite` 401 without `code` | **real** — B122 + B124 |
| `/api/health` 200 without `{data}` | THE ONE documented exception (docs/API_CONVENTIONS.md §55) |
| `/api/enter`, `/api/partner/{enter,exit}` "Failed to fetch" | redirect-only navigation endpoints — 0 `NextResponse.json` between them |

All four are now EXEMPT with the reason printed, not filtered out of the walk.

---

## B126 · An upload endpoint reported a storage failure for a malformed request · FIXED
**Where** `app/api/portal/[tenantSlug]/uploads/image/route.ts`, `app/api/admin/documents/upload-image/route.ts`

`await request.formData()` throws when the body is not multipart — a wrong content-type, a truncated
upload. Unguarded, it fell to the outer catch:

```
POST {}  →  500 {"error":"Image upload failed","code":"STORAGE_ERROR"}
```

The caller is told storage broke when the caller sent a bad request, and the ops dashboard records a
storage incident that never happened. Two lines below, a MISSING file is correctly `422
VALIDATION_ERROR` — an unparseable body is the same class of client mistake and now gets the same
answer.

Fixed alongside: `app/api/stripe/webhook/route.ts` answered `500 DB_ERROR` for absent Stripe keys.
Nothing on that path touches a database and the condition is a deployment gap, not a fault; it also
disagreed with `stripe/checkout` and `stripe/portal`, which have always answered
`STRIPE_NOT_CONFIGURED`. Now `503 STRIPE_NOT_CONFIGURED` — a deliberate status change, safe for the
one caller that matters because Stripe retries the whole 5xx class, so events still replay once the
keys are set instead of being swallowed.

**The instrument is the deliverable: `frontend/scripts/verify-write-contract.mjs` (lens 5).** No
lens walked a write verb — 213 of them — because calling every POST mutates the box being measured.
That is defensible and was never written down, so three green lenses read like a verified API. The
new lens binds every `[param]` to a fresh UUID owning nothing and asserts the one property that
needs no successful write: **a client error answers 4xx with both `error` and `code`, never 500.**

It reported 19 violations on its first run and **16 were its own expectations being wrong** — the
same ratio as B125, from the same cause. `POST /api/admin/promo-codes` documents every field as
optional and mints a code with defaults, so `{}` is its intended input and 201 is the contract
working; unpinning an unpinned card is idempotent. Calling those violations would have been the
"assert the contract the system HAS" mistake at scale, so a 2xx at a nonexistent id is now recorded
as an OBSERVATION and printed, never failed on.

And it reproduced a bug this repo had already written down: it sliced the response body to 400
characters **before** `JSON.parse`, so `/api/applications` — which answers a textbook 702-byte
`{error, code, details}` — was graded unparseable. `verify-api-contract`'s own header describes
that exact failure (38 well-formed responses reported as "not JSON"). The lesson was recorded and
still repeated, which is an argument for putting it in the shared grader rather than in prose.

**Honest limitation, stated in the header:** the lens is **not read-only**, because several routes
take no required input by design and duly did their work. Its footprint on a fresh box is printed
every run (sweep/audit/mint tables only). Sandbox, never production.

---

## B127 · The surface lens called four healthy monitor pages broken, because they display error text · FIXED
**Where** `frontend/scripts/lib/error-surface.mjs`

`verify-surfaces` reported, reproducibly:

```
78 surface(s) driven · 74 clean · 4 broken
  · /admin/process            — error boundary
  · /admin/system             — error boundary
  · /admin/system-state       — error boundary
  · /portal/foundation/activity — error boundary
```

All four render perfectly. They are **event monitors** — their job is to display `system_events`
rows — and the shared matcher's `\bnot found\b` was matching the payload they were displaying:

```
{ "code": "NOT_FOUND", "message": "proposal not found" }      ← inside a <pre>, on /admin/process
proposal not found                                            ← inside an event row, on /admin/system
```

The matcher confused **the page's own message** with **error text the page is displaying as data**.

**The consequence is worse than four wrong rows.** On a freshly migrated database these pages pass,
because no error events exist yet. After any real failure anywhere in the product they fail forever.
So the lens would go permanently red — pointing at the four pages an operator uses to find real
problems — for the crime of the product having logged an error it was designed to log. It passed
this morning only because the box was hours old.

**Found by a harness littering the corpus another harness measures** — B119's shape exactly. The new
write-verb lens calls every route with ids that own nothing, which legitimately produces `NOT_FOUND`
events. It did not break these pages; it put the first error rows on a clean box and made a latent
false positive visible on the very next run.

**The discriminator is structural, and was measured rather than guessed.** An error surface REPLACES
a page's content; a monitor displaying an error still has all of its own:

| page | body text | where the match lives |
|---|---:|---|
| `/portal/<t>/library/foundation/<bad-id>` | 468 ch | `H1` "Not found" |
| `/admin/tenants/<bad-id>` | 638 ch | `H1` "Tenant Not Found" |
| `/admin/process` | 4,167 ch | `PRE`, inside a payload |
| `/admin/system` | 8,262 ch | `P`, inside an event row |
| `/portal/<t>/activity` | 11,685 ch | `PRE`, inside a payload |

**Fix.** The vocabulary splits in two. **Strong** texts ("Something went wrong", "Application
error", "This page could not be found") are never legitimate content and count anywhere. **Weak**
texts ("not found", "failed to load") count only when the page has nothing else: the match is in a
heading, or the page is shorter than the measured floor. Matches inside `pre`/`code` never count —
serialized data rendered verbatim is not a page's own message. The floor (1,500 ch) sits in the gap
above with ~2× clearance either side, and the heading arm means neither test has to be exact. The
documented `/admin/documents/[documentId]` case — a red `<p>`, not a heading — is still caught, by
the short-page arm.

**The instrument now proves itself before it is believed.** `verify-surfaces` opens each actor lane
by driving a route that is DEFINITELY an error surface and requiring a non-zero count; if the
detector cannot see a known-broken page, the run exits 2 with *"every clean below would be
unearned"* instead of printing 78 confident greens. This detector has now been wrong in BOTH
directions — too narrow (an error page captioned as a working screen in the admin guide) and too
broad (this) — which is the argument for the preflight: the vocabulary will be edited again.

Verified in both directions: the preflight fires (exit 2) on a deliberately blinded detector, and
the lens returns **78 driven · 78 clean · 0 broken** with it restored.

---

## B128 · `pip install -r requirements.txt` stopped a third of the way in, and the whole AI half went quiet · FIXED (environment)
**Where** the fresh-box setup, not the product

`pipeline/requirements.txt` line 23 is `anthropic>=0.40.0`. On a fresh container it was not
installed, and neither were `boto3` and `pymupdf4llm`. The install had reported success.

What actually happened:

```
ERROR: Cannot uninstall cryptography 41.0.7, RECORD file not found.
       Hint: The package was installed by debian.
```

pip hit a Debian-managed `cryptography` it could not replace and **aborted** — so every requirement
after that line was skipped. Two things then hid it:

1. the command was `pip install -q -r requirements.txt | tail -5`, so `$?` was **tail's** status,
   not pip's, and a failed install exited 0;
2. the consequence is invisible by design. The agent fabric **safe-skips** rather than dead-ending a
   workflow, so every workflow still reported `completed`:

```
[WARNING] pipeline.workflows.actions.draft_v0: dependency import failed — skipping: No module named 'anthropic'
[WARNING] pipeline.workflows.processor: AI_INVOKE action 'tool.opportunity.scout' skipped (fabric=False)
[ERROR]   pipeline.workflows.actions.cms_content: AI generation failed, falling back to brief: No module named 'anthropic'
```

**This is B115's shape on a fresh box.** B115 was the entire agent workforce inert because
`anthropic 1.0.0` removed a keyword argument; nothing surfaced it because the fabric safe-skips.
Same invisibility, different cause — the SDK absent rather than mis-called. Two of 39 branch drives
caught it (`opp-scout`, `cms-generate`), and only because they assert on the AI half specifically;
the other 37 passed with the workforce doing nothing.

**Fix.** Install the three, restart the worker (it imports at module load, so a running worker keeps
the failure), and the drives pass with a real tool-loop recorded — `rounds=2 · tool_calls=2 ·
tokens=365 · cost_usd=0.0008`. Recorded in docs/CONTINUATION.md §2 with the rest of the
from-nothing recipe.

**The rule worth keeping:** never pipe an installer into `tail`. And when a drive that asserts on AI
behaviour fails while everything else is green, suspect the SDK before suspecting the product —
safe-skip means "quiet", not "fine".

---

## B129 · Two drives failing on things the product never promised · FIXED
**Where** `frontend/scripts/probe-page-scale.mts`, `frontend/scripts/probe-structural-nodes.mts`

The last two of four red drives on a fresh box. Neither was a product defect.

**`page-scale` died with `TypeError: Cannot read properties of undefined (reading 'email')`.** It
took the newest `tenant_documents` row and then looked for a `tenant_admin` in that row's tenant. On
a fresh box the only document belongs to **`rfp-pipeline`, the house tenant, which has zero
tenant_admin memberships by design** — it is the platform's copy-forward shelf, not a customer. The
`!doc` case was guarded; the `!member` case was not, so a missing guard read as a broken product.

It could not have run inside the suite even with the guard: **`drive-canvas-authoring` runs
immediately before it and ends with `cleanup: 5 tenant_documents`** — correctly, because a scenario
that builds its own world should take it away. So the drive before it deletes exactly the row this
one needs. That is **B103's shape** (a self-disposing scenario erasing what another instrument
depends on), and the cure is the same pattern rather than a complaint about it: the probe now
**authors its own document through the product's own POST**, measures, and removes it — 0 leftovers.
Document and actor are now chosen in ONE query, so the pair cannot be split again, and if nothing
matches it reports NOT MEASURED instead of crashing. Result: all four viewports "one scaling".

**`canvas-structural` reported "toc renders its entries in the PDF" as broken.** The TOC works.
`buildTocHtml` **deliberately drops the first level-1 heading** — it is the document's own title,
and "every real contents page omits" it. The probe counted occurrences of exactly that heading, so
it asserted `2×` for the one entry the design guarantees will be `1×`.

Verified against the renderer before touching anything — the TOC block reads
`Table of Contents · Second Chapter Heading · 1`, an entry, indented, with its page number. The
probe now counts the h2 (2× with a toc, 1× without) and **asserts the title omission as its own
check**, so a future change that starts listing the title fails here instead of passing quietly.

**Both are CLAUDE.md rule 4 — assert the contract the system HAS.** The same mistake as expecting a
bucket `DELETE` to remove the row when deactivation is the design. Across this whole sweep that rule
accounted for more red than every real defect combined.

**Standing after the fixes, confirmed by a full re-run from a clean output directory: 39 drives,
39 pass, 0 fail, 0 could-not-run — and zero of the four failures was a product defect.**

---

## B130 · Three more pages for logged-out people, behind the login gate · FIXED
**Where** `middleware.ts` — `PUBLIC_PATHS` / `PUBLIC_EXACT_PATHS`

Found by driving **every** route rather than the two subtrees `verify-surfaces` walks. All three
answer to the same description as B122: *a page whose entire audience cannot log in, requiring a
login.*

**The password-reset flow was a closed loop.** `/login` renders a "Forgot password?" link. Clicking
it redirected to `/login`:

```
/forgot-password                        307 → /login?from=%2Fforgot-password
/reset-password?token=…&email=…         307 → /login?from=%2Freset-password%3Ftoken%3D…
```

The second is the worse one: it is the URL a password-reset **email** points at, so every reset
link ever sent landed on a login wall and the page that consumes the token never ran. Nothing else
in the flow was broken — both pages exist and read their query parameters correctly, and
`POST /api/auth/forgot-password` answers `200 {"data":{"sent":true}}` because `/api/auth/*` is
public. The API was reachable; the UI in front of it was not.

**`/federal-rd-101` is the commercially expensive one.** It is the newcomer on-ramp — its own
content module says so — and it is linked from the homepage **twice** ("Start here →" and the hero's
"New to federal R&D? Start here") and listed in the site nav as "Federal R&D 101". Every one of
those entry points sent a first-time visitor to `/login?from=%2Ffederal-rd-101`.

**The real defect is structural, and so is the fix.** A hand-maintained array of public paths lives
next to a DIRECTORY of public pages and nothing compares them, so every new marketing page is
public-by-intent and gated-by-default until a human notices. Four omissions found in one sweep
(`/api/invite` plus these three), each by a different accident.

Middleware runs on the Edge and cannot read the filesystem, so the array stays hand-written. The
other half now exists: `__tests__/middleware.test.ts` walks `app/(marketing)` and `app/(auth)` from
disk, drives every page through the real middleware with no session, and fails naming any that
redirects to `/login`. `/change-password` is the one stated exception — it needs a session by
design, pinned from the other direction by its own test. Adding a marketing page without listing it
now breaks the suite instead of the site.

Proven red-first: removing `/federal-rd-101` from the list fails with
*"these public pages redirect anonymous visitors to /login: /federal-rd-101"*.

---

## B131 · Two routes reached around the storage abstraction, and one of them is customer-facing · FIXED
**Where** `app/api/admin/storage/route.ts`, `app/api/portal/[tenantSlug]/proposals/[proposalId]/dropbox/route.ts`

The admin Storage Manager rendered a red banner:

```
Failed to list storage objects   [Dismiss]
```

`lib/storage/s3-client.ts` exists so nothing else talks to S3 directly; its header states the
contract — with `STORAGE_DRIVER=local` the SAME helpers are backed by the filesystem. The local
driver is implemented in that file **and nowhere else**. Both routes imported the proper helpers
*and also* built raw `ListObjectsV2Command` / `HeadObjectCommand` / `DeleteObjectsCommand` and called
`s3.send` directly — so on any local or dev deployment they went straight to AWS with no credentials
and got `InvalidAccessKeyId`.

The dropbox one is worse than the admin one: it is the **customer-facing** proposal dropbox, so a
collaborator saw an error where their uploaded files were.

**Why the gap existed, and why that shaped the fix.** The abstraction had no recursive listing, no
stat and no bulk delete — exactly the three things a folder rename, an upload verification and a
folder delete need. Callers did not go around it out of carelessness; they went around it because it
was missing. So `listObjectsDeep`, `objectStat` and `deleteObjects` were added, and both routes now
use them exclusively. **Zero files outside `lib/storage/` import the AWS SDK.**

Two latent bugs fell out of writing them: the raw list calls were **un-paginated**, so a folder
rename or delete would have silently processed only the first 1000 objects; and the upload path
issued two `HeadObject` calls where one stat does.

**Why nothing caught it.** The page answered HTTP 200. The failing fetch returned a textbook
`{error:'Failed to list storage objects', code:'STORAGE_ERROR'}` — so the envelope lens was
*satisfied*, correctly. The rendered text says "Failed to **list**", which is not in the
error-surface vocabulary ("failed to load"), so `verify-surfaces` scored it clean. Every instrument
was right and the page was visibly broken. **It was found by looking at a screenshot.**

**Guard:** `__tests__/storage-abstraction-boundary.test.ts` fails if anything under `app/` or `lib/`
outside `lib/storage/` imports `@aws-sdk/client-s3` or calls `s3.send`, and asserts the abstraction
still exports what the callers needed. It also asserts its own walk found >300 files, because a
green from an empty list is not a pass.

---

## B132 · The UI atlas harness, wrong four ways before it was right · FIXED
**Where** `frontend/scripts/catalog-ui.mjs`, `capture-ui-atlas.mjs`

Recorded because the ratio is the finding: a new instrument's first output describes the instrument.

1. **250 API routes counted as UI components.** The walk took every `.ts` under `app/`, so
   `route.ts` files joined the component population — 438 "components" instead of 188 — and then
   every one was reported an ORPHAN, because an API route has no render path by definition. The
   orphan list is the entire point of that section, and it was 265 rows of noise.
2. **A layout is a terminal in the render graph, and it was not.** Walking up only to a `page` meant
   every component rendered by a layout — the admin nav, the portal notification panel, the
   shadow-space banner, the marketing mobile menu — resolved to NO route and was listed as a
   deprecation candidate. Nine of fifteen remaining "orphans" were chrome that appears on every page
   of a tree. After both fixes: **one** orphan, a 6-line barrel re-export that is alive.
3. **The portal id binding was not tenant-scoped** — so the immobileyes lane was handed FOUNDATION's
   portal and drove `/portal/immobileyes/portals/<foundation-portal>`. The product refused it exactly
   as it should; the harness photographed the refusal and reported the page **broken**. That was the
   single red in a 150-shot run, and the note warning against precisely this (B91) is thirty lines
   above the bug, written in the same sitting. Knowing the rule is not enough.
4. **Two wrong password constants in a row** cost the `/vaults` lane twice: `TENANT_PW` (never
   applied to lighthouse) then `LIGHTHOUSE_PW` (belongs to `eric@lighthouse.com`, a different
   account). It is `COLLAB_PW`, which `scripts/sandbox-env.sh` — the file whose stated purpose is to
   hold these in one place — did not export. Now it does.

Each was caught by checking a claim against the source before believing it. The one that matters
most is (3): it would have been reported as a product defect in a document about product defects.

---

## B133 · The primary navigation was announced as a dialog, on every page · FIXED
**Where** `components/ui/drawer.tsx` (+ `components/ui/nav-shell.tsx`)

`Drawer` set `role="dialog"` unconditionally:

```jsx
<aside role="dialog" aria-modal={open ? true : undefined} …>
```

The nav rail is a `Drawer` with `inlineAt="lg"`, and at or above that breakpoint it is **not an
overlay at all** — it is the static 256px navigation column of every admin and portal page. So a
screen-reader user met a **dialog** where the site's main navigation should be, on every page, and
the navigation was absent from landmark navigation, which is the standard way they would jump to it.

Measured at rest on a page nobody had opened anything on:

```
/admin/events   role=dialog  256 × 26045  bg rgb(26,24,22)   ← the nav rail
/admin/tenants  role=dialog  256 × 1665   bg rgb(26,24,22)   ← the nav rail
```

**Found by a harness, not by reading.** A new overlay probe kept reporting one persistent
`role="dialog"` present before any interaction, which is exactly what this defect looks like from
the outside. Nothing that reads source would have flagged it — the line is unremarkable, and the
component's own header describes the two modes correctly. It is the *rendered* semantics that were
wrong, and only something asking the DOM what it claims to be could see that.

**Fix.** The role follows the behaviour: `role={!inlineAt || open ? 'dialog' : inlineRole}`. A
drawer with no `inlineAt` is always a real overlay and keeps the dialog role. An `inlineAt` drawer
is a dialog only while genuinely open as an overlay — and `open` is only ever true BELOW the
breakpoint, because the hamburger that sets it is `lg:hidden`. At rest it takes the new `inlineRole`
prop, and `NavShell` passes `navigation`, so the rail becomes a labelled navigation landmark.

**The fix was wrong once, in the safe-looking direction.** The first version also dropped
`aria-modal` for `inlineAt` drawers — which would have demoted the MOBILE nav from a modal to a
plain region *while it covers the page*. That is the case the middle test now pins.

Proven red-first: two of four assertions fail on the unfixed component with
*"the inline nav rail must not be announced as a dialog"*.

**One config change came with it.** `__tests__/drawer-landmark-role.test.ts` is the first test here
to render a component, and it died with `ReferenceError: React is not defined` — the components use
Next's AUTOMATIC JSX runtime while vitest's esbuild defaults to the classic one. `vitest.config.ts`
now sets `esbuild: { jsx: 'automatic' }`, which aligns the two and changes no test's scope. The
test itself is written with `createElement` rather than JSX, because the suite's include pattern is
`__tests__/**/*.test.ts` and widening it for one file would change what every future run picks up.

---

## B134 · The measurement grid was offered on the main authoring surface and wired to nothing · FIXED
**Where** `components/canvas/fluid-document-view.tsx`, `components/canvas/canvas-editor.tsx`

The canvas OverlayLayer offers a **Grid** chip — *"Measurement grid in points — inch lines, margin
box, page ruler"*. On the **default tab of the proposal workspace**, the surface an author spends
their day in, it toggled, lit up, reported `aria-pressed="true"`, and drew nothing at all.

Confirmed four independent ways before a line was changed:

1. `fluid-document-view.tsx` contained **zero** references to `grid`.
2. It renders `<CanvasRenderer>` without the `grid` prop; `CanvasRenderer` defaults `grid = false`.
3. `app/globals.css` has **0 rules for `ov-grid`** — so the class `overlayClass()` adds is inert.
   (`ov-sections` 2, `ov-atoms` 2, `ov-groups` 14, `ov-provenance` 9, `ov-grid` **0**.)
4. Measured: **0.1%** of the canvas changed when toggled, against **2.08%** on a blank letter where
   the same layer works.

**The same defect on decks, from the other direction.** `canvas-editor.tsx` offered Grid for slide
documents, and `SlideEditor` has no concept of a measurement grid — zero references. That chip was
filtered out; the fluid view's was wired up, because the grid is genuinely useful on an assembled
eleven-page document and hiding it would have been the lesser fix.

**This is the failure the file itself names.** `canvas-overlays.tsx` already filters `groups` with
the comment: *"A toggle that provably paints nothing is worse than an absent one."* The rule was
written, applied to one layer, and never extended to `grid`.

**Proven live, by the same measurement that found it:** fluid-document Grid **0.1% → 2.33%**,
matching the 2.03–2.08% every other doc surface paints, and the deck's chip is gone.

### The instrument, which was wrong three times first

`scripts/drive-canvas-overlays.mjs` walks seven layers across six surfaces — the fluid document, a
proposal section, and one blank canvas per preset (`letter`/`deck`/`sheet`/`flier`, **authored
through the product's own POST because this fixture stores only `letter`**, then removed). It
verifies each chip via `aria-pressed` *and* asserts the layer actually PAINTED. Three corrections
were needed before its output meant anything:

| wrong | why | fix |
|---|---|---|
| shot the viewport at scroll-0 | the overlay bar sits ~900px down under the Studio card; ten shots captured chrome with the canvas below the fold | scroll the chip bar to the top first |
| whole-frame diff against a 0.35% floor | a 1px dotted outline is a vanishing fraction of a frame — the Atoms layer paints beautifully and measures **0.21%** | exclude the chip bar, then any change is paint |
| "no paint" on an empty canvas | a blank deck has no primitives to outline; the layer was working | require the layer's prerequisite DATA before calling it dead |

The third correction had a second half. Provenance was reported dead on a section that had two
nodes — but `globals.css` paints a gutter only for `ai_draft`, `library` and `imported`, and both
nodes were `manual`. The prerequisite has to match **what the stylesheet keys on**, not what sounds
right; otherwise the check measures a rule the product does not have. First run: 12 findings, 1
real. After the three fixes: exactly the one.

---

## B135 · Purchase history could not tell two purchases apart, while the answer sat computed in a route nothing called · FIXED

`GET /api/portal/[tenantSlug]/purchases` has always LEFT JOINed `proposals` and `opportunities` to
return `proposal_title` and `opportunity_title` beside each row. Nothing had ever called it. Both
surfaces that show purchase history — `app/portal/[slug]/billing/page.tsx` and `…/manage/page.tsx` —
ran their own bare `SELECT id, product_type, amount_cents, status, created_at, opportunity_id` and
handed it to the same `BillingPanel`, which rendered Product / Amount / Status / Date.

So a customer with three portal purchases saw three rows reading `Proposal Portal (Phase I) · $0.00 ·
Completed`, **two of them on the same date**, with nothing to distinguish them. Which build was
which was unanswerable from the billing page — and had been computed, correctly, in an endpoint with
no caller for as long as both had existed.

Found by the capability reconciliation (`frontend/scripts/reconcile-capability.mjs`, join 1) and
confirmed in `docs/ui-atlas/tenant__portal-tenantSlug-billing.jpg` — a screenshot taken before any of
this started, showing the three identical rows.

Fixed by giving both page queries the joins **copied from the route** rather than rewritten, so the
three implementations of the same read cannot drift on scoping or ordering. The panel shows the
proposal title, falls back to the opportunity title (a comp-code purchase creates the `purchases` row
before the portal is provisioned, so `proposal_id` is null and the opportunity is the only thing that
can name it), and adds nothing when a purchase bought no particular thing (consulting hours).

Proven live: `docs/ui-surfaced/billing-purchase-history.jpg` now reads `DoW 2026 STTR — Direct to
Phase II: Autonomous Large-Scale Concrete Additive Manufacturing` and `DoW 2026 SBIR — Navy Phase I:
Additive Construction for Expeditionary Basing` on the two same-day rows.

`__tests__/purchase-history-surfacing.test.ts` asserts both halves — the panel renders a title, and
*both pages actually select one*. The second half matters: a panel test alone passes while a page
silently supplies null for every row, which is the shape the defect had.

---

## B136 · A customer's audit trail told them "Shadow descended" · FIXED

`describeEvent()` never drops an event type it does not recognise — it de-punctuates the raw
identifier and title-cases it. That is good fallback behaviour and it is why this was invisible for
so long: the Activity feed is populated, the words are English, nothing looks broken.

But thirteen types that reach a **customer's** feed (a real `tenant_id`, not an admin row) were
arriving as the system's own vocabulary. The worst is `shadow.descended` — an RFP administrator
entering a customer's account through the RLS shadow, recorded deliberately under that customer's
tenant because, as the emit site's own comment says, *"the event belongs to the customer's audit
trail"*. The company that bought a proposal portal was being told, in their own audit log, that a
shadow descended. Also `card.applied`, `review_todos.prestaged`, `workflow.instance_created` — bridge,
prestaging and process-instance terms, in front of a customer.

Found by joining the DISTINCT event types the database has actually emitted with a non-null
`tenant_id` against the labels in `lib/event-labels.ts`. Not readable from the code: the question is
about what has been emitted, and the fallback makes every gap look intentional.

Fixed with labels for all thirteen. `__tests__/event-label-jargon.test.ts` asserts the *property* —
that the label is not the fallback — rather than the strings, which would only re-assert what was
typed. It also guards the guard: an unlabelled type must still fall back.

Two scoping errors preceded the real number, both instructive:

| scope | reported | why it was wrong |
|---|---|---|
| every emitted type vs the map | 194 of 243 | the map is scoped to "the customer-relevant taxonomy" **on purpose**; every admin type is unlabelled by design. It also swept up `widget.frobnicated`, a fixture. |
| types with a real `tenant_id` | 29 | `describeEvent` answers whole namespaces before consulting the map — every `tool` event renders as "AI tool started/completed", so `memory.stored`'s 50 tenant rows were never unlabelled |
| …minus branch-handled namespaces | **13** | correct |

---

## B137 · The page editor kept a full publish history and showed two lines of it · FIXED

`content_pages` stores every version of every marketing page. `getVersions()` returns them
newest-first. `GET /api/admin/site/pages/[pageKey]/versions` serves them. Nothing called it, and
`getVersions` had exactly one reference in the entire repo — that route.

The editor header could only ever say "Live v7 · Draft v8". An editor about to overwrite v8 had no
way to see that v6 had been live for a month, or who published v4, or what the note on v2 said.

Fixed with a collapsed **Show version history** disclosure in `app/admin/site/[pageKey]/editor-client.tsx`,
fetched on first open — version, status, saved, published, author and audit note. Proven live in
`docs/ui-surfaced/admin-site-version-history.jpg`.

---

## B138 · The reconciliation instrument, wrong fifteen ways before it was right · FIXED

The join that found B135–B137 produced a confident, wrong answer at every stage of its construction.
Fifteen distinct shortcuts, each of which reported working, shipped, on-screen features as
unreachable capability — full table in **docs/CAPABILITY_RECONCILIATION.md**. The three that
generalise beyond this instrument:

1. **A dynamically-enumerated list has no names in source, by construction.** Asking "is this
   template named in the UI?" reported 13 of 22 workflow templates as invisible; `/admin/workflows`
   fetches the roster and draws all of them. A source-literal scan against a fetched list is
   *structurally guaranteed* to report every entry as missing. It cannot pass.
2. **`try { … } catch { return null }` around a file read turns a broken lookup into a confident
   negative.** One wrong path segment (`r.file` is relative to `frontend/`, not the repo root) had
   the duplication rule reporting every route as non-duplicated, silently.
3. **A shared import is not a shared answer.** Matching any common symbol claimed the vault
   download route was "duplicated by" a page because both call `isValidUUID`. Requiring the symbol be
   async still matched `resolveVaultAccess` — an async *authorization* resolver. What finally worked:
   auth, RLS, export and validation helpers named individually as cross-cutting, with the reason.

The instrument now self-tests against sixteen hand-verified answers and prints `the reconciliation
below is not trustworthy` on any failure. Unsurfaced-route count across the fixes: **23 → 8**, and
every one of the eight is dispositioned by hand.

---

## B139 · Thirty-one handlers left the event bracket open when they threw · FIXED

The canonical API-route template puts `emitEventStart` inside the handler's one big `try`, closes
the bracket on the success path, and returns 500 from the `catch`. If anything in between throws,
the `start` row is **never terminated**. Thirty-one call sites across 28 files had that shape; the
sandbox corpus carried the proof as two `proposal.created` starts with no `end`.

Not cosmetic. `docs/EVENT_CONTRACT.md` §2 requires an `end` on *every* exit path, and the workflow
engine is built on it: `EventTrigger.matches()` skips events carrying `error` precisely because "a
failed op still emits a terminal phase='end' event (with error set)". A handler that emits nothing
gives the engine no terminal event at all — anything waiting on that operation waits forever with no
signal that it never will arrive. `duration_ms` is computed on `end`, so the failure path had none.

**Why it was written that way is the interesting part**: `const startId` is declared inside the try,
so it is not in scope in the catch. Closing the bracket there was a *syntax error*. Thirty-one sites
made the same forced choice — this was a template problem, not thirty-one oversights.

Fixed in three parts: `withEventBracket()` in `lib/events.ts` so new code cannot drop a bracket;
`scripts/fix-open-event-brackets.mjs`, an AST codemod that hoists the binding to
`let startId: string | null = null` and closes the bracket in the catch; and check 4 in
`event-contract.test.ts`.

That guard is the point. Check 3 already claimed "orphan brackets" — but **per file**: "this file
has a start, does it have an end somewhere?" All 31 passed it, because every file did close the
bracket, on the success path. Check 4 asks per `try`.

One test asserted the old behaviour — `'DB failure inside sql.begin → 500 and emitEventEnd is NOT
called'`, reasoning "the proposal was not created → emitEventEnd signals completion". That reads
`end` as *succeeded*; the contract says *terminated*, with `error` distinguishing them. Updated to
assert the contract.

---

## B140 · The audit that found B139 was wrong ten ways first · FIXED

The first run reported **9 workflows that could never fire and 1 step parked on an event nothing
emits**. All ten were false, and each one was a real emit mechanism the scan did not know about.
The platform has **five** ways to emit an event and only two are ordinary function calls:

| miss | reported | mechanism |
|---|---|---|
| string literals only | `OnRfpUploaded` dead | the type is a **ternary** — `existingSolId ? 'rfp.attached' : 'rfp.uploaded'` |
| string literals only | both `AdvisoryOverlay` workflows dead | **module constants** — `type=_OVERLAY_TYPE` |
| `emitEvent*` calls only | a HITL gate waiting forever | **raw INSERT** — `auth.ts` writes `identity:user.logged_in` directly |
| source files only | 4 scheduled workflows dead | **the shared cron** — the event lives in `pipeline_schedules.source` |
| source files only | `ProjectCollaboration` dead | **the generic launcher** — the event lives in `process_templates.trigger_key` |

The last two are the ones worth remembering: **two of the platform's emitters live in the database,
not in any file**, and that is the design rather than a gap — it is what lets a new scheduled or
launchable workflow exist without a deploy. No source scan can ever see them.

Three more instrument bugs surfaced during verification: a Python function splitter that treated
**nested `def`s** as new functions (so `analyze_section_diff`, which closes its bracket from a
nested `def _end()`, looked like it leaked one); a count that matched **one emit twice** (`_emit_start(`
*and* its `phase="start"` kwarg) making the balanced `draft_v0` look unbalanced; and a self-test
**pinned to a real defect** — "the detector finds `proposals/create/route.ts`" was true and
hand-verified, and became a failure the moment the bracket was closed, leaving only the choice of
deleting the check or keeping the bug. It now carries a synthetic control: two handlers identical
but for the one line that matters, which the detector must tell apart before any clean result is
believed. Same lesson as `verify-surfaces`' RED_CONTROLS, learned again.

Canonical write-up: **docs/AUTOMATION_SPINE_AUDIT.md**.

---

## B141 · Eight workflow notifications named a template the CRM cannot render · FIXED

A workflow NOTIFY step names its template as a **string**; the CRM — a different service, with a
different database — defines one. Nothing compared the two.

Eight of the fifteen named templates existed nowhere. `render_template()` returned None and
`_handle_notification_requested` emitted `system:notification.failed` instead of sending mail. To
the listener's credit that failure is loud rather than silent, but the email does not arrive, and
six of the eight had already been requested in this sandbox — **13 of its 30 notification requests**:

`content_published` · `partner_onboarding_ready` · `content_reshare_ready` ·
`ingest_assessment_ready` · `ops_digest_ready` · `social_queue_ready` · `curation_qa_ready` ·
`amendment_delta_ready`

**This is the second occurrence.** The `TEMPLATES.update({...})` block in
`services/cms/src/templates.py` carries the note from the first: *"absence meant rfp_admin stopped
being notified (the 052 regression)"*. It recurs because neither side fails at boot — the workflow
registers cleanly, the CRM starts cleanly, and the only symptom is mail that never arrives. Two
services, one string, no shared type.

All eight written to the existing `_layout` / `_button` / defensive-`p.get()` pattern, with payload
fields taken from each step's own `input_map` (`_execute_notify` spreads inputs into the event
payload verbatim). Each is rendered from an **empty** payload in the test as well as a populated
one: a template that raises returns None and drops the mail exactly like a missing one.

Guarded by `pipeline/tests/test_notify_templates_exist.py`, proven red on all eight first. It reads
the CRM registry with the **AST**, not a regex — the registry is assembled in two pieces
(`TEMPLATES = {...}` then `TEMPLATES.update({...})`), and a regex over `'name': lambda` matches both
only by luck. Reading just the first dict yields 11 of 29 and would report two-thirds of the working
templates as missing.

Found by join 7 of `frontend/scripts/audit-automation-spine.mjs`. Join 6, added in the same pass,
resolves every step's `action` the way `_execute_action` will at runtime — **all 117 steps land**,
so this was the only interior gap.

---

## B142 · A committed credential for an active tenant_admin, which three cleanups all missed · FIXED

`191_seed_immobileyes_proposals.sql` inserts `admin@immobileyes.test` as a **tenant_admin** with
`is_active = true`, `temp_password = false`, and a literal bcrypt hash. Its plaintext —
`DemoPass123!` — is committed in five driver scripts. Anyone who can read the repository can sign in
to that tenant on any environment the migrations have been applied to.

It is a regression, not an oversight. Migration 124 — "launch_security_rotate_seed_credentials" —
exists to remove exactly this, and migrations 157 and 162 both seed credentials correctly, each
saying so in a comment: *"plaintext delivered out-of-band; temp_password=true forces a reset on
first login"*. 191, sixty-seven migrations after the cleanup, is the only credential-seeding
migration that sets `temp_password = false`.

**Three cleanups have now attempted this class, and the first two were fixed lists:**

| | scope | why it missed |
|---|---|---|
| 124 | `WHERE email IN (three names)` | written before 191 existed |
| 198 | `WHERE password_hash IN (one hash)` | 191 uses a different hash |
| 214 | one name + a shape-based sweep | — |

`__tests__/seeded-credentials.test.ts` is the first thing that generalises: it replays every
migration in apply order and asserts the **property** — no account may end up active, carrying a
bcrypt hash, with no forced reset. It reports statements it cannot attribute rather than narrowing
silently. Red-first: with 214 removed it names `admin@immobileyes.test` and the migration that
introduced it.

The sandbox is unaffected — `sandbox-reset-passwords.mjs` runs after `migrate` in `sandbox-up.sh`,
already targets this exact account, and re-establishes the demo password locally. So every driver
keeps working, and the credential exists only on a box someone deliberately reset. Demonstrated
rather than asserted: 214 was applied, the account went to `hash=invalidated temp_pw=true`, the
reset script restored it, and every subsequent drive signed in normally.

Three parser defects were found while building the guard, each of which would have made it
UNDER-report: a `;` inside a `--` comment split statements (198 documents the bad version it
replaces, semicolon and all); a bcrypt literal in a `WHERE` is a selector, not an assignment, so the
safest migration in the tree read as the riskiest; and statements that can only ever revoke access
were reported as blind spots, which trains a reader to skim the list.

---

## B143 · The agent-flow driver was wrong on its first execution, as unrun scripts are · FIXED

`drive-agent-flows.mjs` had been written, committed, and flagged as never executed — twice, in two
separate hand-offs. On its first real run it died immediately:

```
PostgresError: column "v.content_source" does not exist
  HINT: Perhaps you meant to reference the column "s.content_source".
```

`content_source` (mig 163) is on `proposal_sections`, not `canvas_versions` — it records how a
*section's* current content got there, so an AI-landed section is a section-level fact. The query
had been reviewed, committed and described in two summaries without ever being executed.

Fixed and green. The run then did real work: `OnReviewPhaseRequestedDraft` completed, `proposal_manager`
and `library_seed_suggester` were invoked with `tool:agent.invoked` **start and end** both present,
8 workflow steps started and 8 completed, instances 179→180, events 3899→3923.

The lesson is the one the repo already states and this proves again: **a script that has never been
run is not evidence of anything, however carefully it was written.** Full record:
docs/LAUNCH_READINESS_DRIVE.md.

---

## B144 · A launch could scope an instance to one tenant and point it at another's work · FIXED

`launchTemplate()` accepted an overlay whose `proposalId` belonged to a different tenant than the
`tenantId` the launch was scoped to. The instance was created under tenant A; its agents then logged
their work against tenant B's proposal. **18 rows in `agent_task_log` crossed the boundary** before
the copy-inward invariant checker reported them — after the fact, from a preflight whose finding did
not fail the run that printed `39 passed · 0 failed`.

`launchProjectCollaboration` already refuses an **incomplete** overlay and a **non-UUID** one, with
the reasoning written into the file: *"a presence-only check would let an operator-influenceable
value pass, then SILENTLY null the entity"*. Nothing refused an **inconsistent** one.

That is the harder case, not the easier one. A fabricated uuid fails the first lookup. Real ids from
the wrong tenant pass format, existence and FK checks, and produce a plausible instance doing
cross-tenant work.

Fixed with an `OVERLAY_TENANT_MISMATCH` guard in `lib/process/launch-template.ts`, deliberately
narrow on three axes:

- **Only when a tenant is named.** A platform launch (`tenantId = null`) is scoped to no tenant by
  design (CLAUDE.md) and has nothing to be inconsistent with; refusing there would break every
  admin/platform workflow.
- **Only scope, not format.** A non-uuid overlay value is left to the format check that already owns
  it — treating it as a mismatch would report the wrong reason for the wrong problem.
- **An absent row is not a mismatch.** Only a row that exists and names a *different* tenant refuses.

`__tests__/launch-overlay-tenant-scope.test.ts` covers all four, proven red on the first.

### How it was found, which is the uncomfortable half

By my own harness making the mistake. `drive-dormant-surface.mjs` built its overlay by taking the
OLDEST tenant and the NEWEST proposal independently — foundation and immobileyes' — and the comment
above the code said "Overlay values taken from LIVE rows, not invented", which was true and beside
the point. Both fixed: the overlay now derives the proposal and section FROM the chosen tenant, and
the 18 rows (harness residue, not product data) were cleared. The invariant checker now reports
`✓ no cross-tenant references anywhere in the schema`.

The runner's own doctrine is that a drive which cannot run is a failure, not a skip. This is the
neighbouring case: **a preflight that finds a violation and does not fail the run** reads as green
to anyone who trusts the summary line.


---

## B145 · A preflight found a cross-tenant violation and the suite still printed "0 failed" · FIXED

`run-branch-drives.sh` runs `check-tenant-isolation-invariant.mjs` as a preflight. When it found a
violation, the `else` branch printed a loud banner and the log tail — and **fell through**. No exit
code, no counter, nothing reaching the summary. The run that surfaced B144 printed:

```
✗ CROSS-TENANT REFERENCES FOUND — the rule is that data moves by inward COPY only:
    direct agent_task_log.proposal_id → proposals               18 row(s)
...
── 39 passed · 0 failed · 0 could-not-run · 0 missing ──
```

and exited **0**. A green that is not green, on the summary line most likely to be read without
scrolling up.

It contradicted the file's own rule, stated in its header: a drive that cannot run "is still a
FAILURE here — it is uncovered, not passing, the same rule the four lenses use". A stored reference
across the tenant boundary is strictly worse than uncovered: it is a violation of a non-negotiable
invariant, *found*. The neighbouring RLS-posture preflight already acts on what it sees — it marks
the isolation drives CANT-RUN — so the pattern existed and this one simply talked.

Fixed: the violation sets a flag, prints in the summary, and forces a non-zero exit. It deliberately
does **not** abort — the drives below still measure real things, and killing the run would trade one
blind spot for another.

Verified by injecting a single crossing row: the checker exits 1 with it present and 0 once removed,
and the summary arithmetic was tested for the unset case too, because `set -u` is on and an unset
counter is exactly the shape that made a green run die with a shell error before (see the comment
above the exit block).

**This is the gap that mattered most of the two.** B144 was one launcher accepting one bad overlay.
B145 is the instrument that was supposed to catch it reporting success anyway — and it is the reason
the violation reached a human as a footnote under "39 passed" rather than as a failure.


---

## B146 · Three ways running one suite silently broke another · FIXED

The branch drives passed 39/39. An hour later, after the persona e2e suite had run against the same
box, the identical command produced **four CANT-RUN and one FAIL** — with no code change in between.
Every one was cross-suite contamination, and each is the same shape: two things that must agree,
kept in two places.

### 1 · One account, two passwords

The branch drives resolve a `tenant_admin`'s password through `passwordFor()` — `TENANT_PW`.
`e2e/auth.setup.ts` signs the *same* account in with `LIGHTHOUSE_PW || 'LighthouseAdmin'`. Both are
correct alone; whichever suite ran last won. Running the personas first left `rls-app`,
`rls-portal` and `rls-pages` all reporting

```
CANT-RUN  could not authenticate as eric@lighthouse.com (tenant_admin) — error=invalid
```

which reads exactly like a deny-all isolation failure and is a password. Fixed by naming the account
in `sandbox-reset-passwords.mjs` and pointing `LIGHTHOUSE_PW` at `TENANT_PW` in `sandbox-env.sh`:
one credential, one place — the rule both those files already state (B109, B111).

### 2 · A fixture claiming a provenance it does not have

`scripts/e2e_fixtures.sql` seeds two Lighthouse `tenant_opportunity_cards` with `bridge_version = 1`
and never inserts bridge version 1. `drive-bridge-buckets` checks the spine invariant — every card
is a mirror produced by the forward-only bridge — and failed:

```
✗ no product-made card exists without a bridge event behind it  — lighthouse=2
```

The check is right and the fixture was wrong. A card that says it came from bridge v1 when no such
row exists is asserting a history it never had — the same error the ingest-provenance doctrine
forbids for solicitation values (*a value the product did not read from the solicitation must never
look like one it did*), applied to card provenance. The fixture now seeds its own bridge rows.
Bridgeless cards after: **0**.

### 3 · A resolver that did not select for what its drive needs

`run-branch-drives.sh` hands the `amendment` drive the newest curated solicitation that has
**volumes**. The drive needs one with an active **proposal** — an amendment fans out to the builds
made from it, so a solicitation nobody has built against has nothing to fan to. The two predicates
agreed by luck; the moment any newer volume-bearing solicitation appeared, the resolver handed over
one the drive had to refuse.

Measured directly: the resolver picked a solicitation with **0** active proposals while one with
**1** existed. The table then read CANT-RUN — which this runner's own header calls *uncovered, not
passing* — so the amendment path quietly stopped being covered, which is the exact failure the
runner was written to prevent ("run one at a time by hand, which is how one quietly stops being run
at all").

The join uses the direction `docs/SCHEMA_MAP.md` §4 warns about: the populated FK is
`opportunities.solicitation_id`, **not** `curated_solicitations.opportunity_id` (B46).

### The meta-point

None of the three is a product defect and all three degrade the evidence. A suite that passes only
when run in the right order is not passing — it is reporting the order. The failures were legible
here only because the drives print *why* they cannot run; the same three, under a harness that
skipped quietly, would have read as coverage.
