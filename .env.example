# === REQUIRED ===
DATABASE_URL=postgresql://govtech:changeme@localhost:5432/govtech_intel
AUTH_SECRET=                    # openssl rand -base64 32
AUTH_URL=http://localhost:3000
API_KEY_ENCRYPTION_SECRET=      # openssl rand -base64 32
STORAGE_ROOT=/data

# === RECOMMENDED ===
SAM_GOV_API_KEY=               # SAM.gov API key
ANTHROPIC_API_KEY=             # Claude API key
CLAUDE_MODEL=claude-sonnet-4-20250514

# === PAYMENTS ===
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_FINDER=           # Stripe price ID for $199/mo subscription
STRIPE_PRICE_PHASE1=           # Stripe price ID for $999 proposal
STRIPE_PRICE_PHASE2=           # Stripe price ID for $2500 proposal

# === EMAIL ===
RESEND_API_KEY=
EMAIL_FROM=noreply@rfppipeline.com

# === OPTIONAL ===
USE_STUB_DATA=false
LOG_LEVEL=info
APP_ENV=development
NODE_ENV=development

# === CMS SERVICE (not deployed V1) ===
CMS_SERVICE_URL=               # http://cms:8000 when deployed
CMS_DATABASE_URL=              # Separate CMS database
