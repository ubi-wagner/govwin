# RFP Pipeline — runtime dependencies
#
# KEEP LEAN. Every package here adds minutes to Railway build time
# and MBs to the image. Before adding one, confirm an import actually
# exists. Before keeping one, `grep -r "import <pkg>" src/` should
# return at least one hit.
#
# Last audit: 2026-04-22 — 19 deps → 7 deps, ~3GB → ~400MB

# DB
asyncpg>=0.29.0

# HTTP client (ingesters)
httpx>=0.27.0

# AWS SDK (S3 storage)
boto3>=1.34.0

# Claude API (shredder)
anthropic>=0.40.0

# PDF → markdown (shredder text extraction)
pymupdf4llm>=0.0.10

# AES-GCM for API-key encryption at rest
cryptography>=42.0.0

# Document agents — format-specific lifecycle agents (ingest→export)
# These are lightweight (no PyTorch). ~50MB total image delta.
python-docx>=1.1.0
python-pptx>=0.6.23
openpyxl>=3.1.0
lxml>=5.0.0

# .env loading in local dev (prod uses Railway env vars directly)
python-dotenv>=1.0.0
